---
phase: 01-foundation-authentication
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - tests/auth/signup.test.ts
  - tests/auth/login.test.ts
  - tests/auth/session.test.ts
  - tests/auth/reset-password.test.ts
  - tests/onboarding/profile.test.ts
  - tests/onboarding/timezone.test.tsx
  - tests/onboarding/interests.test.tsx
  - tests/lib/db.test.ts
  - tests/lib/validation.test.ts
  - tests/middleware.test.ts
  - src/middleware.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/components/auth/LoginForm.tsx
  - src/components/auth/SignupForm.tsx
  - .env.local.example
  - README.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - TECH-01
  - TECH-02
  - TECH-03
must_haves:
  truths:
    - "All auth flows have comprehensive test coverage (>80% per RESEARCH.md)"
    - "All onboarding components have unit tests with Testing Library"
    - "Error messages are gentle and helpful across all auth flows (D-07)"
    - "Middleware correctly protects routes and redirects based on auth state"
    - "Project is deployable with clear documentation in README.md"
    - "All environment variables are documented in .env.local.example"
  artifacts:
    - path: "tests/auth/signup.test.ts"
      provides: "Email/password signup flow tests"
      contains: "describe('Signup flow')"
    - path: "tests/auth/login.test.ts"
      provides: "Login flow tests (all three methods)"
      contains: "describe('Login flow')"
    - path: "tests/auth/session.test.ts"
      provides: "Session persistence and logout tests"
      contains: "describe('Session management')"
    - path: "README.md"
      provides: "Project documentation and setup instructions"
      contains: "# FocusFlow - Setup Instructions"
  key_links:
    - from: "tests/"
      to: "vitest configuration"
      via: "npm test command"
      pattern: "vitest.config.ts"
    - from: "README.md"
      to: "Project setup"
      via: "Setup instructions section"
      pattern: "## Getting Started"
    - from: ".env.local.example"
      to: "Environment configuration"
      via: "Environment variables template"
      pattern: "MONGODB_URI, NEXTAUTH_SECRET"
---

<objective>
Create comprehensive test coverage for authentication and onboarding flows, polish error messages to be gentle and helpful, verify middleware route protection, and create project documentation for deployment.

Purpose: Complete Phase 1 with quality assurance per RESEARCH.md §Validation Architecture (80%+ test coverage), ensure all error messages follow D-07 (gentle, helpful), and document project for deployment readiness.

Output: Full test suite passing with >80% coverage, gentle error messages throughout, verified middleware protection, and comprehensive README.md for local development and deployment.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md

# Locked decisions from CONTEXT.md
- D-07: Use gentle, helpful error messages throughout auth flows
- All Phase 1 requirements: AUTH-01 through AUTH-05, ONBD-01 through ONBD-04, TECH-01 through TECH-03

# Architecture patterns from RESEARCH.md
- Validation Architecture: Test framework (vitest), Phase Requirements → Test Map, Sampling Rate
- Wave 0 Gaps: Missing test files that need to be created
- Code examples: Gentle Error Messages (already implemented in Plan 03)
- Success criteria: Full suite green + coverage >80% for auth/onboarding modules
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create authentication flow tests (signup, login, session, password reset)</name>
  <files>tests/auth/signup.test.ts, tests/auth/login.test.ts, tests/auth/session.test.ts, tests/auth/reset-password.test.ts</files>
  <action>
Create comprehensive tests for all authentication flows per RESEARCH.md §Validation Architecture:

1. Create tests/auth/signup.test.ts:
   ```typescript
   import { describe, it, expect, beforeEach, afterEach } from 'vitest'
   import { connectDB, mongoose } from '@/lib/db'
   import { User } from '@/models/User'
   import bcrypt from 'bcryptjs'

   describe('Signup Flow', () => {
     beforeEach(async () => {
       await connectDB()
     })

     afterEach(async () => {
       await User.deleteMany({})
       await mongoose.disconnect()
     })

     it('should create user with email/password', async () => {
       const response = await fetch('http://localhost:3000/api/auth/signup', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({
           email: 'test@example.com',
           password: 'Password123',
           name: 'Test User'
         })
       })

       const data = await response.json()
       expect(response.ok).toBe(true)
       expect(data.success).toBe(true)

       const user = await User.findOne({ email: 'test@example.com' })
       expect(user).toBeTruthy()
       expect(user?.name).toBe('Test User')
     })

     it('should reject weak passwords', async () => {
       const response = await fetch('http://localhost:3000/api/auth/signup', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({
           email: 'test@example.com',
           password: 'weak',
           name: 'Test User'
         })
       })

       const data = await response.json()
       expect(response.ok).toBe(false)
       expect(data.error).toContain('stronger')
     })

     it('should reject duplicate email', async () => {
       await User.create({
         email: 'test@example.com',
         password: await bcrypt.hash('Password123', 10),
         name: 'Existing User'
       })

       const response = await fetch('http://localhost:3000/api/auth/signup', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({
           email: 'test@example.com',
           password: 'Password123',
           name: 'New User'
         })
       })

       const data = await response.json()
       expect(response.ok).toBe(false)
       expect(data.error).toContain('already exists')
     })
   })
   ```

2. Create tests/auth/login.test.ts covering all three login methods (credentials, Google OAuth, magic link)

3. Create tests/auth/session.test.ts covering session persistence and logout

4. Create tests/auth/reset-password.test.ts covering password reset flow via email

This implements Wave 0 Gaps per RESEARCH.md §Validation Architecture.
  </action>
  <verify>
    <automated>npm test -- tests/auth/ passes all authentication flow tests</automated>
  </verify>
  <done>Comprehensive authentication tests created for signup, login, session, and password reset flows</done>
</task>

<task type="auto">
  <name>Task 2: Create onboarding component tests (profile, timezone, interests)</name>
  <files>tests/onboarding/profile.test.ts, tests/onboarding/timezone.test.tsx, tests/onboarding/interests.test.tsx</files>
  <action>
Create tests for onboarding components per RESEARCH.md §Validation Architecture:

1. Create tests/onboarding/profile.test.ts for profile creation and photo upload

2. Create tests/onboarding/timezone.test.tsx for timezone detection and selection

3. Create tests/onboarding/interests.test.tsx for interest tag selection

4. Ensure all tests use Testing Library for component testing

This implements Wave 0 Gaps per RESEARCH.md §Validation Architecture.
  </action>
  <verify>
    <automated>npm test -- tests/onboarding/ passes all onboarding component tests</automated>
  </verify>
  <done>Onboarding component tests created for profile, timezone, and interest selection</done>
</task>

<task type="auto">
  <name>Task 3: Verify and polish error messages to be gentle and helpful</name>
  <files>src/lib/validation.ts, src/components/auth/LoginForm.tsx, src/components/auth/SignupForm.tsx, src/app/api/auth/[...nextauth]/route.ts</files>
  <action>
Review all error messages across authentication flows to ensure they follow D-07 (gentle, helpful):

1. Review src/lib/validation.ts error messages:
   - Check for technical jargon ("Invalid credentials" → "Hmm, that password doesn't match")
   - Ensure messages are empathetic and constructive
   - Add helpful suggestions ("Try again or reset it if you forgot")

2. Review auth form components (LoginForm, SignupForm):
   - Ensure error messages are displayed in user-friendly format
   - Add color coding (red for errors, green for success)
   - Include helpful hints (e.g., "Check your spam folder" for magic links)

3. Review NextAuth.js configuration error messages:
   - Ensure authorize() function returns gentle errors
   - Check OAuth error handling
   - Verify magic link error messages

4. Create test file tests/lib/error-messages.test.ts to verify gentle tone:
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { authErrorMessages } from '@/lib/validation'

   describe('Error Message Tone', () => {
     it('should use gentle language', () => {
       expect(authErrorMessages.invalidEmail).not.toContain('Invalid')
       expect(authErrorMessages.weakPassword).not.toContain('weak')
       expect(authErrorMessages.incorrectPassword).toContain('Hmm')
     })

     it('should provide helpful suggestions', () => {
       expect(authErrorMessages.incorrectPassword).toContain('Try again')
       expect(authErrorMessages.magicLinkExpired).toContain('fresh one')
     })

     it('should avoid technical jargon', () => {
       expect(authErrorMessages.userNotFound).not.toContain('404')
       expect(authErrorMessages.emailExists).not.toContain('duplicate')
     })
   })
   ```

This implements D-07 per CONTEXT.md decision.
  </action>
  <verify>
    <automated>npm test -- tests/lib/error-messages.test.ts confirms all error messages are gentle and helpful</automated>
  </verify>
  <done>All error messages reviewed and polished to be gentle, helpful, and free of technical jargon</done>
</task>

<task type="auto">
  <name>Task 4: Verify middleware route protection and create middleware tests</name>
  <files>src/middleware.ts, tests/middleware.test.ts</files>
  <action>
Verify middleware correctly protects routes and create comprehensive tests:

1. Review src/middleware.ts:
   - Confirm it uses NextAuth.js withAuth
   - Verify it checks isOnboarded flag
   - Ensure it redirects non-onboarded users to /onboarding/step1
   - Confirm matcher protects /dashboard, /rooms, /api/protected

2. Create tests/middleware.test.ts:
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { NextRequest } from 'next/server'

   describe('Middleware', () => {
     it('should redirect unauthenticated users from /dashboard to /login', async () => {
       // Mock NextRequest without session
       const request = new NextRequest(new URL('http://localhost:3000/dashboard'))

       // Test middleware behavior
       // This requires integration testing with Next.js test utils
     })

     it('should redirect non-onboarded users to /onboarding/step1', async () => {
       // Test with authenticated but non-onboarded session
     })

     it('should allow onboarded users to access /dashboard', async () => {
       // Test with authenticated and onboarded session
     })
   })
   ```

3. Test manually:
   - Access /dashboard without auth → should redirect to /login
   - Access /dashboard with auth but no onboarding → should redirect to /onboarding/step1
   - Access /dashboard with auth and onboarding → should show dashboard
   - Access /onboarding/step1 without auth → should redirect to /login

This implements middleware verification per RESEARCH.md Pattern 5.
  </action>
  <verify>
    <automated>npm test -- tests/middleware.test.ts passes middleware protection tests</automated>
  </verify>
  <done>Middleware verified to correctly protect routes and redirect based on auth and onboarding state</done>
</task>

<task type="auto">
  <name>Task 5: Run full test suite and verify >80% coverage</name>
  <files>vitest.config.ts, package.json</files>
  <action>
Ensure comprehensive test coverage per RESEARCH.md §Validation Architecture:

1. Update vitest.config.ts to enable coverage:
   ```typescript
   import { defineConfig } from 'vitest/config'
   // ... existing config

   export default defineConfig({
     // ... existing config
     test: {
       // ... existing test config
       coverage: {
         provider: 'v8',
         reporter: ['text', 'json', 'html'],
         exclude: [
           'node_modules/',
           'tests/',
           '**/*.test.ts',
           '**/*.test.tsx',
           'src/app/api/auth/[...nextauth]/route.ts', // NextAuth internal
         ]
       }
     }
   })
   ```

2. Install coverage provider:
   ```bash
   npm install -D @vitest/coverage-v8
   ```

3. Run full test suite with coverage:
   ```bash
   npm run test:all
   ```

4. Verify coverage >80% for:
   - src/lib/ (db.ts, auth.ts, validation.ts, email.ts)
   - src/models/ (User.ts)
   - src/components/auth/ (LoginForm.tsx, SignupForm.tsx)
   - src/components/onboarding/ (all step components)

5. If coverage <80%, add missing tests until threshold is met

This implements RESEARCH.md §Validation Architecture Sampling Rate (phase gate: coverage >80%).
  </action>
  <verify>
    <automated>npm run test:all shows coverage >80% for auth and onboarding modules</automated>
  </verify>
  <done>Full test suite passing with >80% coverage for authentication and onboarding code</done>
</task>

<task type="auto">
  <name>Task 6: Create comprehensive README.md with setup and deployment instructions</name>
  <files>README.md, .env.local.example</files>
  <action>
Create comprehensive README.md documenting project setup, local development, and deployment:

1. Create README.md:
   ```markdown
   # FocusFlow - ADHD Focus Rooms

   A web-based focus accountability platform for people with ADHD and focus challenges.

   ## Tech Stack

   - **Frontend**: Next.js 16.2.2 with App Router, React 19, TypeScript 5.x, Tailwind CSS 3.4+
   - **Backend**: Node.js 22.x LTS, Next.js API Routes
   - **Database**: MongoDB 7.0+ with Mongoose 8.x ODM
   - **Authentication**: NextAuth.js 4.24.13 (magic link primary, Google OAuth optional, email/password fallback)
   - **UI Components**: shadcn/ui (Radix UI + Tailwind CSS)
   - **Testing**: Vitest 4.1.2 + Testing Library
   - **State Management**: Zustand 4.x

   ## Prerequisites

   - Node.js 22.x LTS
   - Docker (for local MongoDB)
   - npm or yarn

   ## Getting Started

   ### 1. Clone and Install

   \`\`\`bash
   git clone <repository-url>
   cd intentiveclone2
   npm install
   \`\`\`

   ### 2. Start MongoDB (Docker)

   \`\`\`bash
   npm run docker:mongo
   \`\`\`

   MongoDB will be available at `mongodb://localhost:27017/focusflow`

   ### 3. Configure Environment Variables

   Copy `.env.local.example` to `.env.local`:

   \`\`\`bash
   cp .env.local.example .env.local
   \`\`\`

   Edit `.env.local` and configure:

   \`\`\`bash
   # Database
   MONGODB_URI=mongodb://localhost:27017/focusflow

   # NextAuth.js
   NEXTAUTH_URL=http://localhost:3000
   NEXTAUTH_SECRET=your-secret-key-generate-with-openssl-rand-base64-32

   # Email (for magic links - use Mailtrap for development)
   EMAIL_SERVER_HOST=smtp.resend.com
   EMAIL_SERVER_PORT=587
   EMAIL_SERVER_USER=resend
   EMAIL_SERVER_PASSWORD=your-api-key
   EMAIL_FROM=noreply@focusflow.com

   # Google OAuth (optional - get from Google Cloud Console)
   GOOGLE_CLIENT_ID=your-google-client-id
   GOOGLE_CLIENT_SECRET=your-google-client-secret
   \`\`\`

   Generate NEXTAUTH_SECRET:
   \`\`\`bash
   openssl rand -base64 32
   \`\`\`

   ### 4. Run Development Server

   \`\`\`bash
   npm run dev
   \`\`\`

   Visit [http://localhost:3000](http://localhost:3000)

   ## Testing

   Run tests:
   \`\`\`bash
   npm test
   \`\`\`

   Run tests with coverage:
   \`\`\`bash
   npm run test:all
   \`\`\`

   ## Deployment

   ### DigitalOcean VPS Deployment

   1. **Install Node.js 22.x LTS on VPS**
   2. **Deploy MongoDB** (choose one):
      - MongoDB Atlas (recommended for MVP)
      - Docker container on VPS
   3. **Configure Environment Variables** on VPS
   4. **Build and Start**:
      \`\`\`bash
      npm run build
      npm start
      \`\`\`
   5. **Use PM2 for Process Management**:
      \`\`\`bash
      npm install -g pm2
      pm2 start npm --name "focusflow" -- start
      pm2 save
      pm2 startup
      \`\`\`

   ## Project Structure

   \`\`\`
   src/
   ├── app/                    # Next.js App Router
   │   ├── (auth)/             # Auth route group
   │   ├── (onboarding)/       # Onboarding route group
   │   ├── api/                # API routes
   │   └── layout.tsx          # Root layout
   ├── components/             # React components
   │   ├── ui/                 # shadcn/ui components
   │   ├── auth/               # Auth components
   │   └── onboarding/         # Onboarding components
   ├── lib/                    # Utility functions
   │   ├── db.ts               # MongoDB connection
   │   ├── auth.ts             # Auth helpers
   │   └── validation.ts       # Zod schemas
   ├── models/                 # Mongoose models
   │   └── User.ts             # User model
   └── middleware.ts           # Next.js middleware
   \`\`\`

   ## Authentication

   FocusFlow supports three authentication methods:
   1. **Magic Link** (primary) - Passwordless login via email
   2. **Google OAuth** (optional) - One-click Google login
   3. **Email/Password** (fallback) - Traditional credentials

   ## License

   MIT
   \`\`\`

2. Update .env.local.example with all required variables and comments

This implements deployment documentation per Phase 1 completion criteria.
  </action>
  <verify>
    <automated>README.md exists with all sections filled out, .env.local.example documents all required environment variables</automated>
  </verify>
  <done>Comprehensive README.md created with setup instructions, environment variable documentation, and deployment guide</done>
</task>

<task type="auto">
  <name>Task 7: Final verification and documentation</name>
  <files>.planning/phases/01-foundation-authentication/01-PHASE-SUMMARY.md</files>
  <action>
Perform final verification of all Phase 1 requirements and create phase summary:

1. Verify all success criteria from ROADMAP.md:
   - [ ] User can create account with email/password and log in across browser sessions
   - [ ] User can sign up with Google OAuth
   - [ ] User can reset password via email link and log out from any page
   - [ ] User completes onboarding flow (name, photo, timezone, interests) with skippable tour
   - [ ] User's timezone is auto-detected with manual override option
   - [ ] Next.js 16.2.2 project with App Router is configured and deployable
   - [ ] MongoDB 7.0+ database is connected with Mongoose 8.x ORM and core schema defined

2. Verify all locked decisions from CONTEXT.md are implemented:
   - [ ] D-01: shadcn/ui components used
   - [ ] D-02 through D-16: All authentication and onboarding decisions implemented

3. Verify test coverage >80% for auth and onboarding modules

4. Create phase summary document with:
   - What was built
   - Technical decisions made
   - Test results
   - Known issues or workarounds
   - Next steps for Phase 2

This completes Phase 1 planning and verification.
  </action>
  <verify>
    <automated>All 7 success criteria from ROADMAP.md verified, all 16 locked decisions from CONTEXT.md implemented, test coverage >80%</automated>
  </verify>
  <done>Phase 1 complete with all requirements met, comprehensive documentation, and verified test coverage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Code→Production Code | Test isolation, mocking |
| Documentation→User | Setup instructions, security warnings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Environment variables in git | mitigate | .env.local in .gitignore, .env.local.example provided as template |
| T-05-02 | Information Disclosure | README secrets | mitigate | Document security requirements (NEXTAUTH_SECRET generation), never commit real secrets |
| T-05-03 | Denial of Service | Test resource leaks | mitigate | Proper test cleanup (afterEach hooks), database connection teardown |
| T-05-04 | Spoofing | Test authentication bypass | mitigate | Mock NextAuth sessions properly, never use real credentials in tests |
</threat_model>

<verification>
## Phase Checks

- [ ] All authentication tests pass (signup, login, session, password reset)
- [ ] All onboarding tests pass (profile, timezone, interests)
- [ ] Middleware tests confirm route protection
- [ ] Test coverage >80% for auth and onboarding modules
- [ ] All error messages are gentle and helpful (verified by tests)
- [ ] README.md documents complete setup process
- [ ] .env.local.example includes all required variables
- [ ] All 7 success criteria from ROADMAP.md verified
- [ ] All 16 locked decisions from CONTEXT.md implemented
</verification>

<success_criteria>
1. Developer can follow README.md to set up project locally in <15 minutes
2. All tests pass with npm test
3. Test coverage report shows >80% for auth and onboarding modules
4. No sensitive information (API keys, secrets) in git repository
5. All error messages in auth flows are gentle and helpful
6. Middleware correctly protects routes based on auth state
7. Project is deployable to DigitalOcean VPS with documented process
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-05-SUMMARY.md` with:
- Test results: XX tests passing, YY% coverage
- All 7 success criteria verified
- All 16 locked decisions implemented
- Known issues (if any)
- Recommendations for Phase 2 (Room Management)
- Final assessment: Phase 1 complete and ready for deployment
</output>

---
phase: 07-analytics-operations
plan: 05
type: execute
wave: 4
depends_on: [07-01, 07-04]
files_modified:
  - src/app/admin/bandwidth/page.tsx
  - src/components/admin/BandwidthOverview.tsx
  - src/components/admin/BandwidthChart.tsx
  - src/components/admin/BandwidthAlerts.tsx
  - src/lib/bandwidth-alerts.ts
autonomous: false
requirements:
  - TECH-06
must_haves:
  truths:
    - Admin can view bandwidth dashboard with daily totals and trends
    - Admin sees per-room bandwidth breakdown
    - Admin sees relay vs direct ratio
    - Admin sees estimated cost with trends
    - Admin receives alerts when bandwidth exceeds thresholds
  artifacts:
    - path: src/app/admin/bandwidth/page.tsx
      provides: "Bandwidth monitoring dashboard page"
      exports: ["default"]
    - path: src/components/admin/BandwidthOverview.tsx
      provides: "Bandwidth summary cards component"
      exports: ["BandwidthOverview"]
    - path: src/components/admin/BandwidthChart.tsx
      provides: "Simple SVG chart for bandwidth trends"
      exports: ["BandwidthChart"]
    - path: src/components/admin/BandwidthAlerts.tsx
      provides: "Alert configuration and display component"
      exports: ["BandwidthAlerts"]
    - path: src/lib/bandwidth-alerts.ts
      provides: "Bandwidth alert checking utilities"
      exports: ["checkBandwidthAlerts", "sendBandwidthAlert"]
  key_links:
    - from: src/app/admin/bandwidth/page.tsx
      to: src/app/api/admin/bandwidth/route.ts
      via: "fetch GET request"
      pattern: "fetch.*api/admin/bandwidth"
    - from: src/lib/bandwidth-alerts.ts
      to: src/lib/bandwidth-tracker.ts
      via: "getDailyStats function call"
      pattern: "getDailyStats"
---

<objective>
Create bandwidth monitoring dashboard UI with alerts for quota and cost thresholds.

Purpose: Provide admin visibility into TURN bandwidth usage and costs with proactive alerts.

Output: Bandwidth dashboard page, overview cards, simple chart, alerts component.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-operations/07-CONTEXT.md
@src/app/admin/page.tsx
@src/components/admin/DashboardCard.tsx
@src/lib/bandwidth-tracker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BandwidthOverview component</name>
  <files>src/components/admin/BandwidthOverview.tsx</files>
  <read_first>src/components/admin/DashboardCard.tsx</read_first>
  <action>
Create `src/components/admin/BandwidthOverview.tsx` with:

1. Props interface:
   ```typescript
   interface BandwidthOverviewProps {
     summary: {
       totalBytesRelayed: number
       totalBytesDirect: number
       totalCost: number
       relayVsDirectRatio: number
       totalGB: number
     }
     trends?: {
       costChange: number
       usageChange: number
     }
   }
   ```
2. Display 4 cards using DashboardCard component:
   - Total Bandwidth (GB, with trend vs previous period)
   - Relay vs Direct Ratio (percentage, with trend)
   - Estimated Cost (USD, with trend)
   - Daily Average (GB, with trend)
3. Use DashboardCard for each metric
4. Color coding for relay ratio:
   - Green if < 30% (mostly direct, good)
   - Yellow if 30-70% (mixed)
   - Red if > 70% (mostly relay, expensive)

Per D-09: Full monitoring with daily totals and relay vs direct ratio.
Per D-13: Trend direction with arrows and percentage change.
  </action>
  <verify>
    <automated>grep -q "BandwidthOverviewProps" src/components/admin/BandwidthOverview.tsx && grep -q "DashboardCard" src/components/admin/BandwidthOverview.tsx && grep -q "relayVsDirectRatio" src/components/admin/BandwidthOverview.tsx</automated>
  </verify>
  <done>Overview displays 4 cards, colors change based on relay ratio, trends shown</done>
</task>

<task type="auto">
  <name>Task 2: Create simple SVG bandwidth chart</name>
  <files>src/components/admin/BandwidthChart.tsx</files>
  <read_first>src/components/ui/card.tsx</read_first>
  <action>
Create `src/components/admin/BandwidthChart.tsx` client component with:

1. Props interface:
   ```typescript
   interface BandwidthChartProps {
     data: Array<{
       date: string
       bytesRelayed: number
       bytesDirect: number
       cost: number
     }>
     height?: number
   }
   ```
2. Simple SVG line chart implementation (no external chart library for MVP)
3. Two lines: relay bytes (red), direct bytes (green)
4. X-axis: Dates, Y-axis: GB values
5. Auto-scale Y-axis based on max value
6. Hover tooltip showing date, relay GB, direct GB, cost
7. Legend showing which color is which
8. Responsive width (100%)

SVG implementation:
- ViewBox based on data points count and max value
- Polyline for each data series
- Circle points for each data point
- Tooltip on hover (show hidden div with data)

Per D-12: Minimal charts — simple line trends only.
  </action>
  <verify>
    <automated>grep -q "BandwidthChartProps" src/components/admin/BandwidthChart.tsx && grep -q "svg\|SVG" src/components/admin/BandwidthChart.tsx && grep -q "polyline\|Polyline" src/components/admin/BandwidthChart.tsx</automated>
  </verify>
  <done>Chart renders two lines, tooltips show data on hover, responsive to width</done>
</task>

<task type="auto">
  <name>Task 3: Create bandwidth alerts utilities</name>
  <files>src/lib/bandwidth-alerts.ts</files>
  <read_first>src/lib/bandwidth-tracker.ts</read_first>
  <action>
Create `src/lib/bandwidth-alerts.ts` with:

1. `checkBandwidthAlerts()` function:
   - Fetch today's bandwidth stats
   - Check quota percentage: (todayRelayBytes / MONTHLY_QUOTA_GB * 1e9) * 100
   - Check cost threshold: todayCost >= COST_THRESHOLD
   - Return alert object if threshold exceeded

2. Alert types:
   ```typescript
   interface BandwidthAlert {
     type: 'warning' | 'critical' | 'cost'
     message: string
     current: number
     threshold: number
     timestamp: Date
   }
   ```
3. Thresholds from environment variables:
   - BANDWIDTH_WARNING_THRESHOLD: 80 (default)
   - BANDWIDTH_CRITICAL_THRESHOLD: 90 (default)
   - BANDWIDTH_COST_THRESHOLD: 5.00 USD (default)
   - BANDWIDTH_MONTHLY_QUOTA_GB: 1000 GB (default)

4. `sendBandwidthAlert(alert)` function:
   - Send email to admin (use nodemailer from notifications)
   - Log alert to console for debugging

Per D-10: Dual alert system for quota % and cost threshold.
  </action>
  <verify>
    <automated>grep -q "checkBandwidthAlerts" src/lib/bandwidth-alerts.ts && grep -q "BANDWIDTH_WARNING_THRESHOLD\|BANDWIDTH_COST_THRESHOLD" src/lib/bandwidth-alerts.ts && grep -q "BandwidthAlert" src/lib/bandwidth-alerts.ts</automated>
  </verify>
  <done>Alert checking returns correct alert types, thresholds configurable via env</done>
</task>

<task type="auto">
  <name>Task 4: Create BandwidthAlerts component</name>
  <files>src/components/admin/BandwidthAlerts.tsx</files>
  <read_first>src/components/ui/card.tsx, src/components/ui/badge.tsx</read_first>
  <action>
Create `src/components/admin/BandwidthAlerts.tsx` with:

1. Props interface:
   ```typescript
   interface BandwidthAlertsProps {
     alerts: BandwidthAlert[]
   }
   ```
2. Display active alerts as alert banners
3. Color coding:
   - Warning: Yellow badge (bg-yellow-100 text-yellow-800)
   - Critical: Red badge (bg-red-100 text-red-800)
   - Cost: Orange badge (bg-orange-100 text-orange-800)
4. Show current vs threshold values
5. Add "Configure Alerts" button that opens dialog with threshold settings
6. Store threshold settings in environment (read-only display, actual config in .env)

Configuration display: Show current thresholds from env vars, note that actual configuration requires server restart.
  </action>
  <verify>
    <automated>grep -q "BandwidthAlertsProps" src/components/admin/BandwidthAlerts.tsx && grep -q "warning\|critical\|cost" src/components/admin/BandwidthAlerts.tsx && grep -q "Configure" src/components/admin/BandwidthAlerts.tsx</automated>
  </verify>
  <done>Alerts display with correct colors, thresholds shown, config dialog opens</done>
</task>

<task type="auto">
  <name>Task 5: Create bandwidth monitoring page</name>
  <files>src/app/admin/bandwidth/page.tsx</files>
  <read_first>src/app/admin/page.tsx, src/components/admin/BandwidthOverview.tsx, src/components/admin/BandwidthChart.tsx</read_first>
  <action>
Create `src/app/admin/bandwidth/page.tsx` with:

1. Import `requireAdmin` for authorization
2. Client-side fetch from /api/admin/bandwidth?startDate=...&endDate=...
3. Parse query params for date range (default: past 30 days)
4. Render page structure:
   - Header: "Bandwidth Monitoring" with description
   - BandwidthOverview (summary cards)
   - BandwidthChart (30-day trend line)
   - BandwidthAlerts (active alerts section)
   - Room breakdown table (top 10 rooms by bandwidth)
5. Room breakdown columns: Room ID, Relay GB, Direct GB, Cost, Sessions
6. Add time range selector (reuse TimeRangeSelector from 07-02)

Data fetching: Use SWR or react-query for client-side polling, or fetch on mount with refresh button.
  </action>
  <verify>
    <automated>grep -q "requireAdmin" src/app/admin/bandwidth/page.tsx && grep -q "BandwidthOverview" src/app/admin/bandwidth/page.tsx && grep -q "BandwidthChart" src/app/admin/bandwidth/page.tsx && grep -q "api/admin/bandwidth" src/app/admin/bandwidth/page.tsx</automated>
  </verify>
  <done>Page loads bandwidth data, overview displays metrics, chart shows trends, alerts display</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Bandwidth monitoring dashboard with overview, chart, and alerts</what-built>
  <how-to-verify>
1. Visit https://localhost:3000/admin/bandwidth (must be admin)
2. Verify bandwidth overview cards display metrics
3. Verify chart shows 30-day bandwidth trend
4. Verify room breakdown table shows top bandwidth consumers
5. If bandwidth data exists, verify relay vs direct ratio calculation
6. Verify cost estimate is calculated correctly
7. Check that alerts section displays any active warnings
8. Test time range selector to change date range
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the bandwidth dashboard</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→bandwidth API | Untrusted admin client requests bandwidth data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | S | Bandwidth dashboard | mitigate | assertAdmin check on API and page |
| T-07-16 | I | Bandwidth alerts | accept | Operational data, no user PII |
| T-07-17 | D | Alert thresholds | accept | Read-only display, server config required |
</threat_model>

<verification>
- BandwidthOverview displays 4 metric cards
- BandwidthChart renders SVG with two lines
- BandwidthAlerts shows active alerts with correct colors
- Bandwidth page fetches and displays data
- Alert thresholds configurable via env vars
- Room breakdown table shows top consumers
</verification>

<success_criteria>
1. Admin can navigate to /admin/bandwidth and see dashboard
2. Overview cards show total bandwidth, ratio, cost, daily average
3. Chart displays relay and direct trends over time
4. Room breakdown shows top bandwidth consumers
5. Alerts display when thresholds exceeded
6. Time range selector updates chart and stats
7. Human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-operations/07-05-SUMMARY.md`
</output>

---
phase: 07-analytics-operations
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/app/admin/users/page.tsx
  - src/app/api/admin/users/[userId]/route.ts
  - src/components/admin/UserTable.tsx
  - src/components/admin/BanSuspendModal.tsx
  - src/models/User.ts
  - src/models/AdminLog.ts
autonomous: false
requirements:
  - ADMN-05
must_haves:
  truths:
    - Admin can view list of all users with search and filter
    - Admin can ban or suspend users with reason and duration
    - Admin sees user details (signup date, status, last seen, sessions, no-show rate, streak)
    - System logs all admin actions (ban, suspend) for audit trail
  artifacts:
    - path: src/app/admin/users/page.tsx
      provides: "User management page"
      exports: ["default"]
    - path: src/components/admin/UserTable.tsx
      provides: "User list table with search/filter"
      exports: ["UserTable"]
    - path: src/components/admin/BanSuspendModal.tsx
      provides: "Modal for ban/suspend actions"
      exports: ["BanSuspendModal"]
    - path: src/app/api/admin/users/[userId]/route.ts
      provides: "API for user status updates"
      exports: ["PATCH"]
    - path: src/models/AdminLog.ts
      provides: "Admin action audit log model"
      exports: ["AdminLog"]
  key_links:
    - from: src/app/admin/users/page.tsx
      to: src/app/api/admin/users/[userId]/route.ts
      via: "fetch PATCH request"
      pattern: "PATCH.*api/admin/users"
    - from: src/app/api/admin/users/[userId]/route.ts
      to: src/models/AdminLog.ts
      via: "AdminLog.create"
      pattern: "AdminLog\\.create"
    - from: src/app/admin/users/page.tsx
      to: src/models/User.ts
      via: "User.find queries"
      pattern: "User\\.find"
---

<objective>
Create user management page with search/filter, ban/suspend modal, and audit logging.

Purpose: Enable admins to manage problematic users with proper accountability through audit trails.

Output: User management page with full user list, search/filter, ban/suspend with reason, admin action logging.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-operations/07-CONTEXT.md
@src/models/User.ts
@src/lib/admin.ts
@src/components/admin/CaptainAssignment.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend User model with status fields</name>
  <files>src/models/User.ts</files>
  <read_first>src/models/User.ts, src/models/types.ts</read_first>
  <action>
Update `src/models/User.ts` schema to add:

1. Status field:
   ```typescript
   status: {
     type: String,
     enum: ['active', 'suspended', 'banned'],
     default: 'active',
     index: true
   }
   ```
2. Ban reason field:
   ```typescript
   banReason: {
     type: String,
     trim: true
   }
   ```
3. Ban expiration field:
   ```typescript
   banExpiresAt: {
     type: Date,
     index: true
   }
   ```
4. Update IUser interface in `src/models/types.ts` to include these fields
5. Add index on status for filtering queries

Per D-06: Modal form requires reason and duration — schema must support these.
Per D-07: User list shows status (active, suspended, banned).
  </action>
  <verify>
    <automated>grep -q "status.*enum.*active.*suspended.*banned" src/models/User.ts && grep -q "banReason" src/models/User.ts && grep -q "banExpiresAt" src/models/User.ts && grep -q "status.*suspended.*banned" src/models/types.ts</automated>
  </verify>
  <done>User schema has status, banReason, banExpiresAt fields; types.ts IUser interface updated</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminLog model for audit trail</name>
  <files>src/models/AdminLog.ts</files>
  <read_first>src/models/User.ts</read_first>
  <action>
Create `src/models/AdminLog.ts` with:

1. Schema definition:
   ```typescript
   interface IAdminLog {
     adminId: mongoose.Types.ObjectId
     action: 'ban' | 'suspend' | 'unsuspend' | 'unban' | 'delete_room' | 'assign_captain' | string
     targetUserId?: mongoose.Types.ObjectId
     targetRoomId?: mongoose.Types.ObjectId
     reason: string
     details?: Record<string, any>
     createdAt: Date
   }
   ```
2. Required fields: adminId, action, reason
3. At least one of targetUserId or targetRoomId required
4. Indexes: adminId, action, createdAt (for querying recent actions)
5. Compound index: adminId + createdAt (for admin's action history)
6. Hot-reload protection pattern

Per D-16: Log user actions in admin panel for audit trail.
  </action>
  <verify>
    <automated>grep -q "export.*AdminLog" src/models/AdminLog.ts && grep -q "adminId" src/models/AdminLog.ts && grep -q "action.*ban.*suspend" src/models/AdminLog.ts && grep -q "mongoose.models.AdminLog" src/models/AdminLog.ts</automated>
  </verify>
  <done>AdminLog model created, indexes defined, exports IAdminLog type</done>
</task>

<task type="auto">
  <name>Task 3: Create user management API endpoint</name>
  <files>src/app/api/admin/users/[userId]/route.ts</files>
  <read_first>src/lib/admin.ts, src/models/User.ts, src/models/AdminLog.ts</read_first>
  <action>
Create `src/app/api/admin/users/[userId]/route.ts` with:

1. GET endpoint: Fetch user by ID with stats
   - Use `assertAdmin` for authorization
   - Return user document with: name, email, status, signup date (createdAt), last seen (updatedAt), sessions attended count, no-show rate, current streak
2. PATCH endpoint: Update user status
   - Use `assertAdmin` for authorization
   - Accept: { action: 'ban' | 'suspend' | 'unsuspend', reason: string, duration?: '1day' | '1week' | '1month' | 'permanent' }
   - For ban: status='banned', banReason=reason, banExpiresAt=permanent ? null : Date.now() + duration
   - For suspend: status='suspended', banReason=reason, banExpiresAt=Date.now() + duration
   - For unsuspend/unban: status='active', clear banReason and banExpiresAt
   - Create AdminLog entry with adminId, action, targetUserId, reason
   - Return updated user document

Duration mapping:
- 1day: 24 hours from now
- 1week: 7 days from now
- 1month: 30 days from now
- permanent: banExpiresAt = null (never expires)

Per D-06: Modal with reason and duration — API must accept and store these.
  </action>
  <verify>
    <automated>grep -q "assertAdmin" src/app/api/admin/users/[userId]/route.ts && grep -q "PATCH" src/app/api/admin/users/[userId]/route.ts && grep -q "AdminLog.create" src/app/api/admin/users/[userId]/route.ts</automated>
  </verify>
  <done>GET returns user stats, PATCH updates status and creates AdminLog entry</done>
</task>

<task type="auto">
  <name>Task 4: Create BanSuspendModal component</name>
  <files>src/components/admin/BanSuspendModal.tsx</files>
  <read_first>src/components/ui/dialog.tsx, src/components/ui/select.tsx, src/components/ui/textarea.tsx</read_first>
  <action>
Create `src/components/admin/BanSuspendModal.tsx` client component with:

1. Props interface:
   ```typescript
   interface BanSuspendModalProps {
     user: { id: string, name: string, email: string, status: string }
     open: boolean
     onClose: () => void
     onSuccess: () => void
   }
   ```
2. Use shadcn/ui Dialog component
3. Form fields:
   - Action type: Select (Ban, Suspend, Unsuspend/Unban) — pre-select based on current status
   - Duration: Select (1 day, 1 week, 1 month, Permanent) — only for ban/suspend
   - Reason: Textarea required, min length 10 chars
4. Warning styling for ban actions (red destructive button)
5. Confirmation step: Show summary before submitting
6. Submit button calls PATCH /api/admin/users/[userId] with form data
7. Error handling: Display error message on failure

Per D-06: Modal with reason and duration, prevents accidental actions.
  </action>
  <verify>
    <automated>grep -q "BanSuspendModalProps" src/components/admin/BanSuspendModal.tsx && grep -q "Dialog" src/components/admin/BanSuspendModal.tsx && grep -q "required.*minLength.*10" src/components/admin/BanSuspendModal.tsx</automated>
  </verify>
  <done>Modal renders form, validation requires reason, submit calls API, success closes modal</done>
</task>

<task type="auto">
  <name>Task 5: Create UserTable component</name>
  <files>src/components/admin/UserTable.tsx</files>
  <read_first>src/components/ui/table.tsx, src/components/admin/BanSuspendModal.tsx</read_first>
  <action>
Create `src/components/admin/UserTable.tsx` client component with:

1. Props interface:
   ```typescript
   interface UserTableProps {
     users: UserListItem[]
   }

   interface UserListItem {
     _id: string
     name: string
     email: string
     status: 'active' | 'suspended' | 'banned'
     createdAt: string
     lastSeen: string
     sessionsAttended: number
     noShowRate: number
     streak: number
   }
   ```
2. Use shadcn/ui Table components (Table, TableBody, TableCell, TableHead, TableHeader, TableRow)
3. Columns: Name, Email, Status (badge), Signup Date, Last Seen, Sessions, No-Show Rate, Streak, Actions
4. Status badge colors: active (green), suspended (yellow), banned (red)
5. Search input: Filter by email or name
6. Filter dropdown: Filter by status (All, Active, Suspended, Banned)
7. Actions column: "Manage" button opens BanSuspendModal
8. Pagination: 25 users per page

Per D-07: User list with search by email and filter by status.
Per D-08: Search by email and filter by status for efficiency.
  </action>
  <verify>
    <automated>grep -q "UserTableProps" src/components/admin/UserTable.tsx && grep -q "status.*badge" src/components/admin/UserTable.tsx && grep -q "search\|filter" src/components/admin/UserTable.tsx</automated>
  </verify>
  <done>Table renders users, search filters by name/email, status dropdown filters, Manage button opens modal</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>User management page with search/filter, user table, and ban/suspend modal</what-built>
  <how-to-verify>
1. Visit https://localhost:3000/admin/users (must be admin)
2. Verify user list loads with all users
3. Test search: Type email or name in search box, verify list filters
4. Test status filter: Select "Suspended" from dropdown, verify only suspended users shown
5. Test ban action: Click "Manage" on a user, select "Ban", choose duration, enter reason, submit
6. Verify user status changes to "banned" in the list
7. Test suspend action: Click "Manage", select "Suspend", choose "1 week", enter reason, submit
8. Verify user status changes to "suspended"
9. Test unsuspend: Click "Manage" on suspended user, select "Unsuspend", submit
10. Verify user status returns to "active"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the user management functionality</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Create user management page</name>
  <files>src/app/admin/users/page.tsx</files>
  <read_first>src/app/admin/rooms/page.tsx, src/components/admin/UserTable.tsx</read_first>
  <action>
Create `src/app/admin/users/page.tsx` with:

1. Import `requireAdmin` for authorization
2. Server-side fetch: Get all users with stats using aggregation
   - Project: name, email, status, createdAt, updatedAt (lastSeen)
   - Lookup: Count sessions attended from Registration where status='attended'
   - Calculate: no-show rate (no-show count / total registrations)
   - Lookup: Current streak from Streak model
3. Pass users to UserTable component
4. Page header: "User Management" with description
5. Add "Add New User" button (placeholder for future) or "Export" button (optional)

User aggregation: Use MongoDB aggregation pipeline for efficient stats calculation across all users.
  </action>
  <verify>
    <automated>grep -q "requireAdmin" src/app/admin/users/page.tsx && grep -q "UserTable" src/app/admin/users/page.tsx && grep -q "aggregate\|find" src/app/admin/users/page.tsx</automated>
  </verify>
  <done>Page loads all users with stats, UserTable displays data correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→user management | Admin modifies user status (potentially abusive) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | S | User management API | mitigate | assertAdmin check, verify admin role on every request |
| T-07-09 | R | Ban/suspend actions | mitigate | AdminLog audit trail captures every action with adminId, reason, timestamp |
| T-07-10 | E | AdminLog storage | mitigate | AdminLog documents immutable (no update/delete endpoints) |
| T-07-11 | S | User data exposure | accept | Admin-only access enforced, no public endpoint |
</threat_model>

<verification>
- User model extended with status fields
- AdminLog model created with indexes
- API endpoint enforces admin authorization
- BanSuspendModal validates form input
- UserTable handles search and filter
- Ban/suspend actions create AdminLog entries
- Admin actions are logged with full details
</verification>

<success_criteria>
1. Admin can view all users with stats (sessions, no-show rate, streak)
2. Search by email/name works
3. Filter by status works
4. Ban/suspend modal requires reason and duration
5. Ban/suspend actions update user status
6. AdminLog captures every admin action
7. Human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-operations/07-03-SUMMARY.md`
</output>

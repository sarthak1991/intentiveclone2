---
phase: 07-analytics-operations
plan: 07
type: execute
wave: 5
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06]
files_modified:
  - .env.local.example
  - src/middleware.ts
  - README.md
  - .planning/phases/07-analytics-operations/VERIFICATION.md
autonomous: false
requirements:
  - ADMN-02, ADMN-03, ADMN-05, TECH-06
must_haves:
  truths:
    - All environment variables documented
    - Admin routes protected by middleware
    - Production deployment guide exists
    - Phase requirements verified complete
  artifacts:
    - path: .env.local.example
      provides: "Complete environment variable documentation"
      contains: "All new Phase 7 env vars"
    - path: src/middleware.ts
      provides: "Route protection middleware"
      exports: ["middleware"]
    - path: README.md
      provides: "Updated project documentation"
      contains: "Phase 7 features"
    - path: .planning/phases/07-analytics-operations/VERIFICATION.md
      provides: "Phase 7 verification checklist"
      contains: "All requirement verifications"
  key_links:
    - from: README.md
      to: .planning/ROADMAP.md
      via: "Phase reference"
      pattern: "Phase 7.*Analytics"
    - from: .env.local.example
      to: src/lib/bandwidth-alerts.ts
      via: "Environment variable references"
      pattern: "BANDWIDTH_"
---

<objective>
Update documentation, verify all Phase 7 requirements, and finalize for production readiness.

Purpose: Complete Phase 7 with comprehensive documentation and verification that all requirements are met.

Output: Updated documentation, verification checklist, phase summary ready for execution.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-operations/07-CONTEXT.md
@.env.local.example
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .env.local.example with Phase 7 variables</name>
  <files>.env.local.example</files>
  <read_first>.env.local.example, src/lib/bandwidth-alerts.ts, src/lib/logger.ts</read_first>
  <action>
Update `.env.local.example` to add Phase 7 environment variables:

1. Bandwidth monitoring variables:
   ```
   # Bandwidth Monitoring
   TURN_COST_PER_GB=0.01
   BANDWIDTH_WARNING_THRESHOLD=80
   BANDWIDTH_CRITICAL_THRESHOLD=90
   BANDWIDTH_COST_THRESHOLD=5.00
   BANDWIDTH_MONTHLY_QUOTA_GB=1000
   ```

2. Logging variables:
   ```
   # Logging
   LOG_LEVEL=info  # debug | info | warn | error
   LOG_FORMAT=json  # json | pretty
   ```

3. Update existing sections with descriptions if needed

4. Add comments explaining each variable's purpose and impact

Format: Keep consistent with existing .env.local.example format (section headers, inline comments).
  </action>
  <verify>
    <automated>grep -q "BANDWIDTH_WARNING_THRESHOLD\|TURN_COST_PER_GB\|LOG_LEVEL" .env.local.example</automated>
  </verify>
  <done>All Phase 7 env vars documented with descriptions</done>
</task>

<task type="auto">
  <name>Task 2: Verify middleware protects admin routes</name>
  <files>src/middleware.ts</files>
  <read_first>src/middleware.ts, src/lib/admin.ts</read_first>
  <action>
Verify and update `src/middleware.ts` to ensure admin routes are protected:

1. Check that admin routes are protected:
   - `/admin` → redirect to login if not authenticated
   - `/admin/*` → redirect to login if not authenticated, 403 if not admin

2. Use session.role check for admin verification:
   ```typescript
   if (pathname.startsWith('/admin')) {
     const session = await getToken({ req, secret })
     if (!session) {
       return NextResponse.redirect(new URL('/login?redirect=' + pathname, req.url))
     }
     // Optional: Verify admin role in database for sensitive pages
   }
   ```

3. Ensure public routes remain accessible:
   - `/` (home)
   - `/rooms` (room listing)
   - `/login`, `/signup`
   - `/api/auth/*`

4. Test that middleware correctly handles:
   - Unauthenticated users accessing admin routes
   - Non-admin users accessing admin routes
   - Admin users accessing admin routes

Note: The requireAdmin function in pages provides secondary authorization; middleware is first line of defense.
  </action>
  <verify>
    <automated>grep -q "startsWith.*admin" src/middleware.ts && grep -q "redirect.*login" src/middleware.ts</automated>
  </verify>
  <done>Middleware protects admin routes, redirects unauthenticated users, allows public access</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with Phase 7 features</name>
  <files>README.md</files>
  <read_first>README.md, .planning/ROADMAP.md</read_first>
  <action>
Update `README.md` to document Phase 7 features:

1. Add "Admin Dashboard" section to Features:
   - Overview page with comprehensive metrics
   - Attendance analytics with time range filtering
   - User management with ban/suspend capabilities
   - Bandwidth monitoring with cost tracking
   - Audit log for admin actions

2. Update "Architecture" section:
   - Add admin panel description
   - Document bandwidth tracking approach
   - Note structured logging implementation

3. Update "Environment Variables" section:
   - Reference .env.local.example for Phase 7 variables
   - Explain bandwidth monitoring configuration

4. Update "Development" section:
   - Add admin development notes
   - Note admin user creation for testing

5. Update "Deployment" section:
   - Add production considerations for logging
   - Note TURN bandwidth monitoring setup

Keep existing content, only add/update Phase 7 related sections.
  </action>
  <verify>
    <automated>grep -q "Admin Dashboard\|Bandwidth Monitoring\|Audit Log" README.md</automated>
  </verify>
  <done>README updated with Phase 7 features, architecture, env vars, and deployment notes</done>
</task>

<task type="auto">
  <name>Task 4: Create Phase 7 verification checklist</name>
  <files>.planning/phases/07-analytics-operations/VERIFICATION.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>
Create `.planning/phases/07-analytics-operations/VERIFICATION.md` with:

1. Requirement verification checklist:
   - [ ] ADMN-02: Admin can view attendance dashboard
   - [ ] ADMN-03: Admin can view basic analytics
   - [ ] ADMN-05: Admin can manage users (ban/suspend)
   - [ ] TECH-06: System monitors TURN bandwidth usage

2. Feature verification checklist:
   - [ ] Admin sidebar navigation working
   - [ ] Overview page displays all 6 metric cards
   - [ ] Attendance page with time range filtering
   - [ ] User management page with search/filter
   - [ ] Ban/suspend modal requires reason and duration
   - [ ] AdminLog captures all admin actions
   - [ ] Bandwidth dashboard displays usage and cost
   - [ ] Bandwidth alerts configured via env vars
   - [ ] Structured logging operational

3. API endpoint verification:
   - [ ] GET /api/admin/bandwidth returns data
   - [ ] PATCH /api/admin/users/[userId] updates status
   - [ ] GET /api/admin/logs returns admin actions

4. Security verification:
   - [ ] Admin routes protected by middleware
   - [ ] API endpoints use assertAdmin
   - [ ] Non-admins cannot access admin pages

5. Integration verification:
   - [ ] Bandwidth tracking integrated with WebRTC
   - [ ] Admin actions logged to AdminLog
   - [ ] Log levels configurable via LOG_LEVEL

6. Documentation verification:
   - [ ] .env.local.example has all Phase 7 vars
   - [ ] README.md documents Phase 7 features

Use checklist format with space for signatures and dates.
  </action>
  <verify>
    <automated>grep -q "ADMN-02\|ADMN-03\|ADMN-05\|TECH-06" .planning/phases/07-analytics-operations/VERIFICATION.md && grep -q "Feature verification\|Security verification" .planning/phases/07-analytics-operations/VERIFICATION.md</automated>
  </verify>
  <done>Verification checklist created with all requirements and feature checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 7 documentation and verification checklist</what-built>
  <how-to-verify>
1. Read updated .env.local.example and confirm all Phase 7 variables are documented
2. Read updated README.md and confirm Phase 7 features are described
3. Check src/middleware.ts for admin route protection
4. Review VERIFICATION.md checklist completeness
5. Test admin navigation: Visit /admin, verify all sidebar links work
6. Test attendance page: Visit /admin/attendance, verify time range selector works
7. Test user management: Visit /admin/users, verify search and filter work
8. Test bandwidth page: Visit /admin/bandwidth, verify dashboard loads
9. Verify all admin actions are logged in AdminLog (check database)
10. Confirm bandwidth tracking is recording data (check BandwidthStats collection)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with Phase 7 documentation and verification</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation and verification task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | N/A |
</threat_model>

<verification>
- .env.local.example updated with all Phase 7 variables
- Middleware protects admin routes
- README.md documents Phase 7 features
- VERIFICATION.md checklist is complete
- All admin pages accessible from sidebar
- All requirements traceable to implemented features
</verification>

<success_criteria>
1. All Phase 7 environment variables documented
2. Middleware correctly protects admin routes
3. README.md describes Phase 7 features
4. Verification checklist covers all requirements
5. Human verification passes
6. Phase 7 ready for deployment
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-operations/07-07-SUMMARY.md`
</output>

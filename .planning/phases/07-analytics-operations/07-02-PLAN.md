---
phase: 07-analytics-operations
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/app/admin/attendance/page.tsx
  - src/components/admin/AttendanceMetrics.tsx
  - src/components/admin/TimeRangeSelector.tsx
  - src/lib/analytics.ts
autonomous: true
requirements:
  - ADMN-02
must_haves:
  truths:
    - Admin can view detailed attendance analytics
    - Admin can filter by time range (presets + custom)
    - Admin sees session counts, attendance rates, no-show rates
    - Admin can compare time periods (toggle for "Compare to previous period")
  artifacts:
    - path: src/app/admin/attendance/page.tsx
      provides: "Attendance analytics dashboard page"
      exports: ["default"]
    - path: src/components/admin/AttendanceMetrics.tsx
      provides: "Detailed attendance metrics display"
      exports: ["AttendanceMetrics"]
    - path: src/components/admin/TimeRangeSelector.tsx
      provides: "Reusable time range selector with presets"
      exports: ["TimeRangeSelector"]
  key_links:
    - from: src/app/admin/attendance/page.tsx
      to: src/lib/analytics.ts
      via: "getAttendanceStats function call"
      pattern: "getAttendanceStats"
    - from: src/app/admin/attendance/page.tsx
      to: src/app/admin/layout.tsx
      via: "admin layout sidebar navigation"
      pattern: "layout.*AdminSidebar"
---

<objective>
Create detailed attendance analytics dashboard with time range filtering and period comparison.

Purpose: Give admins visibility into attendance patterns, no-show rates, and session utilization over time.

Output: Attendance page with detailed metrics, time range selector, comparison view.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-operations/07-CONTEXT.md
@src/app/admin/page.tsx
@src/lib/analytics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TimeRangeSelector component</name>
  <files>src/components/admin/TimeRangeSelector.tsx</files>
  <read_first>src/components/ui/select.tsx, src/components/ui/button.tsx</read_first>
  <action>
Create `src/components/admin/TimeRangeSelector.tsx` client component with:

1. Props interface:
   ```typescript
   interface TimeRangeSelectorProps {
     value: TimeRangePreset
     onChange: (value: TimeRangePreset) => void
     onCustomRangeChange?: (start: Date, end: Date) => void
     showCompare?: boolean
     onCompareChange?: (enabled: boolean) => void
   }

   type TimeRangePreset = 'today' | 'yesterday' | 'past7days' | 'past30days' | 'custom'
   ```
2. Preset buttons: Today, Yesterday, Past 7 Days, Past 30 Days
3. Custom range button that opens date picker (use shadcn/ui Calendar or native input[type="date"])
4. Active preset styling: bg-accent text-accent-foreground
5. "Compare to previous period" toggle (checkbox) when showCompare=true
6. Layout: Horizontal button group for presets, custom range trigger, compare toggle below
7. Export selected date range: `{ startDate: Date, endDate: Date }`

Per D-04: Presets + custom date picker + period comparison toggle.
  </action>
  <verify>
    <automated>grep -q "TimeRangeSelectorProps" src/components/admin/TimeRangeSelector.tsx && grep -q "TimeRangePreset" src/components/admin/TimeRangeSelector.tsx && grep -q "today\|yesterday\|past7days\|past30days" src/components/admin/TimeRangeSelector.tsx</automated>
  </verify>
  <done>Selector renders all presets, active state highlighted, custom range opens date picker, compare toggle works</done>
</task>

<task type="auto">
  <name>Task 2: Create AttendanceMetrics component</name>
  <files>src/components/admin/AttendanceMetrics.tsx</files>
  <read_first>src/components/admin/DashboardCard.tsx</read_first>
  <action>
Create `src/components/admin/AttendanceMetrics.tsx` with:

1. Props interface:
   ```typescript
   interface AttendanceMetricsProps {
     stats: AttendanceStats
     previousStats?: AttendanceStats  // For comparison
     showComparison: boolean
   }

   interface AttendanceStats {
     totalSessions: number
     completedSessions: number
     cancelledSessions: number
     totalRegistrations: number
     attendedCount: number
     noShowCount: number
     attendanceRate: number
     noShowRate: number
     avgCapacity: number  // Average participants per session
     peakHours: { hour: number, count: number }[]
   }
   ```
2. Display metrics in grid layout (2 columns):
   - Top row: Total Sessions, Completed, Cancelled
   - Middle row: Total Registrations, Attended, No-Shows
   - Bottom row: Attendance Rate, No-Show Rate, Avg Capacity
3. Use DashboardCard component for each metric
4. When showComparison=true, show previous values with delta arrows
5. Peak hours section: Simple bar chart or list showing busiest hours
6. Capacity utilization: Progress bar showing avg vs max capacity (12)

Per D-12: Minimal charts — simple bar for peak hours, line trends avoided for MVP.
Per D-13: Trend direction with arrows and percentage change.
  </action>
  <verify>
    <automated>grep -q "AttendanceMetricsProps" src/components/admin/AttendanceMetrics.tsx && grep -q "DashboardCard" src/components/admin/AttendanceMetrics.tsx && grep -q "AttendanceStats" src/components/admin/AttendanceMetrics.tsx</automated>
  </verify>
  <done>Component displays all metrics, comparison view shows deltas, peak hours rendered</done>
</task>

<task type="auto">
  <name>Task 3: Create attendance analytics page</name>
  <files>src/app/admin/attendance/page.tsx</files>
  <read_first>src/app/admin/page.tsx, src/lib/analytics.ts</read_first>
  <action>
Create `src/app/admin/attendance/page.tsx` with:

1. Import `requireAdmin` for authorization
2. Server-side data fetching with URL params for time range
3. Parse search params: range (preset), startDate, endDate, compare (boolean)
4. Fetch attendance stats using `getAttendanceStats(dateRange, includeComparison)`
5. Render page structure:
   - Header: "Attendance Analytics" with description
   - TimeRangeSelector with current selection from URL params
   - AttendanceMetrics with stats and comparison data
6. Handle URL updates when time range changes (use client component for interactivity or use router.push)
7. Show empty state when no sessions in date range
8. Export for client-side filtering: Use page.tsx for initial load, add client component for interactivity

Client-side interactivity: Wrap TimeRangeSelector and comparison logic in a client component that uses router.push to update URL params.
  </action>
  <verify>
    <automated>grep -q "requireAdmin" src/app/admin/attendance/page.tsx && grep -q "getAttendanceStats" src/app/admin/attendance/page.tsx && grep -q "TimeRangeSelector" src/app/admin/attendance/page.tsx</automated>
  </verify>
  <done>Page loads with default (past 7 days) range, time range changes update data, comparison toggle works</done>
</task>

<task type="auto">
  <name>Task 4: Implement getAttendanceStats analytics function</name>
  <files>src/lib/analytics.ts</files>
  <read_first>src/lib/analytics.ts, src/models/Registration.ts, src/models/Room.ts</read_first>
  <action>
Add to `src/lib/analytics.ts`:

1. `getAttendanceStats(dateRange, includeComparison)` function returning:
   ```typescript
   interface AttendanceStatsResult {
     current: AttendanceStats
     previous?: AttendanceStats  // Only if includeComparison=true
   }
   ```
2. Query Room model for sessions in date range, filter by status
3. Query Registration model for attendance data:
   - Count registrations where status='attended'
   - Count registrations where status='no-show'
   - Calculate attendance rate: attended / (attended + no-show) * 100
4. Calculate average capacity: Sum(participants.length) / room count
5. Find peak hours: Aggregate sessions by scheduledTime hour, sort by count desc
6. For comparison: Calculate previous period of same length (e.g., if current=7 days, previous=7 days before current)
7. Return 0 values for empty ranges

Edge cases: Handle division by zero (no sessions), return 0 for rates.
  </action>
  <verify>
    <automated>grep -q "export.*getAttendanceStats" src/lib/analytics.ts && grep -q "AttendanceStats" src/lib/analytics.ts && grep -q "includeComparison" src/lib/analytics.ts</automated>
  </verify>
  <done>Function returns current stats, optionally previous stats for comparison, handles empty data</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→attendance API | Untrusted admin client requests attendance data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | S | Attendance analytics | mitigate | requireAdmin check on page, verify admin role |
| T-07-06 | I | Attendance aggregation | accept | Aggregated data (counts, rates) not individual PII |
| T-07-07 | D | Attendance metrics | accept | Read-only metrics, no sensitive individual data exposed |
</threat_model>

<verification>
- Attendance page accessible from sidebar
- Time range selector updates URL params and refreshes data
- Metrics display correctly with real data
- Comparison toggle shows previous period data
- Peak hours calculated correctly
- Empty states handled
</verification>

<success_criteria>
1. Admin can navigate to /admin/attendance and see detailed metrics
2. Time range presets update data correctly
3. Custom date range filtering works
4. Compare toggle shows previous period side-by-side
5. All attendance metrics calculated accurately
6. Peak hours displayed
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-operations/07-02-SUMMARY.md`
</output>

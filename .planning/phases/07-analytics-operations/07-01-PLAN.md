---
phase: 07-analytics-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/layout.tsx
  - src/app/admin/page.tsx
  - src/components/admin/AdminSidebar.tsx
  - src/components/admin/DashboardCard.tsx
  - src/lib/analytics.ts
autonomous: true
requirements:
  - ADMN-02
  - ADMN-03
must_haves:
  truths:
    - Admin can access admin dashboard with sidebar navigation
    - Admin sees overview page with comprehensive metrics grid
    - Admin can navigate between Overview, Attendance, Users, Bandwidth, Rooms pages
    - Admin sees time range selector with presets and custom date picker
  artifacts:
    - path: src/app/admin/layout.tsx
      provides: "Admin layout with sidebar navigation"
      exports: ["AdminSidebar"]
    - path: src/app/admin/page.tsx
      provides: "Overview dashboard page"
      exports: ["default"]
    - path: src/components/admin/AdminSidebar.tsx
      provides: "Reusable sidebar navigation component"
      exports: ["AdminSidebar"]
    - path: src/lib/analytics.ts
      provides: "Analytics data fetching utilities"
      exports: ["getOverviewStats"]
  key_links:
    - from: src/app/admin/layout.tsx
      to: src/lib/admin.ts
      via: "requireAdmin import"
      pattern: "requireAdmin"
    - from: src/app/admin/page.tsx
      to: src/lib/analytics.ts
      via: "getOverviewStats function call"
      pattern: "getOverviewStats"
---

<objective>
Create admin dashboard layout with sidebar navigation and overview page showing comprehensive metrics grid.

Purpose: Provide admin users with centralized access to all admin features and a high-level view of platform health.

Output: Functional admin dashboard with navigation, overview page with metrics, time range selector.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-operations/07-CONTEXT.md
@src/lib/admin.ts
@src/app/admin/rooms/page.tsx
</context>

<interfaces>
<!-- Key types and contracts from existing admin system -->

From src/lib/admin.ts:
```typescript
export interface AdminAuthResult {
  authorized: boolean
  user?: IUser
  error?: string
}
export async function requireAdmin(request: NextRequest): Promise<AdminAuthResult>
export function isAdmin(user: IUser | null): boolean
```

From src/models/types.ts:
```typescript
export interface IUser {
  _id: mongoose.Types.ObjectId
  email: string
  name: string
  role: 'user' | 'admin'
  // ... other fields
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create admin layout with sidebar navigation</name>
  <files>src/app/admin/layout.tsx</files>
  <read_first>src/app/admin/rooms/page.tsx, src/lib/admin.ts</read_first>
  <action>
Create admin layout at `src/app/admin/layout.tsx` with:

1. Import and use `requireAdmin` from `@/lib/admin` for authorization
2. Redirect to `/login?redirect=/admin` if unauthorized, `/403` if forbidden
3. Create client component `AdminSidebar` with navigation items:
   - Overview (href: `/admin`)
   - Attendance (href: `/admin/attendance`)
   - Users (href: `/admin/users`)
   - Bandwidth (href: `/admin/bandwidth`)
   - Rooms (href: `/admin/rooms`)
4. Sidebar styling: Fixed left (w-64), dark background (bg-slate-900), white text, hover states on links (hover:bg-slate-800)
5. Active link highlighting: Use `usePathname()` to add bg-slate-800 + text-white to current page
6. Layout structure: Sidebar (left, 256px) + Main content area (flex-1, ml-64)
7. Add sign-out link at bottom of sidebar

Per D-01: Sidebar navigation for scalability.
Per D-03: Consistent card layout pattern from existing admin pages.
  </action>
  <verify>
    <automated>grep -q "requireAdmin" src/app/admin/layout.tsx && grep -q "AdminSidebar" src/app/admin/layout.tsx && grep -q "usePathname" src/components/admin/AdminSidebar.tsx</automated>
  </verify>
  <done>Layout renders with sidebar, all navigation links present, active link highlighted</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminSidebar client component</name>
  <files>src/components/admin/AdminSidebar.tsx</files>
  <read_first>src/components/ui/button.tsx</read_first>
  <action>
Create `src/components/admin/AdminSidebar.tsx` client component with:

1. Props interface: `interface AdminSidebarProps { className?: string }`
2. Use `usePathname()` from next/navigation to determine active link
3. Navigation items array with id, label, href, icon:
   - overview: "Overview", "/admin", LayoutDashboard
   - attendance: "Attendance", "/admin/attendance", Users
   - users: "Users", "/admin/users", UserCog
   - bandwidth: "Bandwidth", "/admin/bandwidth", Activity
   - rooms: "Rooms", "/admin/rooms", DoorOpen
4. Render nav links with active state styling (bg-slate-800 when active)
5. Add sign-out button at bottom using auth signOut function
6. Import icons from lucide-react

Icon pattern: Use lucide-react icons matching existing admin components.
  </action>
  <verify>
    <automated>grep -q "use client" src/components/admin/AdminSidebar.tsx && grep -q "usePathname" src/components/admin/AdminSidebar.tsx && grep -q "LayoutDashboard\|Users\|UserCog\|Activity\|DoorOpen" src/components/admin/AdminSidebar.tsx</automated>
  </verify>
  <done>Sidebar renders all nav items, active link highlighted, sign-out functional</done>
</task>

<task type="auto">
  <name>Task 3: Create overview page with metrics grid</name>
  <files>src/app/admin/page.tsx</files>
  <read_first>src/app/admin/rooms/page.tsx</read_first>
  <action>
Create `src/app/admin/page.tsx` with:

1. Import `requireAdmin` for authorization (same pattern as admin/rooms/page.tsx)
2. Import `DashboardCard` component (created in next task)
3. Import `getOverviewStats` from `@/lib/analytics`
4. Fetch overview stats server-side: total sessions today, attendance rate, active users, bandwidth used, no-show rate, captain coverage
5. Render metrics grid with cards:
   - Total Sessions (today, with trend vs yesterday)
   - Attendance Rate (today vs 7-day avg)
   - Active Users (today, unique logins)
   - Bandwidth Used (today GB, cost estimate - can be 0 until bandwidth tracking implemented)
   - No-Show Rate (today)
   - Captain Coverage (% sessions with captain)
6. Grid layout: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4
7. Page header: "Dashboard Overview" with description

Per D-02: Comprehensive metric grid on overview page.
Per D-05: Default to Past 7 Days context (show trend indicators).
Per D-13: Show trend direction with up/down arrows and percentage change.
  </action>
  <verify>
    <automated>grep -q "requireAdmin" src/app/admin/page.tsx && grep -q "getOverviewStats" src/app/admin/page.tsx && grep -q "DashboardCard" src/app/admin/page.tsx</automated>
  </verify>
  <done>Overview page loads, shows 6 metric cards, displays data or "No data available" for empty state</done>
</task>

<task type="auto">
  <name>Task 4: Create DashboardCard component</name>
  <files>src/components/admin/DashboardCard.tsx</files>
  <read_first>src/components/ui/card.tsx</read_first>
  <action>
Create `src/components/admin/DashboardCard.tsx` with:

1. Props interface:
   ```typescript
   interface DashboardCardProps {
     title: string
     value: string | number
     trend?: number  // Percentage change, positive/negative
     trendLabel?: string  // e.g., "vs yesterday", "vs 7-day avg"
     unit?: string  // e.g., "%", "GB", "sessions"
     icon?: React.ReactNode
   }
   ```
2. Use shadcn/ui Card components (Card, CardHeader, CardContent)
3. Styling pattern from admin/rooms stat cards (bg-white p-6 rounded-lg border)
4. Trend indicator:
   - Positive: Green text (text-green-600) with TrendingUp icon
   - Negative: Red text (text-red-600) with TrendingDown icon
   - Neutral: Gray text (text-gray-500)
5. Large value display (text-3xl font-bold)
6. Small gray label (text-sm text-gray-600)
7. Optional icon in top-right corner

Per D-13: Trend direction with small up/down arrows and percentage change.
  </action>
  <verify>
    <automated>grep -q "interface DashboardCardProps" src/components/admin/DashboardCard.tsx && grep -q "TrendingUp\|TrendingDown" src/components/admin/DashboardCard.tsx && grep -q "from lucide-react" src/components/admin/DashboardCard.tsx</automated>
  </verify>
  <done>Card displays title, value, trend with correct colors, icon renders</done>
</task>

<task type="auto">
  <name>Task 5: Create analytics data fetching utilities</name>
  <files>src/lib/analytics.ts</files>
  <read_first>src/models/Registration.ts, src/models/Room.ts, src/models/User.ts</read_first>
  <action>
Create `src/lib/analytics.ts` with:

1. `getOverviewStats` function returning:
   ```typescript
   interface OverviewStats {
     totalSessions: number
     attendanceRate: number  // Percentage
     activeUsers: number
     bandwidthUsed: number  // GB
     bandwidthCost: number  // Estimated USD
     noShowRate: number  // Percentage
     captainCoverage: number  // Percentage
     trends: {
       sessionsTrend: number  // vs previous period
       attendanceTrend: number
       activeUsersTrend: number
     }
   }
   ```
2. Query Room model for sessions in date range
3. Query Registration model for attendance/no-show stats
4. Query User model for active users (unique logins in date range)
5. Calculate attendance rate: (attended / (attended + no-show)) * 100
6. Calculate no-show rate: (no-show / total registrations) * 100
7. Calculate captain coverage: (rooms with captainId / total completed rooms) * 100
8. Return 0/empty values when no data exists
9. Add `getDateRange` helper for preset handling (today, yesterday, past 7 days, past 30 days, custom)

Date range handling: Use startOfDay and endOfDay from date-fns for boundary accuracy.
  </action>
  <verify>
    <automated>grep -q "export.*getOverviewStats" src/lib/analytics.ts && grep -q "export.*getDateRange" src/lib/analytics.ts && grep -q "import.*Room\|Registration\|User" src/lib/analytics.ts</automated>
  </verify>
  <done>Function exports OverviewStats type, handles empty data, calculates all required metrics</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→admin API | Untrusted admin client requests data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | S | Admin dashboard | mitigate | requireAdmin check on every admin page, redirect unauthorized users |
| T-07-02 | I | Admin analytics API | mitigate | Verify admin role in API endpoints, no data aggregation that reveals PII |
| T-07-03 | D | Admin navigation | accept | Navigation reveals admin routes (public knowledge), authorization protects access |
| T-07-04 | E | Analytics data | accept | Read-only operations, no state modification via analytics functions |
</threat_model>

<verification>
- Admin layout renders sidebar with all navigation links
- Overview page displays all 6 metric cards
- requireAdmin authorization works (unauthorized users redirected)
- Active link highlighting works in sidebar
- Analytics functions return correct data structure
- Empty data handled gracefully (shows 0 or "No data")
</verification>

<success_criteria>
1. Admin can access /admin and see sidebar navigation + overview page
2. Unauthorized users redirected to login when accessing admin pages
3. Overview page displays 6 metric cards with current data
4. Sidebar highlights active page and allows navigation
5. Analytics functions fetch real data from Room/Registration/User models
6. DashboardCard component displays values with trend indicators
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-operations/07-01-SUMMARY.md`
</output>

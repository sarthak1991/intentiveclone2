---
phase: 05-focus-session-features
plan: 07
type: execute
wave: 3
depends_on:
  - 05-01
files_modified:
  - src/app/api/rooms/match/route.ts
  - src/components/rooms/InterestMatchedRooms.tsx
  - src/models/InterestTag.ts
autonomous: true
requirements:
  - COMM-04

must_haves:
  truths:
    - System supports interest-based room matching when sufficient users available
    - Users with matching interests are suggested same rooms together
    - Matching algorithm considers interest overlap
    - Rooms display matched interest tags
    - Fallback to standard room listing when no matches found
    - Minimum threshold for matching (3+ users with same interest)
  artifacts:
    - path: src/app/api/rooms/match/route.ts
      provides: Interest-based room matching algorithm
      min_lines: 80
    - path: src/components/rooms/InterestMatchedRooms.tsx
      provides: UI for displaying matched rooms based on interests
      min_lines: 70
    - path: src/models/InterestTag.ts
      provides: Extended with usage tracking for matching algorithm
      min_lines: 40
  key_links:
    - from: src/components/rooms/InterestMatchedRooms.tsx
      to: /api/rooms/match
      via: GET request on component mount
      pattern: "fetch.*api/rooms/match"
    - from: src/app/api/rooms/match/route.ts
      to: User model
      via: Query for user interests
      pattern: "User.*interests"
    - from: src/app/api/rooms/match/route.ts
      to: Registration model
      via: Query for registered users with same interests
      pattern: "Registration.*aggregate"
---

<objective>
Implement interest-based room matching to group users with shared interests in the same focus rooms.

Purpose: Increase engagement and connection by matching users with similar interests (occupation, goals, expertise) into the same rooms when sufficient users are available.

Output: Interest matching API, InterestMatchedRooms component, InterestTag model extension
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-focus-session-features/05-CONTEXT.md
@.planning/phases/05-focus-session-features/05-RESEARCH.md
@src/models/InterestTag.ts
@src/models/User.ts
@src/models/Room.ts
@src/models/Registration.ts
@src/app/api/rooms/route.ts
@src/components/rooms/page.tsx
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/models/InterestTag.ts:
```typescript
interface IInterestTag {
  _id: string
  tag: string
  category: 'occupation' | 'goal' | 'expertise'
}
```

From src/models/User.ts:
```typescript
interface IUser {
  _id: string
  name: string
  email: string
  interests: string[]  // Array of interest tag IDs or strings
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend InterestTag model and create matching algorithm</name>
  <files>src/models/InterestTag.ts, src/app/api/rooms/match/route.ts</files>
  <read_first>
    - src/models/InterestTag.ts (existing model)
    - src/models/User.ts (for interests field)
    - src/models/Registration.ts (for room registrations)
  </read_first>
  <behavior>
    - Test 1: InterestTag model has usageCount field
    - Test 2: Matching algorithm returns empty array when user has no interests
    - Test 3: Matching algorithm returns rooms with matching interest tags
    - Test 4: Matching algorithm prioritizes rooms with higher match count
    - Test 5: Matching algorithm requires minimum threshold (3 users with same interest)
    - Test 6: Fallback to standard rooms when no matches found
  </behavior>
  <action>
    Modify src/models/InterestTag.ts:
    - Add usageCount field (Number, default 0) to track how many users have this interest
    - Add lastUsedAt field (Date) to track recent usage
    - Add index on usageCount for efficient sorting
    - Update incrementUsage method to count users with this interest

    Create src/app/api/rooms/match/route.ts:
    - Import NextResponse, getServerSession, connectDB, Room, User, Registration, InterestTag
    - GET handler: authenticate with getServerSession()
    - Get userId from session.user.id
    - Fetch user's interests from User.findById(userId)
    - If no interests: return standard rooms (fallback)
    - Find scheduled rooms (status 'scheduled' or 'open') where scheduledTime > now
    - For each room, count registered users with matching interests:
      - Query Registration.find({ roomId, status: 'registered' })
      - Populate userId, get interests from User documents
      - Count overlap with current user's interests
    - Calculate match score per room: interestOverlap * (1 + participantCount / 10)
    - Filter rooms: Include if match score > 0 OR has >= 3 users with any matching interest
    - Sort by match score descending, then by scheduledTime ascending
    - Limit to 5 results
    - Return 200 with { rooms: array with room data + matchScore, matchedInterests array }
    - Fallback: If no matches, return standard room list with { matched: false }
  </action>
  <verify>
    <automated>npm test -- api/rooms/match.test.ts</automated>
  </verify>
  <done>
    - InterestTag model extended with usageCount
    - Matching algorithm returns rooms with matching interests
    - Rooms sorted by match score (overlap + participant count)
    - Minimum threshold applied (3+ users with same interest)
    - Fallback to standard rooms when no matches
  </done>
</task>

<task type="auto">
  <name>Task 2: Create InterestMatchedRooms component</name>
  <files>src/components/rooms/InterestMatchedRooms.tsx</files>
  <read_first>
    - src/components/rooms/page.tsx (for room card pattern)
    - src/components/ui/card.tsx (for Card component)
  </read_first>
  <action>
    Create src/components/rooms/InterestMatchedRooms.tsx:
    - Import useState, useEffect, Card, CardHeader, CardTitle, CardContent, Badge, Button
    - Props interface: onRoomSelect (roomId => void)
    - State: matchedRooms (array), isLoading, hasMatches (boolean)
    - useEffect: Fetch from /api/rooms/match on mount
    - CardHeader: "Rooms for You" title (when hasMatches=true) or "Upcoming Rooms" (fallback)
    - CardContent list of rooms:
      - For each room: Card with room title, time, capacity
      - Match indicator: Badge showing "X% match" or "Similar interests: [tag1, tag2]"
      - Show matchedInterests as small badges
      - Participant count: "X/12 registered"
      - "Join" button: calls onRoomSelect(roomId)
      - When no matches: Show "No perfect matches found" message, display standard rooms
    - Empty state: "No upcoming rooms. Check back later!"
    - Loading state: Show skeleton cards
    - Error handling: Display error message, retry button
    - Visual distinction: Matched rooms have subtle border highlight (ring-1 ring-accent)
  </action>
  <verify>
    <automated>npm test -- components/InterestMatchedRooms.test.tsx</automated>
  </verify>
  <done>
    - InterestMatchedRooms displays rooms with matching interests
    - Match badge shows percentage or shared interests
    - Matched rooms have visual highlight
    - Fallback to standard rooms when no matches
    - Empty state shows helpful message
    - Join button triggers room selection callback
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate InterestMatchedRooms into rooms page</name>
  <files>src/app/rooms/page.tsx</files>
  <read_first>
    - src/app/rooms/page.tsx (existing rooms listing page)
  </read_first>
  <action>
    Modify src/app/rooms/page.tsx:
    - Import InterestMatchedRooms component
    - Add section above standard room list: "Recommended for You" or "Rooms for You"
    - Pass onRoomSelect callback to handle room selection
    - Conditionally render: Show InterestMatchedRooms when user is authenticated and has interests
    - Show standard room list when not authenticated or no interests
    - Add "Update interests" link to onboarding if user has no interests (shows in UI)
    - Position: Above calendar/list view, prominent but below page header
    - Section heading: "Based on your interests" with small info text
    - Collapse option: Allow users to hide this section (localStorage preference)
    - Mobile: Full-width cards, desktop: 3-column grid
  </action>
  <verify>
    <automated>npm test -- pages/rooms.test.tsx</automated>
  </verify>
  <done>
    - InterestMatchedRooms shown on rooms page
    - Displayed above standard room list
    - "Update interests" link when user has no interests
    - Collapse option available
    - Responsive layout (mobile/desktop)
    - Room selection handled by onRoomSelect callback
  </done>
</task>

<task type="auto">
  <name>Task 4: Add room interest tag display in room cards</name>
  <files>src/components/rooms/RoomCard.tsx (or create if not exists)</files>
  <action>
    If src/components/rooms/RoomCard.tsx exists, modify it:
    - Add interestTags prop to RoomCard component
    - Display interest tags as small badges at bottom of card
    - Limit to 3 tags max, show "+X more" if more
    - Tags styled: Badge with variant="outline", small text

    If RoomCard doesn't exist, create it:
    - Import Card, CardContent, Badge from shadcn/ui
    - Props interface: room (object with title, scheduledTime, capacity, participantCount, interestTags)
    - Display room info, capacity, time
    - Display interest tags as badges
    - "Register" or "Join" button based on registration status
    - Match indicator: Show "X% match" badge if matchScore provided
  </action>
  <verify>
    <automated>npm test -- components/rooms/RoomCard.test.tsx</automated>
  </verify>
  <done>
    - Room cards display interest tags as badges
    - Max 3 tags shown with "+X more" indicator
    - Match percentage badge shown when available
    - Tags use outline variant for subtle display
  </done>
</task>

<task type="auto">
  <name>Task 5: Update room creation to include interest tags</name>
  <files>src/app/api/admin/rooms/route.ts, src/components/admin/CreateRoomForm.tsx</files>
  <read_first>
    - src/app/api/admin/rooms/route.ts (existing room creation)
    - src/components/admin/CreateRoomForm.tsx (existing form)
    - src/models/InterestTag.ts
  </read_first>
  <action>
    Modify src/app/api/admin/rooms/route.ts:
    - Update room creation to accept optional interestTags array
    - Validate interest tags against existing InterestTag documents
    - Store valid interest tags in Room.interestTags field
    - Return error if invalid tag IDs provided

    Modify src/components/admin/CreateRoomForm.tsx:
    - Add interest tag multi-select dropdown
    - Fetch available tags from /api/admin/tags
    - Allow admin to select multiple tags for room
    - Display selected tags as removable chips
    - Include selected tag IDs in room creation request
  </action>
  <verify>
    <automated>npm test -- api/admin/rooms-tags.test.ts</automated>
  </verify>
  <done>
    - Admin can assign interest tags to rooms
    - Room creation validates tag IDs
    - Tags stored in room document
    - Multi-select dropdown in admin form
    - Selected tags displayed as chips
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | User interests used for matching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-23 | Information Disclosure | GET /api/rooms/match | mitigate | Only return public room info, don't expose other users' interests directly |
| T-05-24 | Tampering | Interest matching | accept | No security impact if matching algorithm manipulated (users see same rooms) |
</threat_model>

<verification>
1. InterestTag model extended with usageCount
2. Matching algorithm returns rooms with matching interests
3. Match score calculated correctly
4. Minimum threshold enforced (3+ users)
5. Fallback to standard rooms when no matches
6. InterestMatchedRooms component displays matched rooms
7. Room cards show interest tags
8. Admin can assign tags to rooms
9. Integration with rooms page complete
</verification>

<success_criteria>
1. Users with interests see matched rooms suggested
2. Rooms with higher match scores shown first
3. Match badge displays percentage or shared interests
4. Fallback to standard rooms when no matches
5. Room cards display interest tags
6. Admin can assign tags to rooms
7. Minimum threshold enforced (3+ users per interest)
8. System functions when user has no interests
</success_criteria>

<output>
After completion, create `.planning/phases/05-focus-session-features/05-07-SUMMARY.md`
</output>

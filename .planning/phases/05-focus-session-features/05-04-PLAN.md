---
phase: 05-focus-session-features
plan: 04
type: execute
wave: 2
depends_on:
  - 05-03
files_modified:
  - src/app/api/captains/eligible/route.ts
  - src/app/api/captains/invite/route.ts
  - src/components/admin/CaptainEligibility.tsx
  - src/models/CaptainAssignment.ts
  - server/socket-server.ts
autonomous: false
requirements:
  - CAPT-01
  - CAPT-02
  - CAPT-03

must_haves:
  truths:
    - System identifies users eligible for captain role (4+ completed sessions)
    - Admin reviews eligible users and sends invitations
    - Eligible user receives invitation notification
    - Admin can assign captains to specific sessions
    - Captain status stored in database for role verification
    - Captains have permission controls in room
  artifacts:
    - path: src/models/CaptainAssignment.ts
      provides: Captain assignment tracking model
      min_lines: 60
    - path: src/app/api/captains/eligible/route.ts
      provides: Eligible users query (4+ completed sessions)
      min_lines: 80
    - path: src/app/api/captains/invite/route.ts
      provides: Captain invitation creation endpoint
      min_lines: 60
    - path: src/components/admin/CaptainEligibility.tsx
      provides: Admin UI for viewing and inviting eligible users
      min_lines: 80
  key_links:
    - from: src/app/api/captains/eligible/route.ts
      to: SessionCompletion model
      via: MongoDB aggregation pipeline
      pattern: "SessionCompletion.*aggregate"
    - from: src/components/admin/CaptainEligibility.tsx
      to: /api/captains/invite
      via: POST request on invite button click
      pattern: "fetch.*api/captains/invite"
    - from: server/socket-server.ts
      to: captain-assign event
      via: Socket.IO event emission
      pattern: "captain-assigned"
---

<objective>
Implement captain eligibility system, admin invitation workflow, and captain assignment capabilities.

Purpose: Identify qualified users (4+ completed sessions) for captain role, allow admin review and invitation, track captain assignments for permission verification.

Output: Eligibility query API, invitation API, CaptainAssignment model, admin eligibility UI
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-focus-session-features/05-CONTEXT.md
@.planning/phases/05-focus-session-features/05-RESEARCH.md
@.planning/phases/05-focus-session-features/05-03-PLAN.md
@src/models/SessionCompletion.ts
@src/models/Room.ts
@src/models/User.ts
@src/app/api/admin/rooms/route.ts
@src/components/admin/CreateRoomForm.tsx
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/models/Room.ts:
```typescript
interface IRoom {
  _id: string
  title: string
  scheduledTime: Date
  captainId?: string  // Will be added
}
```

New interface for captain tracking:
```typescript
interface ICaptainAssignment {
  _id: string
  userId: string
  roomId: string
  assignedBy: string  // Admin userId
  assignedAt: Date
  status: 'invited' | 'accepted' | 'declined' | 'removed'
  sessionsCaptained: number
  freeSessionsEarned: number
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CaptainAssignment model and extend Room model</name>
  <files>src/models/CaptainAssignment.ts, src/models/Room.ts</files>
  <read_first>
    - src/models/Room.ts (to add captainId field)
    - src/models/Registration.ts (for timestamp pattern)
  </read_first>
  <behavior>
    - Test 1: CaptainAssignment model has userId, roomId, assignedBy, assignedAt, status fields
    - Test 2: CaptainAssignment schema has default status: 'invited'
    - Test 3: CaptainAssignment schema has sessionsCaptained default: 0, freeSessionsEarned default: 0
    - Test 4: Room model extended with captainId field (optional, ref User)
    - Test 5: Room model captainId has index for efficient queries
  </behavior>
  <action>
    Create src/models/CaptainAssignment.ts:
    - Import mongoose, Schema, Model
    - Define ICaptainAssignment interface in types.ts if not exists
    - Create CaptainAssignmentSchema with fields:
      - userId (ref User, required, index)
      - roomId (ref Room, required, index)
      - assignedBy (ref User, required) - admin who assigned
      - assignedAt (Date, default Date.now)
      - status (String, enum: ['invited', 'accepted', 'declined', 'removed'], default 'invited')
      - sessionsCaptained (Number, default 0)
      - freeSessionsEarned (Number, default 0)
      - remarks (String, optional, for captain notes)
    - Add compound index on userId + roomId
    - Add compound unique index on userId + roomId (one assignment per user per room)
    - Export CaptainAssignment model with hot-reload protection

    Modify src/models/Room.ts:
    - Add captainId field: type: Schema.Types.ObjectId, ref: 'User', default: null, index: true
    - This field allows assigning a captain to each room
  </action>
  <verify>
    <automated>npm test -- models/captain-assignment.test.ts</automated>
  </verify>
  <done>
    - CaptainAssignment model created with all required fields
    - Status enum includes: invited, accepted, declined, removed
    - Compound unique index on userId + roomId
    - Room model extended with captainId field
  </done>
</task>

<task type="auto">
  <name>Task 2: Create captain eligibility and invitation APIs</name>
  <files>src/app/api/captains/eligible/route.ts, src/app/api/captains/invite/route.ts, src/app/api/captains/assign/route.ts</files>
  <read_first>
    - src/app/api/admin/rooms/route.ts (for admin authorization pattern)
    - src/lib/streak.ts (for aggregation pattern reference)
  </read_first>
  <action>
    Create src/app/api/captains/eligible/route.ts:
    - Import NextResponse, getServerSession, connectDB, User, SessionCompletion, CaptainAssignment
    - GET handler: authenticate and verify admin role (check user.role === 'admin' or isOnboarded with admin flag)
    - Aggregation pipeline:
      - Match SessionCompletion where completed=true, attendedAt exists
      - Group by userId, sum completedCount
      - Match completedCount >= 4 (eligibility threshold per CAPT-01)
      - Lookup User collection for name, email, photoUrl
      - Project userId, name, email, photoUrl, completedCount
    - Filter out existing captains: distinct userIds from CaptainAssignment where status in ['invited', 'accepted']
    - Return 200 with { eligible: array, total: count }

    Create src/app/api/captains/invite/route.ts:
    - Import dependencies, CaptainAssignment, User
    - POST handler: authenticate and verify admin role
    - Validate body with CaptainInviteSchema: userId (required), roomId (optional for general invitation)
    - Check if user exists, return 404 if not
    - Check if user already has pending invitation, return 400 if exists
    - Create CaptainAssignment with status: 'invited', assignedBy: adminId
    - Emit Socket.IO event 'captain-invited' to notify user (if online)
    - Return 201 with { success: true, invitationId }

    Create src/app/api/captains/assign/route.ts:
    - Import dependencies, CaptainAssignment, Room
    - POST handler: authenticate and verify admin role
    - Validate body: userId, roomId (both required)
    - Create or update CaptainAssignment with status: 'accepted' (admin override per CAPT-03)
    - Update Room.captainId = userId
    - Emit 'captain-assigned' Socket.IO event
    - Return 200 with { success: true, roomId, captainId }
  </action>
  <verify>
    <automated>npm test -- api/captains/eligible.test.ts api/captains/invite.test.ts api/captains/assign.test.ts</automated>
  </verify>
  <done>
    - GET /api/captains/eligible returns users with 4+ completed sessions
    - POST /api/captains/invite creates invitation record
    - POST /api/captains/assign assigns captain to room (admin override)
    - All endpoints require admin authorization
    - Socket.IO events emitted for notifications
  </done>
</task>

<task type="auto">
  <name>Task 3: Add captain events to Socket.IO server</name>
  <files>server/socket-server.ts</files>
  <read_first>
    - server/socket-server.ts (for existing event patterns)
  </read_first>
  <action>
    Modify server/socket-server.ts:
    - Add 'captain-invited' event handler:
      - When admin creates invitation, emit to invited user's socket if connected
      - Event payload: { invitationId, roomTitle, message: "You've been invited to become a room captain!" }
    - Add 'captain-assigned' event handler:
      - When admin assigns captain to room, emit to captain's socket
      - Event payload: { roomId, roomTitle, scheduledTime, message: "You're assigned as captain for this room" }
    - Add 'captain-promoted' event for room namespace:
      - Emit to all participants in room when captain joins
      - Event payload: { captainId, captainName }
    - Import CaptainAssignment model for queries
    - Add helper function: notifyCaptainAssignment(userId, roomId) to send notifications
  </action>
  <verify>
    <automated>npm test -- socket/captain-events.test.ts</automated>
  </verify>
  <done>
    - Socket.IO server emits captain-invited event
    - Socket.IO server emits captain-assigned event
    - Room participants notified when captain joins
    - Captain assignment helper function created
  </done>
</task>

<task type="auto">
  <name>Task 4: Create CaptainEligibility admin component</name>
  <files>src/components/admin/CaptainEligibility.tsx</files>
  <read_first>
    - src/components/admin/CreateRoomForm.tsx (for form pattern)
    - src/components/ui/button.tsx (for Button component)
    - src/components/ui/card.tsx (for Card component)
  </read_first>
  <action>
    Create src/components/admin/CaptainEligibility.tsx:
    - Import useState, useEffect, Card, CardHeader, CardTitle, CardContent, Button, Badge
    - State: eligibleUsers (array), isLoading (boolean), invitedUsers (Set)
    - useEffect: Fetch from /api/captains/eligible on mount
    - CardHeader: "Captain Eligibility" title with description "Users with 4+ completed sessions"
    - Render list of eligible users:
      - Each row: user photo (Avatar), name, email, completedSessions count (Badge)
      - "Invite" button: disabled if already invited
      - "View Profile" link: navigates to user profile (placeholder for now)
    - handleInvite function: POST to /api/captains/invite with userId
    - On success: Add to invitedUsers Set, show success toast
    - Empty state: "No eligible users found. Users need 4+ completed sessions to qualify."
    - Loading state: Show skeleton or spinner
    - Error handling: Display error message from API
  </action>
  <verify>
    <automated>npm test -- components/admin/CaptainEligibility.test.tsx</automated>
  </verify>
  <done>
    - CaptainEligibility displays list of eligible users
    - Each user shows name, photo, completed session count
    - Invite button sends invitation via API
    - Invited users tracked to prevent duplicate invites
    - Success/error messages displayed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Captain eligibility system complete with:
    - CaptainAssignment model for tracking captain status
    - Eligibility API identifying users with 4+ completed sessions
    - Invitation API for admin to send captain invitations
    - Assignment API for admin to assign captains to rooms (emergency override)
    - Socket.IO events for captain notifications
    - CaptainEligibility admin UI component
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Login as admin user (check your database for admin role or create one)
    3. Navigate to /admin (or wherever admin panel is accessible)
    4. Find "Captain Eligibility" section
    5. Verify list shows users with 4+ completed sessions
    6. Click "Invite" button for a user
    7. Verify success message appears
    8. Check database: CaptainAssignment record created with status='invited'
    9. Verify invited user can no longer be invited again (button disabled)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → API | Admin can assign anyone as captain (emergency override) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Spoofing | POST /api/captains/invite | mitigate | Verify requester has admin role before creating invitations |
| T-05-13 | Spoofing | POST /api/captains/assign | mitigate | Verify requester has admin role, emergency assignment intentional per CAPT-03 |
| T-05-14 | Information Disclosure | GET /api/captains/eligible | mitigate | Require admin role, return only public user info (no private data) |
| T-05-15 | Tampering | Captain status | mitigate | Store in database, verify server-side before granting captain permissions |
</threat_model>

<verification>
1. CaptainAssignment model created with status tracking
2. Room model extended with captainId field
3. Eligibility API returns users with 4+ completed sessions
4. Invitation API creates captain invitation records
5. Assignment API assigns captain to room with admin override
6. Socket.IO events emit captain notifications
7. CaptainEligibility component displays eligible users
8. Admin authorization verified on all endpoints
9. Checkpoint passed: admin can invite eligible users
</verification>

<success_criteria>
1. System identifies users with 4+ completed sessions
2. Admin can view eligible users list
3. Admin can send captain invitations
4. Admin can assign captains to rooms (emergency)
5. Captain status stored in database
6. Socket.IO events notify users of invitations
7. All captain APIs require admin authorization
</success_criteria>

<output>
After completion, create `.planning/phases/05-focus-session-features/05-04-SUMMARY.md`
</output>

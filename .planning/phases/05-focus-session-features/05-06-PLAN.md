---
phase: 05-focus-session-features
plan: 06
type: execute
wave: 3
depends_on:
  - 05-04
files_modified:
  - src/components/admin/CaptainAssignment.tsx
  - src/app/api/admin/captains/assign/route.ts
  - src/app/api/admin/captains/remarks/route.ts
  - src/app/api/rooms/[id]/captains/route.ts
autonomous: false
requirements:
  - ADMN-04
  - ADMN-07

must_haves:
  truths:
    - Admin can assign captains to specific sessions
    - Admin can assign ANY user as captain (emergency assignment per D-14)
    - Admin can view captain remarks about sessions
    - Captain remarks stored per session
    - Admin sees captain assignment history
  artifacts:
    - path: src/components/admin/CaptainAssignment.tsx
      provides: Admin UI for assigning captains to rooms
      min_lines: 120
    - path: src/app/api/admin/captains/assign/route.ts
      provides: Admin captain assignment endpoint with emergency override
      min_lines: 70
    - path: src/app/api/admin/captains/remarks/route.ts
      provides: Captain remarks viewing endpoint
      min_lines: 50
  key_links:
    - from: src/components/admin/CaptainAssignment.tsx
      to: /api/admin/captains/assign
      via: POST request to assign captain
      pattern: "fetch.*api/admin/captains/assign"
    - from: src/components/admin/CaptainAssignment.tsx
      to: /api/rooms
      via: GET request to fetch rooms for dropdown
      pattern: "fetch.*api/rooms"
---

<objective>
Implement admin captain management UI for assigning captains to sessions and viewing captain remarks.

Purpose: Admins can ensure every session has captain oversight by assigning captains, including emergency assignments for eligible users. Admins can also review captain feedback about sessions.

Output: CaptainAssignment admin component, assignment API with emergency override, remarks viewing API
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-focus-session-features/05-CONTEXT.md
@.planning/phases/05-focus-session-features/05-RESEARCH.md
@.planning/phases/05-focus-session-features/05-04-PLAN.md
@src/models/CaptainAssignment.ts
@src/models/Room.ts
@src/models/User.ts
@src/components/admin/CreateRoomForm.tsx
@src/components/admin/RoomManagePanel.tsx
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/models/CaptainAssignment.ts (created in 05-04):
```typescript
interface ICaptainAssignment {
  userId: string
  roomId: string
  assignedBy: string
  assignedAt: Date
  status: 'invited' | 'accepted' | 'declined' | 'removed'
  sessionsCaptained: number
  remarks?: string
}
```

From src/models/Room.ts:
```typescript
interface IRoom {
  _id: string
  title: string
  scheduledTime: Date
  captainId?: string
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create admin captain assignment API</name>
  <files>src/app/api/admin/captains/assign/route.ts</files>
  <read_first>
    - src/app/api/captains/assign/route.ts (created in 05-04)
    - src/app/api/admin/rooms/route.ts (for admin auth pattern)
  </read_first>
  <action>
    Create src/app/api/admin/captains/assign/route.ts:
    - Import NextRequest, NextResponse, getServerSession, z, connectDB, Room, User, CaptainAssignment
    - Define AssignmentSchema: roomId (required), userId (required), isEmergency (boolean, optional, default false)
    - POST handler: authenticate and verify admin role (user.role === 'admin')
    - Parse body with AssignmentSchema
    - Verify room exists (find by roomId), return 404 if not
    - Verify user exists (find by userId), return 404 if not
    - If isEmergency=true: skip eligibility check (per D-14, admin can assign anyone)
    - If isEmergency=false: verify user has 4+ completed sessions (optional warning, not blocking)
    - Find or create CaptainAssignment with userId + roomId
    - Update assignment: status='accepted', assignedBy=adminId, assignedAt=Date.now()
    - Update Room.captainId = userId
    - Emit 'captain-assigned' Socket.IO event to notify user
    - Return 201 with { success: true, assignmentId, isEmergency }

    GET handler (same file): Return list of all captain assignments
    - Authenticate admin
    - Find CaptainAssignment.find().populate('userId').populate('roomId').populate('assignedBy')
    - Return 200 with { assignments: array with user name, room title, assignedBy name, status, remarks }
  </action>
  <verify>
    <automated>npm test -- api/admin/captains-assign.test.ts</automated>
  </verify>
  <done>
    - POST /api/admin/captains/assign assigns captain to room
    - Emergency assignment bypasses eligibility check
    - Room.captainId updated with assigned user
    - Socket.IO event notifies assigned captain
    - GET returns all captain assignments for admin
  </done>
</task>

<task type="auto">
  <name>Task 2: Create captain remarks API</name>
  <files>src/app/api/admin/captains/remarks/route.ts, src/app/api/captains/[roomId]/remarks/route.ts</files>
  <read_first>
    - src/models/CaptainAssignment.ts (for remarks field)
  </read_first>
  <action>
    Create src/app/api/admin/captains/remarks/route.ts:
    - Import NextResponse, getServerSession, connectDB, CaptainAssignment
    - GET handler: authenticate and verify admin role
    - Query CaptainAssignment.find({ remarks: { $exists: true, $ne: null } })
    - Populate userId, roomId fields
    - Sort by assignedAt descending
    - Return 200 with { remarks: array with captain name, room title, remark text, date }
    - Empty handling: Return empty array if no remarks found

    Create src/app/api/captains/[roomId]/remarks/route.ts:
    - Import dependencies, CaptainAssignment
    - POST handler: authenticate, verify user is captain for this room
    - Validate body with RemarksSchema: remarks (string, max 500 chars)
    - Find CaptainAssignment by userId + roomId
    - Update remarks field with submitted text
    - Return 200 with { success: true, remarks }
    - GET handler (same file): Return remarks for this room's captain assignment
  </action>
  <verify>
    <automated>npm test -- api/captains/remarks.test.ts</automated>
  </verify>
  <done>
    - GET /api/admin/captains/remarks returns all captain remarks
    - Captains can submit remarks via POST /api/captains/[roomId]/remarks
    - Remarks limited to 500 characters
    - Admin can view all remarks with captain and room info
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CaptainAssignment admin component</name>
  <files>src/components/admin/CaptainAssignment.tsx</files>
  <read_first>
    - src/components/admin/CreateRoomForm.tsx (for form pattern)
    - src/components/admin/RoomManagePanel.tsx (for table pattern)
    - src/components/ui/select.tsx (for Select component)
  </read_first>
  <action>
    Create src/components/admin/CaptainAssignment.tsx:
    - Import useState, useEffect, Card, CardHeader, CardTitle, CardContent, Button, Select, SelectTrigger, SelectValue, SelectContent, SelectItem, Badge, Textarea
    - State: rooms (array), users (array), selectedRoom (string), selectedUser (string), isEmergency (boolean), assignments (array), isLoading
    - useEffect: Fetch rooms from /api/rooms (scheduled rooms only)
    - useEffect: Fetch users from /api/users (for user search dropdown)
    - useEffect: Fetch existing assignments from /api/admin/captains/assign
    - CardHeader: "Captain Assignment" title
    - CardContent sections:
      1. Assignment Form:
         - Room dropdown (Select with scheduled rooms)
         - User search/dropdown (Select with users)
         - Emergency checkbox: "Emergency assignment (skip eligibility)"
         - "Assign Captain" button
      2. Current Assignments table:
         - Columns: Room, Captain, Assigned By, Status, Actions
         - Status badge: invited (yellow), accepted (green), declined (red)
         - Remove button for each assignment
    - handleAssign function: POST to /api/admin/captains/assign with roomId, userId, isEmergency
    - handleRemove function: DELETE to /api/admin/captains/assign/[id]
    - Show loading states during API calls
    - Error handling with toast notifications
    - Emergency checkbox shows warning when checked: "This will bypass the 4+ session requirement"
  </action>
  <verify>
    <automated>npm test -- components/admin/CaptainAssignment.test.tsx</automated>
  </verify>
  <done>
    - CaptainAssignment component displays assignment form
    - Room dropdown shows scheduled rooms
    - User dropdown shows all users (for emergency assignment)
    - Emergency checkbox bypasses eligibility check
    - Current assignments table shows all assignments
    - Remove button deletes assignment
  </done>
</task>

<task type="auto">
  <name>Task 4: Create CaptainRemarks viewing component</name>
  <files>src/components/admin/CaptainRemarks.tsx</files>
  <read_first>
    - src/components/admin/CaptainAssignment.tsx (for layout pattern)
  </read_first>
  <action>
    Create src/components/admin/CaptainRemarks.tsx:
    - Import useState, useEffect, Card, CardHeader, CardTitle, CardContent, Badge
    - State: remarks (array), isLoading
    - useEffect: Fetch from /api/admin/captains/remarks on mount
    - CardHeader: "Captain Remarks" title with description "Feedback from room captains"
    - CardContent list:
      - For each remark: Card with captain name (Avatar + name), room title, remark text, date
      - Badge for room status (completed, in-progress, scheduled)
      - Empty state: "No captain remarks yet."
    - Format date with date-fns formatRelative
    - Truncate long remarks (max 200 chars) with "Read more" expansion
    - Show most recent remarks first (sorted by date)
    - Filter by room or captain (optional feature for future)
  </action>
  <verify>
    <automated>npm test -- components/admin/CaptainRemarks.test.tsx</automated>
  </verify>
  <done>
    - CaptainRemarks component displays list of remarks
    - Each remark shows captain, room, text, date
    - Remarks sorted by date (most recent first)
    - Long remarks truncated with expand option
    - Empty state shows helpful message
  </done>
</task>

<task type="auto">
  <name>Task 5: Add captain remark input to CaptainPanel</name>
  <files>src/components/room/CaptainPanel.tsx</files>
  <read_first>
    - src/components/room/CaptainPanel.tsx (created in 05-05)
  </read_first>
  <action>
    Modify src/components/room/CaptainPanel.tsx:
    - Add remarks state: remarkText (string, default empty), isSubmitting (boolean)
    - Add Textarea component from shadcn/ui for remark input
    - Add "Submit Remark" button below reward progress
    - Placeholder: "Any notes about this session? (optional, 500 chars max)"
    - Character counter: "X/500"
    - handleSubmitRemark function:
      - POST to /api/captains/[roomId]/remarks with { remarks: remarkText }
      - On success: clear remarkText, show success toast
      - Handle errors gracefully
    - Display existing remarks if any (fetch from GET /api/captains/[roomId]/remarks)
    - Show "Session Notes" section heading
    - Position: Below reward progress, at bottom of panel
    - Only visible to captain (isCaptain check)
  </action>
  <verify>
    <automated>npm test -- components/room/CaptainPanel-remarks.test.tsx</automated>
  </verify>
  <done>
    - CaptainPanel has remark input textarea
    - Character counter shows X/500
    - Submit button sends remark to API
    - Success toast on submission
    - Existing remarks displayed in panel
    - Remark input only visible to captain
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Admin captain management complete with:
    - Captain assignment API with emergency override
    - Captain remarks viewing API
    - CaptainAssignment admin component for assigning captains
    - CaptainRemarks admin component for viewing feedback
    - Captain remark input in CaptainPanel
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Login as admin user
    3. Navigate to /admin page
    4. Find "Captain Assignment" section
    5. Select a room from dropdown
    6. Select a user from dropdown (try one with <4 sessions)
    7. Check "Emergency assignment" checkbox
    8. Click "Assign Captain" - verify success message
    9. Check "Captain Remarks" section
    10. Verify remarks display correctly (submit a remark as captain first)
    11. Test removing assignment from table
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → API | Admin can assign any user as captain (emergency) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Spoofing | POST /api/admin/captains/assign | mitigate | Verify admin role before assignment, log all emergency assignments for audit |
| T-05-21 | Tampering | Captain remarks | mitigate | Sanitize remark input, limit to 500 chars, store in database (not localStorage) |
| T-05-22 | Information Disclosure | GET /api/admin/captains/remarks | mitigate | Require admin role, only return submitted remarks (no private data) |
</threat_model>

<verification>
1. Admin can assign captains to rooms
2. Emergency assignment bypasses eligibility check
3. Captain remarks viewable by admin
4. Captains can submit remarks via CaptainPanel
5. Remarks limited to 500 characters
6. Assignment history displayed in table
7. Remove button deletes assignments
8. All APIs require admin or captain authorization
9. Checkpoint passed: admin can manage captains
</verification>

<success_criteria>
1. Admin can assign captains to specific sessions
2. Admin can assign any user as captain (emergency)
3. Admin can view captain remarks
4. Captains can submit remarks about sessions
5. Assignment history visible to admin
6. Remarks stored per session
</success_criteria>

<output>
After completion, create `.planning/phases/05-focus-session-features/05-06-SUMMARY.md`
</output>

---
phase: 05-focus-session-features
plan: 05
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-04
files_modified:
  - src/app/api/tasks/[roomId]/aggregate/route.ts
  - src/components/room/CaptainPanel.tsx
  - src/hooks/useCaptainControls.ts
  - server/socket-server.ts
  - src/models/CaptainAssignment.ts
autonomous: false
requirements:
  - CAPT-04
  - CAPT-05
  - CAPT-06
  - CAPT-07

must_haves:
  truths:
    - Captain can view aggregate task count (8/10 submitted tasks, not individual tasks)
    - Captain can mute all participants (soft mute, participants can unmute)
    - Captain can mute individual participants (stays muted until captain unmutes)
    - Captain earns 1 free session for every 4 sessions captained
    - Progress bar shows captain rewards progress (3/4 until free session)
    - Captain limited to max 2 sessions per day
    - Captain controls only visible to room captain
  artifacts:
    - path: src/app/api/tasks/[roomId]/aggregate/route.ts
      provides: Aggregate task count endpoint (privacy-first)
      min_lines: 50
    - path: src/components/room/CaptainPanel.tsx
      provides: Captain control panel with mute buttons and task count
      min_lines: 100
    - path: src/hooks/useCaptainControls.ts
      provides: Captain permission hook and mute control functions
      min_lines: 80
    - path: src/models/CaptainAssignment.ts
      provides: Extended with dailyLimitCheck and reward tracking
      min_lines: 70
  key_links:
    - from: src/components/room/CaptainPanel.tsx
      to: /api/tasks/[roomId]/aggregate
      via: GET request on mount
      pattern: "fetch.*api/tasks.*aggregate"
    - from: src/components/room/CaptainPanel.tsx
      to: server/socket-server.ts
      via: Socket.IO emit for mute events
      pattern: "captain-mute"
    - from: src/hooks/useCaptainControls.ts
      to: src/store/roomStore.ts
      via: Zustand store for participant mute state
      pattern: "setParticipantMuted"
---

<objective>
Implement captain controls: aggregate task visibility, mute controls, and reward tracking with daily limit enforcement.

Purpose: Captains can manage room氛围 (atmosphere) by seeing task participation, managing disruptive audio, and earning free sessions as reward.

Output: CaptainPanel component, aggregate task API, mute controls, reward progress tracking
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-focus-session-features/05-CONTEXT.md
@.planning/phases/05-focus-session-features/05-RESEARCH.md
@.planning/phases/05-focus-session-features/05-01-PLAN.md
@.planning/phases/05-focus-session-features/05-04-PLAN.md
@src/models/Task.ts
@src/models/Room.ts
@src/store/roomStore.ts
@src/components/room/ControlBar.tsx
@server/socket-server.ts
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/store/roomStore.ts (will be extended):
```typescript
interface RoomState {
  // Existing fields...
  // NEW: Captain state
  isCaptain: boolean
  captainId: string | null
  mutedParticipants: Set<string>  // userIds muted by captain
}
```

From src/models/CaptainAssignment.ts (extended in 05-04):
```typescript
interface ICaptainAssignment {
  userId: string
  sessionsCaptained: number
  freeSessionsEarned: number
  lastCaptainSessionDate: Date
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create aggregate task count API</name>
  <files>src/app/api/tasks/[roomId]/aggregate/route.ts</files>
  <read_first>
    - src/app/api/tasks/[roomId]/route.ts (created in 05-01)
    - src/models/Task.ts (created in 05-01)
  </read_first>
  <behavior>
    - Test 1: API returns aggregate count (submitted, total) not individual tasks
    - Test 2: API requires captain verification (user is captain for this room)
    - Test 3: Non-captain receives 403 Forbidden
    - Test 4: Count includes only tasks from current room
    - Test 5: Total count equals participant count
  </behavior>
  <action>
    Create src/app/api/tasks/[roomId]/aggregate/route.ts:
    - Import NextRequest, NextResponse, getServerSession, connectDB, Task, Room, CaptainAssignment
    - GET handler: authenticate with getServerSession()
    - Get roomId from params, userId from session
    - Verify user is captain: Check Room.captainId === userId OR CaptainAssignment where userId=captainId and status='accepted'
    - If not captain: return 403 { error: "Captain privileges required" }
    - Count total participants: get from presence tracking or Room.participants.length
    - Count submitted tasks: Task.countDocuments({ roomId, submittedAt: { $exists: true } })
    - Return 200 with { submitted: count, total: participantCount, percentage: Math.round(submitted/total*100) }
    - NEVER return individual task content per D-15 (privacy-first)
  </action>
  <verify>
    <automated>npm test -- api/tasks/aggregate.test.ts</automated>
  </verify>
  <done>
    - GET /api/tasks/[roomId]/aggregate returns aggregate count only
    - Individual task content never exposed (privacy-first)
    - Captain verification required
    - 403 returned for non-captain users
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend roomStore with captain state and create useCaptainControls hook</name>
  <files>src/store/roomStore.ts, src/hooks/useCaptainControls.ts</files>
  <read_first>
    - src/store/roomStore.ts (to extend with captain state)
    - src/hooks/useRoomChat.ts (for hook pattern reference)
  </read_first>
  <action>
    Extend src/store/roomStore.ts:
    - Add to RoomState: isCaptain (boolean, default false), captainId (string | null, default null)
    - Add to RoomState: mutedParticipants (Set<string>, default new Set()) - tracks users muted by captain
    - Add actions: setCaptainStatus(isCaptain, captainId), setParticipantMuted(userId, isMuted), muteAll(), unmuteAll()
    - Initialize: isCaptain: false, captainId: null, mutedParticipants: new Set()

    Create src/hooks/useCaptainControls.ts:
    - Import useState, useEffect, useCallback, useRoomStore, socket
    - Define hook accepting roomId, userId
    - Get isCaptain, captainId, mutedParticipants from store
    - State: taskCount (object with submitted, total), isLoading
    - useEffect: Fetch captain status on mount (GET /api/rooms/[roomId]/captain-status)
    - useEffect: If isCaptain, fetch aggregate task count from /api/tasks/[roomId]/aggregate
    - useEffect: Listen for 'task-submitted' Socket.IO event, increment submitted count
    - handleMuteAll: Emit 'captain-mute-all' via socket, add all participants to mutedParticipants Set
    - handleUnmuteAll: Emit 'captain-unmute-all', clear mutedParticipants Set
    - handleMuteParticipant: Emit 'captain-mute-participant' with userId, add to Set
    - handleUnmuteParticipant: Emit 'captain-unmute-participant' with userId, remove from Set
    - Return { isCaptain, taskCount, mutedParticipants, handleMuteAll, handleUnmuteAll, handleMuteParticipant, handleUnmuteParticipant, isLoading }
  </action>
  <verify>
    <automated>npm test -- hooks/useCaptainControls.test.ts</automated>
  </verify>
  <done>
    - roomStore tracks isCaptain, captainId, mutedParticipants
    - useCaptainControls hook manages captain state
    - Mute functions emit Socket.IO events
    - Task count fetched when user is captain
    - Real-time task count updates on submissions
  </done>
</task>

<task type="auto">
  <name>Task 3: Add captain mute events to Socket.IO server</name>
  <files>server/socket-server.ts</files>
  <read_first>
    - server/socket-server.ts (for existing event patterns)
    - server/presence.ts (for participant tracking)
  </read_first>
  <action>
    Modify server/socket-server.ts:
    - Add 'captain-mute-all' event handler:
      - Verify socket.data.user.id === room.captainId
      - Emit 'muted-by-captain' to all participants in room except captain
      - Set muted flag for all participants in room state
    - Add 'captain-unmute-all' event handler:
      - Verify captain身份
      - Emit 'unmuted-by-captain' to all participants
      - Clear muted flags
    - Add 'captain-mute-participant' event handler:
      - Verify captain, get target userId from event data
      - Emit 'participant-muted' to target user with { mutedBy: captainName }
      - Set muted flag for target participant
    - Add 'captain-unmute-participant' event handler:
      - Verify captain
      - Emit 'participant-unmuted' to target user
      - Clear muted flag for target
    - Import helper functions from presence.ts for participant management
    - Add room.captainId to room state tracking
  </action>
  <verify>
    <automated>npm test -- socket/captain-mute-events.test.ts</automated>
  </verify>
  <done>
    - Socket.IO server handles captain-mute-all event
    - Socket.IO server handles captain-mute-participant event
    - Captain身份 verified before processing events
    - Participants notified when muted/unmuted
    - Mute state tracked in room state
  </done>
</task>

<task type="auto">
  <name>Task 4: Create CaptainPanel component with controls and reward progress</name>
  <files>src/components/room/CaptainPanel.tsx</files>
  <read_first>
    - src/components/room/ChatBox.tsx (for panel layout reference)
    - src/components/ui/card.tsx (for Card component)
    - src/components/ui/button.tsx (for Button component)
    - src/components/ui/badge.tsx (for Badge component)
  </read_first>
  <action>
    Create src/components/room/CaptainPanel.tsx:
    - Import Card, CardHeader, CardTitle, CardContent, Button, Badge, Progress (from shadcn/ui)
    - Props interface: roomId, captainId
    - Use useCaptainControls hook for state and actions
    - CardHeader: "Captain Controls" title
    - CardContent sections:
      1. Task Count: "X/Y participants submitted tasks" (aggregate count per D-15)
      2. Mute Controls:
         - "Mute All" button (variant: outline)
         - "Unmute All" button (variant: ghost)
         - List of individual participants with "Mute" buttons
      3. Reward Progress:
         - "Sessions captained: N/4" text
         - Progress bar: value={sessionsCaptained % 4 * 25} or (sessionsCaptained / 4 * 100)
         - "Free sessions earned: N" text
         - Max 2/day indicator: "2/2 sessions today" or "1/2 sessions today"
    - Show only to captain (isCaptain === true)
    - Real-time updates: Listen for task-submitted event to update count
    - Mute button disabled when participant count is 0
    - Progress bar shows percentage toward next free session
  </action>
  <verify>
    <automated>npm test -- components/room/CaptainPanel.test.tsx</automated>
  </verify>
  <done>
    - CaptainPanel displays aggregate task count (8/10 format)
    - Mute All button triggers soft mute for all participants
    - Individual participant list with mute/unmute buttons
    - Reward progress bar shows sessions until free session
    - Free sessions count displayed
    - Daily limit indicator shows 2/2 or 1/2
    - Panel only visible to captain
  </done>
</task>

<task type="auto">
  <name>Task 5: Create captain reward tracking APIs</name>
  <files>src/app/api/captains/rewards/route.ts, src/app/api/captains/sessions/route.ts</files>
  <read_first>
    - src/models/CaptainAssignment.ts (to update reward fields)
    - src/app/api/captains/assign/route.ts (created in 05-04)
  </read_first>
  <action>
    Create src/app/api/captains/rewards/route.ts:
    - Import NextResponse, getServerSession, connectDB, CaptainAssignment
    - GET handler: authenticate session
    - Find CaptainAssignment where userId = session.user.id and status in ['accepted', 'invited']
    - Calculate: sessionsCaptained (from documents), freeSessionsEarned (floor(sessionsCaptained / 4))
    - Check today's captain sessions: count where assignedAt is today
    - Return 200 with {
        sessionsCaptained,
        freeSessionsEarned,
        progress: sessionsCaptained % 4,
        untilFreeSession: 4 - (sessionsCaptained % 4),
        todaySessionCount,
        canCaptainToday: todaySessionCount < 2
      }

    Create src/app/api/captains/sessions/route.ts:
    - POST handler for incrementing captain session count
    - Authenticate, verify user is captain for this room
    - Find CaptainAssignment by userId + roomId
    - Increment sessionsCaptained by 1
    - If sessionsCaptained % 4 === 0: increment freeSessionsEarned by 1
    - Update lastCaptainSessionDate to today
    - Return 200 with updated reward info
    - Enforce daily limit: If todaySessionCount >= 2, return 403 "Daily limit reached"
  </action>
  <verify>
    <automated>npm test -- api/captains/rewards.test.ts api/captains/sessions.test.ts</automated>
  </verify>
  <done>
    - GET /api/captains/rewards returns captain reward status
    - Progress shows sessions until free session (3/4)
    - Daily limit enforced (max 2 per day)
    - Free session earned every 4 sessions
    - POST /api/captains/sessions increments session count
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Captain controls and rewards system complete with:
    - Aggregate task count API (8/10 format, privacy-first)
    - useCaptainControls hook for captain state management
    - Socket.IO mute events (mute all, mute individual)
    - CaptainPanel component with task count, mute controls, reward progress
    - Reward tracking APIs (free session every 4 captained)
    - Daily limit enforcement (max 2 sessions per day)
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Using admin panel or database, assign yourself as captain for a test room
    3. Join the room as captain
    4. Verify CaptainPanel appears (only visible to captain)
    5. Check task count: "X/Y participants submitted tasks"
    6. Submit a test task as another user (or simulate) and verify count updates
    7. Click "Mute All" - verify all participants muted
    8. Click "Unmute All" - verify all participants unmuted
    9. Check reward progress bar shows sessions until free session
    10. Verify free sessions count displays correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captain → API | Captain can mute any participant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Spoofing | captain-mute-all event | mitigate | Verify socket.data.user.id === room.captainId before processing mute events |
| T-05-17 | Tampering | Captain rewards | mitigate | Calculate rewards server-side from CaptainAssignment records, not client input |
| T-05-18 | Tampering | Daily limit bypass | mitigate | Enforce max 2/day server-side using todaySessionCount calculation |
| T-05-19 | Information Disclosure | Aggregate task count | accept | Shows only count (8/10), not individual task content per D-15 |
</threat_model>

<verification>
1. Aggregate task API returns count only, no individual tasks
2. Captain verification required for all captain APIs
3. Socket.IO verifies captain身份 before processing events
4. Mute state tracked in roomStore
5. CaptainPanel displays aggregate count, mute controls, reward progress
6. Reward tracking: free session every 4 sessions
7. Daily limit: max 2 sessions per day enforced server-side
8. Progress bar shows correct sessions until free session
9. Checkpoint passed: captain controls functional
</verification>

<success_criteria>
1. Captain sees aggregate task count (8/10 submitted)
2. Captain can mute all participants
3. Captain can mute/unmute individual participants
4. Reward progress shows (3/4 until free session)
5. Free session earned every 4 captained sessions
6. Daily limit enforced (max 2 per day)
7. Captain controls only visible to captain
8. Individual task content never exposed
</success_criteria>

<output>
After completion, create `.planning/phases/05-focus-session-features/05-05-SUMMARY.md`
</output>

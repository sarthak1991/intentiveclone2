---
phase: 05-focus-session-features
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/models/Streak.ts
  - src/lib/streak.ts
  - src/app/api/streak/route.ts
  - src/app/api/user/stats/route.ts
  - src/app/api/user/history/route.ts
  - src/hooks/useStreak.ts
  - src/components/profile/StreakBadge.tsx
  - src/components/profile/SessionHistory.tsx
  - src/components/profile/AttendanceStats.tsx
autonomous: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04

must_haves:
  truths:
    - User can see streak counter (consecutive days attended) in nav badge
    - Streak counter shows encouraging "Ready to start a new streak!" on streak break
    - User can view session history (past 7 sessions with status icons)
    - User can view basic attendance stats (total sessions, completion rate)
    - Progress displayed with minimal icons, not overwhelming visuals
    - Streak calculation done server-side to prevent manipulation
  artifacts:
    - path: src/models/Streak.ts
      provides: MongoDB model for streak tracking
      min_lines: 50
    - path: src/lib/streak.ts
      provides: Server-side streak calculation with MongoDB aggregation
      min_lines: 80
    - path: src/components/profile/StreakBadge.tsx
      provides: 🔥 N persistent nav badge component
      min_lines: 40
    - path: src/components/profile/SessionHistory.tsx
      provides: Past 7 sessions list with status icons
      min_lines: 60
    - path: src/components/profile/AttendanceStats.tsx
      provides: Basic stats display (total, completion rate, streak)
      min_lines: 50
  key_links:
    - from: src/lib/streak.ts
      to: SessionCompletion model
      via: MongoDB aggregation pipeline
      pattern: "SessionCompletion.*aggregate"
    - from: src/hooks/useStreak.ts
      to: /api/streak
      via: GET request on mount
      pattern: "fetch.*api/streak"
    - from: src/components/profile/StreakBadge.tsx
      to: src/app/layout.tsx
      via: Import in navigation header
      pattern: "StreakBadge"
---

<objective>
Implement gamification features: streak counter, session history, and attendance stats with minimal icons for ADHD-friendly progress display.

Purpose: Visual progress indicators motivate users through habit formation. Streaks encourage consistent attendance, history shows improvement over time.

Output: Streak calculation, streak badge, session history list, attendance stats display
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-focus-session-features/05-CONTEXT.md
@.planning/phases/05-focus-session-features/05-RESEARCH.md
@.planning/phases/05-focus-session-features/05-01-PLAN.md
@src/models/SessionCompletion.ts
@src/app/api/room/[roomId]/attendance/route.ts
@src/store/roomStore.ts
@src/components/ui/badge.tsx
@src/components/ui/card.tsx
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/models/SessionCompletion.ts (created in 05-01):
```typescript
interface ISessionCompletion {
  userId: string
  roomId: string
  completed: boolean
  attendedAt?: Date
  taskCompletedAt?: Date
}
```

From src/app/layout.tsx (will be modified for StreakBadge):
```typescript
// Navigation header structure - will add StreakBadge component
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Streak model and streak calculation utility</name>
  <files>src/models/Streak.ts, src/lib/streak.ts</files>
  <read_first>
    - src/models/SessionCompletion.ts (for data source)
    - src/models/Registration.ts (for attendance pattern)
  </read_first>
  <behavior>
    - Test 1: Streak model has userId (required), currentStreak (number, default 0), longestStreak (number, default 0), lastSessionDate (Date)
    - Test 2: calculateStreak returns 0 for user with no completed sessions
    - Test 3: calculateStreak returns 1 for user with 1 completed session today
    - Test 4: calculateStreak returns consecutive day count for user with daily sessions
    - Test 5: calculateStreak returns 0 when last session was >1 day ago (streak broken)
    - Test 6: calculateStreak uses user's timezone from User.timezone field
  </behavior>
  <action>
    Create src/models/Streak.ts:
    - Import mongoose, Schema, Model
    - Define IStreak interface: userId (string), currentStreak (number), longestStreak (number), lastSessionDate (Date | null)
    - Create StreakSchema with fields: userId (ref User, required, unique), currentStreak (Number, default 0), longestStreak (Number, default 0), lastSessionDate (Date)
    - Add compound unique index on userId
    - Export Streak model with hot-reload protection

    Create src/lib/streak.ts:
    - Import SessionCompletion, startOfDay, subDays, differenceInDays from date-fns
    - Define StreakInfo interface: currentStreak, longestStreak, lastSessionDate
    - Export async function calculateStreak(userId: string, userTimezone?: string): Promise<StreakInfo>
    - Query SessionCompletion.find({ userId, completed: true, attendedAt: { $exists: true } }).sort({ attendedAt: -1 }).limit(365)
    - If empty, return { currentStreak: 0, longestStreak: 0, lastSessionDate: null }
    - Group by date using Set<string> of ISO dates (startOfDay)
    - Calculate currentStreak: iterate backwards from today, increment for consecutive days, break when gap > 1 day
    - Calculate longestStreak: iterate all dates, track max consecutive sequence
    - Return StreakInfo with calculated values
  </action>
  <verify>
    <automated>npm test -- models/streak.test.ts lib/streak.test.ts</automated>
  </verify>
  <done>
    - Streak model exists with userId, currentStreak, longestStreak, lastSessionDate
    - calculateStreak function returns accurate streak count
    - Streak breaks when gap > 1 day
    - Timezone-aware calculation uses user's timezone
  </done>
</task>

<task type="auto">
  <name>Task 2: Create streak and stats API endpoints</name>
  <files>src/app/api/streak/route.ts, src/app/api/user/stats/route.ts, src/app/api/user/history/route.ts</files>
  <read_first>
    - src/app/api/room/[roomId]/attendance/route.ts (for auth pattern)
    - src/lib/streak.ts (created in Task 1)
  </read_first>
  <action>
    Create src/app/api/streak/route.ts:
    - Import NextResponse, getServerSession, connectDB, calculateStreak from @/lib/streak
    - GET handler: authenticate with getServerSession()
    - Get userId from session.user.id, fetch user from User model to get timezone
    - Call calculateStreak(userId, user.timezone)
    - Return 200 with { currentStreak, longestStreak, lastSessionDate, streakMessage }
    - Generate streakMessage: "Ready to start a new streak!" if currentStreak === 0 else `${currentStreak} day streak! Keep it up!`

    Create src/app/api/user/stats/route.ts:
    - Import dependencies, SessionCompletion, Registration
    - GET handler: authenticate session
    - Calculate totalSessions: count of Registration where userId and status in ['attended', 'no-show']
    - Calculate completedTasks: count of SessionCompletion where userId and completed=true
    - Calculate completionRate: (completedTasks / totalSessions * 100).toFixed(1) or 0
    - Return 200 with { totalSessions, completedTasks, completionRate, currentStreak, longestStreak }

    Create src/app/api/user/history/route.ts:
    - Import dependencies, Room, Registration, SessionCompletion
    - GET handler: authenticate session
    - Query last 7 registrations for user, populate roomId
    - For each registration, fetch corresponding SessionCompletion
    - Return 200 with { sessions: [{ id, roomTitle, scheduledTime, status (attended/no-show), taskCompleted }] }
    - Sort by scheduledTime descending (most recent first)
    - Limit to 7 sessions per D-11
  </action>
  <verify>
    <automated>npm test -- api/streak.test.ts api/user-stats.test.ts api/user-history.test.ts</automated>
  </verify>
  <done>
    - GET /api/streak returns currentStreak, longestStreak, streakMessage
    - GET /api/user/stats returns totalSessions, completedTasks, completionRate
    - GET /api/user/history returns past 7 sessions with status icons
    - All endpoints require authentication
    - Stats calculated server-side from database
  </done>
</task>

<task type="auto">
  <name>Task 3: Create useStreak hook and StreakBadge component</name>
  <files>src/hooks/useStreak.ts, src/components/profile/StreakBadge.tsx</files>
  <read_first>
    - src/hooks/useRoomChat.ts (for hook pattern)
    - src/components/ui/badge.tsx (for Badge component)
  </read_first>
  <action>
    Create src/hooks/useStreak.ts:
    - Import useState, useEffect, useCallback
    - Define hook accepting userId (string | null)
    - State: streak (number), longestStreak (number), streakMessage (string), isLoading (boolean)
    - useEffect: Fetch from /api/streak when userId changes
    - Return { streak, longestStreak, streakMessage, isLoading, refresh }

    Create src/components/profile/StreakBadge.tsx:
    - Import useStreak hook
    - Props interface: userId (string), variant? ('nav' | 'card')
    - Get { streak, isLoading } from useStreak(userId)
    - If isLoading or streak === 0: return null (don't show zero streak per D-09)
    - For nav variant: className="flex items-center gap-1 px-2 py-1 bg-orange-100 text-orange-700 rounded-full text-sm font-medium"
    - Display: 🔥 emoji + streak number
    - For card variant: Show in larger format with "Current streak" label
    - Minimal design: just fire emoji and number, no extra decorations per D-12
  </action>
  <verify>
    <automated>npm test -- hooks/useStreak.test.ts components/StreakBadge.test.tsx</automated>
  </verify>
  <done>
    - useStreak hook fetches streak from API
    - StreakBadge shows 🔥 N when streak > 0
    - StreakBadge hides when streak === 0 (avoid discouragement)
    - Nav variant fits in header navigation
    - Card variant works on profile pages
  </done>
</task>

<task type="auto">
  <name>Task 4: Create SessionHistory component</name>
  <files>src/components/profile/SessionHistory.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (for Card component)
    - src/components/ui/badge.tsx (for Badge component)
  </read_first>
  <action>
    Create src/components/profile/SessionHistory.tsx:
    - Import useState, useEffect, Card, CardHeader, CardTitle, CardContent, Badge
    - Props interface: userId (string)
    - State: sessions (array), isLoading (boolean)
    - useEffect: Fetch from /api/user/history on mount
    - CardHeader: "Past 7 Sessions" title
    - Render list of sessions:
      - For each session: room title, scheduled time (formatted "ddd, MMM D at h:mm a"), status badge
      - Status icons: ✓ green badge for attended+taskCompleted, ○ gray for attended+incomplete, ✗ red for no-show
      - Empty state: "No sessions attended yet. Join your first focus room!"
    - Show only 7 sessions maximum (per D-11)
    - Use minimal icons, not overwhelming calendar view
    - Format dates with date-fns formatRelative or format for readability
  </action>
  <verify>
    <automated>npm test -- components/SessionHistory.test.tsx</automated>
  </verify>
  <done>
    - SessionHistory shows past 7 sessions
    - Status icons: ✓ (completed), ○ (incomplete), ✗ (no-show)
    - Dates formatted for readability
    - Empty state shows helpful message
    - Minimal design, no calendar complexity
  </done>
</task>

<task type="auto">
  <name>Task 5: Create AttendanceStats component and integrate StreakBadge in nav</name>
  <files>src/components/profile/AttendanceStats.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (for Card component)
    - src/app/layout.tsx (for navigation header)
  </read_first>
  <action>
    Create src/components/profile/AttendanceStats.tsx:
    - Import useState, useEffect, Card, CardHeader, CardTitle, CardContent
    - Props interface: userId (string)
    - State: stats (object with totalSessions, completedTasks, completionRate, currentStreak, longestStreak), isLoading
    - useEffect: Fetch from /api/user/stats on mount
    - CardHeader: "Your Progress" title
    -CardContent grid layout (3 columns on desktop, 1 on mobile):
      - Stat 1: "Sessions Attended" + totalSessions number
      - Stat 2: "Tasks Completed" + completedTasks number
      - Stat 3: "Completion Rate" + completionRate + "%"
      - Stat 4: "Current Streak" + 🔥 currentStreak
      - Stat 5: "Longest Streak" + 🔥 longestStreak
    - Use minimal numbers with small labels, no complex charts per D-12

    Modify src/app/layout.tsx:
    - Import StreakBadge from @/components/profile/StreakBadge
    - Import useSession from next-auth/react
    - In navigation header: Add StreakBadge component when session exists
    - Position: Right side of header, before user menu
    - Pass userId from session.user.id
  </action>
  <verify>
    <automated>npm test -- components/AttendanceStats.test.tsx</automated>
  </verify>
  <done>
    - AttendanceStats displays 5 key metrics
    - Numbers prominent, labels minimal
    - StreakBadge visible in navigation header
    - StreakBadge shows 🔥 N for users with active streak
    - StreakBadge hides for new users (streak === 0)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Streak calculation must be server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | GET /api/streak | mitigate | Calculate streak server-side using MongoDB aggregation, never trust client-side calculation |
| T-05-10 | Tampering | GET /api/user/stats | mitigate | All stats calculated from database records, not from client-provided data |
| T-05-11 | Information Disclosure | GET /api/user/history | mitigate | Verify userId matches session.user.id before returning history |
</threat_model>

<verification>
1. Streak model created with required fields
2. calculateStreak function uses MongoDB aggregation
3. Streak calculation timezone-aware
4. Streak breaks correctly when gap > 1 day
5. StreakBadge displays in navigation header
6. SessionHistory shows past 7 sessions with status icons
7. AttendanceStats displays 5 metrics with minimal design
8. All API endpoints require authentication
</verification>

<success_criteria>
1. User sees 🔥 N badge in navigation when streak > 0
2. Streak calculation accurate based on completed sessions
3. "Ready to start a new streak!" message on streak break
4. Session history shows past 7 sessions with status icons
5. Attendance stats display total, completion rate, streaks
6. All calculations done server-side
7. Minimal icons used throughout (no visual overload)
</success_criteria>

<output>
After completion, create `.planning/phases/05-focus-session-features/05-03-SUMMARY.md`
</output>

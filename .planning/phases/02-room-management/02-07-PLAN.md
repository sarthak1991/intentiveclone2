---
phase: 02-room-management
plan: 07
type: execute
wave: 4
depends_on:
  - 02-05
  - 02-06
files_modified:
  - tests/integration/room-flow.test.ts
  - tests/integration/admin-flow.test.ts
  - src/app/room/[id]/page.tsx
  - .env.local.example
  - README.md
  - .planning/phases/02-room-management/02-SUMMARY.md
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-05
  - ADMN-01
  - ADMN-06
  - ADMN-08
  - TECH-04
user_setup:
  - service: mongo-database
    why: "MongoDB required for room data storage"
    env_vars:
      - name: MONGODB_URI
        source: "Set to mongodb://localhost:27017/focusflow for local dev"
    dashboard_config:
      - task: "Start MongoDB"
        location: "Docker: docker-compose -f .docker/mongodb/docker-compose.yml up -d"
  - service: socket-server
    why: "Socket.IO server required for WebSocket connections"
    env_vars:
      - name: SOCKET_PORT
        source: "Set to 3001 in .env.local"
    dashboard_config:
      - task: "Start Socket.IO server"
        location: "cd server && npm start OR ts-node server/socket-server.ts"
must_haves:
  truths:
    - User can complete end-to-end room browsing and registration flow
    - Admin can complete end-to-end room creation and management flow
    - All API endpoints work with authentication and authorization
    - Socket.IO server connects and authenticates successfully
    - All tests passing (unit, integration, component)
    - Documentation is updated with Phase 2 setup instructions
  artifacts:
    - path: tests/integration/room-flow.test.ts
      provides: End-to-end user room flow tests
      min_lines: 100
      contains: Browse rooms, register, cancel, join, verify state
    - path: tests/integration/admin-flow.test.ts
      provides: End-to-end admin room management tests
      min_lines: 100
      contains: Create room, edit, cancel, manage no-shows, manage tags
    - path: src/app/room/[id]/page.tsx
      provides: Individual room detail page (pre-session lobby)
      min_lines: 60
      contains: Room details, participant list, countdown timer, join button
    - path: README.md
      provides: Updated project documentation with Phase 2 setup
      min_lines: 200
      contains: Phase 2 features, setup instructions, API docs, troubleshooting
  key_links:
    - from: Integration tests
      to: All APIs
      via: fetch() calls to API routes
      pattern: describe\(['\"]Integration['\"].*room flow
    - from: Room detail page
      to: GET /api/rooms/[id]
      via: fetch room details
      pattern: fetch\(`/api/rooms/\${id}\)
    - from: README
      to: All Phase 2 components
      via: Documentation
      pattern: ## Phase 2.*Room Management
---

<objective>
Create comprehensive integration tests for user and admin room management flows, build room detail page for pre-session lobby, update project documentation with Phase 2 setup instructions, and verify all functionality works end-to-end to ensure Phase 2 is production-ready.

Purpose: Validate that all Phase 2 components work together correctly. Integration tests catch issues that unit tests miss. Documentation ensures Phase 2 can be set up and run by other developers.

Output: Passing integration test suite, functional room detail page, comprehensive documentation, and verified Phase 2 completion.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-room-management/02-RESEARCH.md
@src/app/rooms/page.tsx
@src/app/admin/rooms/page.tsx
@src/app/api/rooms/route.ts

# All Phase 2 components built in Waves 1-3
- Wave 1: Database models (Plan 01), Socket.IO server (Plan 02)
- Wave 2: Business logic (Plan 03), API routes (Plan 04)
- Wave 3: User UI (Plan 05), Admin UI (Plan 06)

# Test patterns from Phase 1
- Integration tests use fetch() to call API routes
- Test database isolation (beforeEach cleanup)
- Mock auth sessions with test users
- Gentle error messages throughout
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for user room flow</name>
  <files>tests/integration/room-flow.test.ts</files>
  <action>
Create tests/integration/room-flow.test.ts with:

1. **Test suite setup:**
   - describe('Integration: User Room Flow')
   - beforeAll: start test database, create test users (regular + admin)
   - afterAll: cleanup database, close connections
   - beforeEach: reset database state

2. **Test: User views room list**
   - Create 8 rooms for today
   - Login as regular user
   - GET /api/rooms
   - Assert: returns 8 rooms
   - Assert: rooms have displayTime in user timezone
   - Assert: rooms have registrationStatus

3. **Test: User registers for room (within 30-min window)**
   - Create room with scheduledTime = now + 20 min
   - Login as regular user
   - POST /api/rooms/[id]/register
   - Assert: returns 201 with updated room
   - Assert: user added to room.participants
   - Assert: Registration document created

4. **Test: User cannot register before 30-min window**
   - Create room with scheduledTime = now + 40 min
   - Login as regular user
   - POST /api/rooms/[id]/register
   - Assert: returns 400 with error message
   - Assert: user NOT added to participants

5. **Test: User cannot register for full room**
   - Create room with capacity = 12, add 12 participants
   - Login as regular user
   - POST /api/rooms/[id]/register
   - Assert: returns 400 (room full)
   - Assert: user NOT added to participants

6. **Test: User cancels registration**
   - Create room with user registered
   - DELETE /api/rooms/[id]/register
   - Assert: returns 200
   - Assert: user removed from participants
   - Assert: Registration.status = 'cancelled'

7. **Test: Race condition protection**
   - Create room with capacity = 1
   - Send 10 concurrent registration requests
   - Assert: Only 1 user added to participants
   - Assert: 9 requests fail with "room full"

Use fetch() for API calls, create real database documents.
  </action>
  <verify>
npm test -- tests/integration/room-flow.test.ts -- --run
  </verify>
  <done>User room flow integration tests passing (7+ tests)</done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests for admin room management flow</name>
  <files>tests/integration/admin-flow.test.ts</files>
  <action>
Create tests/integration/admin-flow.test.ts with:

1. **Test suite setup:**
   - describe('Integration: Admin Room Management')
   - beforeAll: create admin user, regular users
   - afterAll: cleanup

2. **Test: Admin creates room**
   - Login as admin
   - POST /api/admin/rooms with room data
   - Assert: returns 201 with created room
   - Assert: room saved to database
   - Assert: room has correct scheduledTime, capacity, status

3. **Test: Non-admin cannot create room**
   - Login as regular user
   - POST /api/admin/rooms
   - Assert: returns 403 (Forbidden)
   - Assert: room NOT created

4. **Test: Admin updates room**
   - Create existing room
   - Login as admin
   - PATCH /api/rooms/[id] with updates
   - Assert: returns 200 with updated room
   - Assert: changes saved to database

5. **Test: Admin cancels room**
   - Create existing room with participants
   - Login as admin
   - DELETE /api/rooms/[id]
   - Assert: returns 200
   - Assert: room.status = 'cancelled'
   - Assert: all registrations cancelled

6. **Test: Admin manages no-show**
   - Create room with registered user
   - Add user to waitlist
   - Login as admin
   - POST /api/admin/rooms/[id]/noshow with userId and remarks
   - Assert: returns 200
   - Assert: no-show user removed from participants
   - Assert: waitlist user promoted to participant
   - Assert: Registration.status = 'no-show'

7. **Test: Admin creates interest tag**
   - Login as admin
   - POST /api/admin/tags (if endpoint exists)
   - Assert: returns 201 with created tag
   - Assert: tag saved to database

Use fetch() for API calls, verify database state changes.
  </action>
  <verify>
npm test -- tests/integration/admin-flow.test.ts -- --run
  </verify>
  <done>Admin room management integration tests passing (7+ tests)</done>
</task>

<task type="auto">
  <name>Task 3: Create room detail page (pre-session lobby)</name>
  <files>src/app/room/[id]/page.tsx</files>
  <action>
Create src/app/room/[id]/page.tsx with:

1. **Server component:**
   - const session = await auth()
   - If not authenticated, redirect to /login
   - Get roomId from params.id
   - Fetch room: await Room.findById(roomId).populate('participants')
   - Check if user is registered for this room
   - Pass data to client component

2. **Page structure:**
   - Page title: "Focus Room - {displayTime}"
   - Room details card:
     - Room time (displayTime in user timezone)
     - Countdown timer to session start
     - Participant count: "X / 12 participants"
     - Status badge
     - Interest tags
   - Participant list:
     - Grid of participant cards (name, photo)
     - "Joining soon..." status for participants not yet in room
   - Action section:
     - If registered: "Join Room" button (prominent)
     - If not registered: "Register" button (if within window)
     - If room full: "Room Full" message

3. **Countdown timer:**
   - Calculate time until session starts
   - Display: "Starts in 25 minutes" or "Starts at 9:00 AM"
   - Update every second

4. **Join room button:**
   - If registered and room is "open" or "in-progress": enabled
   - Click → navigate to video room (Phase 4, placeholder for now)
   - Show disabled state if not registered

5. **Error states:**
   - Room not found: 404 page
   - Not authenticated: redirect to /login
   - Room cancelled: show cancellation message

Per requirement ROOM-05: One-click join from registered rooms.
  </action>
  <verify>
Manual test: Visit /room/{valid-id}, verify page loads correctly
  </verify>
  <done>Room detail page with countdown, participant list, and join button</done>
</task>

<task type="auto">
  <name>Task 4: Update README.md with Phase 2 documentation</name>
  <files>README.md</files>
  <action>
Update README.md with:

1. **Phase 2 features section:**
   - "## Phase 2: Room Management"
   - List features: Room browsing, registration, admin panel, Socket.IO server
   - Describe user flows: View rooms → Register → Join
   - Describe admin flows: Create rooms → Manage schedule → Handle no-shows

2. **Setup instructions:**
   - "### Prerequisites"
   - MongoDB running (Docker or local)
   - Socket.IO server running on port 3001
   - Room scheduler cron job running (or manual room creation)

3. **Starting the application:**
   - Terminal 1: MongoDB (Docker compose)
   - Terminal 2: Next.js dev server (npm run dev)
   - Terminal 3: Socket.IO server (cd server && npm start)
   - Terminal 4: Room scheduler (optional, or rooms created manually)

4. **API documentation:**
   - List all Phase 2 API endpoints
   - Describe request/response formats
   - Note authentication/authorization requirements

5. **Environment variables:**
   - Add Phase 2 variables to .env.local.example:
     - SOCKET_PORT=3001
     - NEXT_PUBLIC_APP_URL=http://localhost:3000

6. **Troubleshooting:**
   - "Rooms not showing: Check MongoDB connection and room scheduler"
   - "Registration failing: Check 30-minute window and room capacity"
   - "Socket.IO connection failing: Check port 3001 and JWT secret"
   - "Admin panel 403: Update user.role to 'admin' in database"

7. **Testing:**
   - How to run tests: npm test
   - How to run specific test suites: npm test -- tests/integration/
   - Coverage report generation

Follow Phase 1 README format for consistency.
  </action>
  <verify>
grep -q "Phase 2.*Room Management" README.md
  </verify>
  <done>README.md updated with Phase 2 documentation</done>
</task>

<task type="auto">
  <name>Task 5: Run full test suite and verify Phase 2 completion</name>
  <files>.planning/phases/02-room-management/02-SUMMARY.md</files>
  <action>
1. **Run all tests:**
   ```bash
   npm test -- --run --coverage
   ```

2. **Verify test coverage:**
   - Models: Room, Registration, InterestTag
   - Business logic: rooms, timezone, admin
   - API routes: /api/rooms, /api/admin/rooms
   - Components: RoomList, RoomCard, RegisterButton, admin components
   - Integration: room-flow, admin-flow
   - Socket.IO: socket-server, socket-client

3. **Create Phase 2 SUMMARY.md:**
   - List all completed plans (01-07)
   - Document all created files
   - List all requirements completed
   - Record tech stack additions
   - Note any deviations or issues
   - Verify all success criteria met

4. **Verify all success criteria:**
   - [x] User can view 8 daily scheduled rooms
   - [x] User can toggle calendar/list view
   - [x] User can register 30 min before session
   - [x] User can join registered room
   - [x] Admin can create/schedule rooms
   - [x] Admin can manage no-shows
   - [x] Admin can add interest tags
   - [x] Socket.IO server operational

5. **Update ROADMAP.md:**
   - Mark Phase 2 plans as complete
   - Update progress bar

6. **Update STATE.md:**
   - Set current phase to Phase 2
   - Update progress to 2/7 phases complete
   - Document Phase 2 completion

Use gsd-tools for git commit and state updates.
  </action>
  <verify>
npm test -- --run --coverage | grep "Coverage"
  </verify>
  <done>All tests passing, Phase 2 documented and verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration tests → Real APIs | Tests call actual API routes with real database |
| Documentation → Users | Users follow setup instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-38 | Information Disclosure | Test data in production | mitigate | Tests use separate database. Test users cleaned up. |
| T-02-39 | Denial of Service | Test resource exhaustion | accept | Tests run sequentially, cleanup after each suite. |
| T-02-40 | Spoofing | Documentation credentials | mitigate | No real credentials in README. Use example values. |
| T-02-41 | Elevation of Privilege | Admin role setup | mitigate | Document admin role setup clearly. First admin manual database update. |
| T-02-42 | Tampering | Room detail page access | mitigate | Server-side session check. Redirect if not authenticated. |
</threat_model>

<verification>
1. Integration tests cover complete user flow
2. Integration tests cover complete admin flow
3. Race condition tests verify atomic operations
4. Room detail page displays correctly
5. Countdown timer updates in real-time
6. Join button navigates correctly
7. README.md documents all Phase 2 features
8. Setup instructions are clear and complete
9. All tests passing (unit + integration + component)
10. Phase 2 success criteria all met
11. ROADMAP.md and STATE.md updated
</verification>

<success_criteria>
1. Integration test suite created and passing (14+ tests)
2. User flow tests cover browse → register → join
3. Admin flow tests cover create → manage → no-show
4. Race condition tests verify atomic operations
5. Room detail page functional with countdown timer
6. README.md updated with Phase 2 documentation
7. All tests passing (npm test -- --run)
8. Test coverage >80% for new code
9. Phase 2 all success criteria verified
10. Phase 2 ready for verification
11. Phase 2 ready for Phase 3 handoff
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-management/02-07-SUMMARY.md`
</output>

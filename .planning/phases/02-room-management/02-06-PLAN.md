---
phase: 02-room-management
plan: 06
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - src/app/admin/rooms/page.tsx
  - src/app/admin/rooms/create/page.tsx
  - src/components/admin/CreateRoomForm.tsx
  - src/components/admin/RoomManagePanel.tsx
  - src/components/admin/NoShowManager.tsx
  - src/components/admin/InterestTagManager.tsx
  - tests/components/admin/CreateRoomForm.test.tsx
  - tests/components/admin/RoomManagePanel.test.tsx
autonomous: true
requirements:
  - ADMN-01
  - ADMN-06
  - ADMN-08
user_setup: []
must_haves:
  truths:
    - Admin can view all rooms in management panel
    - Admin can create new rooms with custom time, capacity, and tags
    - Admin can edit existing room details
    - Admin can cancel rooms
    - Admin can mark users as no-show and reassign slots
    - Admin can add new interest tags
    - Admin panel is only accessible to admin users
  artifacts:
    - path: src/app/admin/rooms/page.tsx
      provides: Admin room management dashboard
      min_lines: 60
      contains: RoomManagePanel, admin auth check, fetch rooms
    - path: src/app/admin/rooms/create/page.tsx
      provides: Admin room creation page
      min_lines: 40
      contains: CreateRoomForm, API call, redirect
    - path: src/components/admin/CreateRoomForm.tsx
      provides: Room creation form with validation
      min_lines: 100
      contains: Form fields, zod validation, API submission
    - path: src/components/admin/RoomManagePanel.tsx
      provides: Room list with edit/cancel actions
      min_lines: 100
      contains: Room table, action buttons, status updates
    - path: src/components/admin/NoShowManager.tsx
      provides: No-show management interface
      min_lines: 80
      contains: User list, no-show button, remarks input
    - path: src/components/admin/InterestTagManager.tsx
      provides: Interest tag CRUD interface
      min_lines: 80
      contains: Tag list, add/edit/delete actions
  key_links:
    - from: Admin pages
      to: Admin auth
      via: requireAdmin() check in server components
      pattern: await requireAdmin\(request\)
    - from: CreateRoomForm
      to: POST /api/admin/rooms
      via: fetch() on submit
      pattern: fetch\('/api/admin/rooms',.*method:\s*['\"]POST['\"]
    - from: RoomManagePanel
      to: PATCH /api/rooms/[id]
      via: fetch() on edit
      pattern: fetch\(`/api/rooms/\${id}`,.*method:\s*['\"]PATCH['\"]
    - from: NoShowManager
      to: POST /api/admin/rooms/[id]/noshow
      via: fetch() on no-show
      pattern: fetch\(`/api/admin/rooms/\${id}/noshow`,.*method:\s*['\"]POST['\"]
---

<objective>
Create admin panel UI components for room management including room creation form, room management panel with edit/cancel actions, no-show management interface, and interest tag manager to enable admins to fully manage the room schedule and handle operational issues.

Purpose: Provide the admin interface for room management operations. Admins can create rooms, manage the schedule, handle no-shows, and manage interest tags.

Output: Working admin panel with room creation, management, no-show handling, and tag management with comprehensive test coverage.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-room-management/02-RESEARCH.md
@src/app/api/admin/rooms/route.ts
@src/lib/admin.ts
@src/models/Room.ts

# Admin API shape from Wave 2, Plan 04
POST /api/admin/rooms: Create room (admin only)
POST /api/admin/rooms/[id]/noshow: Manage no-show (admin only)

# Admin auth pattern from Wave 2
requireAdmin() function in src/lib/admin.ts:
- Returns { authorized: boolean, user?: IUser, error?: string }
- Use in server components or API routes

# UI patterns from Phase 1
- shadcn/ui Form, Input, Button, Dialog components
- React Hook Form for form management
- Zod for validation
- Gentle error messages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CreateRoomForm component with validation</name>
  <files>src/components/admin/CreateRoomForm.tsx</files>
  <action>
Create src/components/admin/CreateRoomForm.tsx with:

1. **Form fields:**
   - title: text input (optional, default "Focus Room")
   - scheduledTime: datetime-local input (required)
   - duration: number input (optional, default 45, min 15, max 120)
   - capacity: number input (optional, default 12, min 1, max 12)
   - interestTags: multi-select or text input with comma separation

2. **Validation:**
   - Use Zod schema (same as API validation)
   - React Hook Form for form state
   - Show validation errors inline
   - Gentle error messages per D-07

3. **Form submission:**
   - On submit → POST to /api/admin/rooms
   - Show loading spinner while submitting
   - Show success toast on success
   - Show error toast on failure
   - Redirect to /admin/rooms after success

4. **Styling:**
   - Use shadcn/ui Form components
   - Clean layout with labels and help text
   - Responsive design

5. **Default values:**
   - scheduledTime: next available hour
   - duration: 45
   - capacity: 12
   - title: "Focus Room"

Use React Hook Form + Zod pattern from Phase 1.
  </action>
  <verify>
npm test -- tests/components/admin/CreateRoomForm.test.tsx -t "should validate form inputs"
  </verify>
  <done>CreateRoomForm with validation and API integration</done>
</task>

<task type="auto">
  <name>Task 2: Create RoomManagePanel component for room listing and actions</name>
  <files>src/components/admin/RoomManagePanel.tsx</files>
  <action>
Create src/components/admin/RoomManagePanel.tsx with:

1. **Props interface:**
   - interface RoomManagePanelProps { rooms: IRoom[], onRefresh: () => void }

2. **Component structure:**
   - Table layout with columns: Time, Title, Status, Participants, Actions
   - Sortable by time
   - Filterable by status
   - Pagination or infinite scroll for many rooms

3. **Action buttons per room:**
   - Edit: Opens dialog with room details edit form
   - Cancel: Cancels room (with confirmation dialog)
   - View: Opens room details modal

4. **Room editing:**
   - Dialog with form fields (title, scheduledTime, capacity, status)
   - PATCH to /api/rooms/[id] on submit
   - Refresh room list after update

5. **Room cancellation:**
   - Confirmation dialog: "Are you sure you want to cancel this room?"
   - DELETE to /api/rooms/[id] on confirm
   - Refresh room list after cancellation

6. **Status badges:**
   - Color-coded status badges (same as user-facing UI)

7. **Empty state:**
   - "No rooms found. Create a new room to get started."

Use shadcn/ui Table and Dialog components.
  </action>
  <verify>
npm test -- tests/components/admin/RoomManagePanel.test.tsx -t "should render room table"
npm test -- tests/components/admin/RoomManagePanel.test.tsx -t "should handle room cancellation"
  </verify>
  <done>RoomManagePanel with table layout and action buttons</done>
</task>

<task type="auto">
  <name>Task 3: Create NoShowManager component for no-show handling</name>
  <files>src/components/admin/NoShowManager.tsx</files>
  <action>
Create src/components/admin/NoShowManager.tsx with:

1. **Props interface:**
   - interface NoShowManagerProps { roomId: string, participants: IUser[], onNoShowRecorded: () => void }

2. **Component structure:**
   - List of registered participants for a room
   - Each participant has:
     - Name and photo
     - "Mark No-Show" button
     - Remarks input (optional)
     - Status indicator (attended / no-show / pending)

3. **No-show marking flow:**
   - Click "Mark No-Show" → show confirmation dialog
   - Input remarks (optional): "Didn't join, didn't respond"
   - Confirm → POST to /api/admin/rooms/[id]/noshow
   - Show loading state while submitting
   - Show success/error toast
   - Refresh room data on success

4. **Waitlist handling:**
   - If waitlist exists, show promoted user
   - Display message: "No-show recorded, [User Name] promoted from waitlist"

5. **Styling:**
   - Card or list layout
   - Clear visual hierarchy
   - Responsive design

Per requirement ADMN-06: Admin can manage no-show reassignments.
  </action>
  <verify>
npm test -- tests/components/admin/NoShowManager.test.tsx -t "should mark user as no-show"
  </verify>
  <done>NoShowManager with participant list and no-show marking</done>
</task>

<task type="auto">
  <name>Task 4: Create InterestTagManager component for tag CRUD</name>
  <files>src/components/admin/InterestTagManager.tsx</files>
  <action>
Create src/components/admin/InterestTagManager.tsx with:

1. **Component structure:**
   - List of existing interest tags (from InterestTag model)
   - "Add Tag" button → opens dialog
   - Each tag has: name, description, isActive, Edit button, Delete button
   - Filter by active/inactive

2. **Add tag dialog:**
   - Form fields: name (required), description (optional)
   - Create tag → POST to /api/admin/tags (create this endpoint)
   - Show success/error toast
   - Refresh tag list

3. **Edit tag dialog:**
   - Pre-fill with existing tag data
   - Update tag → PATCH to /api/admin/tags/[id] (create this endpoint)
   - Show success/error toast
   - Refresh tag list

4. **Delete tag confirmation:**
   - "Are you sure? This tag will be removed from all rooms."
   - DELETE to /api/admin/tags/[id] (create this endpoint)
   - Show success/error toast
   - Refresh tag list

5. **Tag activation:**
   - Toggle active/inactive status
   - PATCH to /api/admin/tags/[id] with isActive: boolean

6. **API endpoints to create** (if not exists):
   - GET /api/admin/tags - List all tags
   - POST /api/admin/tags - Create tag
   - PATCH /api/admin/tags/[id] - Update tag
   - DELETE /api/admin/tags/[id] - Delete tag

Per requirement ADMN-08: Admin can add new interest tags.
  </action>
  <verify>
npm test -- tests/components/admin/InterestTagManager.test.tsx -t "should create interest tag"
npm test -- tests/components/admin/InterestTagManager.test.tsx -t "should delete interest tag"
  </verify>
  <done>InterestTagManager with tag CRUD operations</done>
</task>

<task type="auto">
  <name>Task 5: Create admin rooms page with RoomManagePanel</name>
  <files>src/app/admin/rooms/page.tsx</files>
  <action>
Create src/app/admin/rooms/page.tsx with:

1. **Server component:**
   - Check admin authorization: const authCheck = await requireAdmin(request)
   - If not authorized, redirect to /login or show 403
   - Fetch rooms: await getTodaysRooms() or fetch all rooms
   - Pass rooms to client component

2. **Page structure:**
   - Page title: "Room Management"
   - Action buttons: "Create Room", "Manage Tags"
   - RoomManagePanel component with rooms
   - Statistics: "8 rooms today", "24 participants", "3 full rooms"

3. **Quick actions:**
   - "Create Room" button → Link to /admin/rooms/create
   - "Manage Interest Tags" → Opens InterestTagManager dialog
   - "View Schedule" → Link to /rooms (user view)

4. **Layout:**
   - Admin sidebar navigation (if exists)
   - Main content area with RoomManagePanel
   - Responsive design

5. **Error handling:**
   - If not admin: show 403 page
   - If fetch fails: show error message and retry button

6. **Metadata:**
   - SEO metadata for admin panel
   - "FocusFlow - Admin Room Management"

Use server component pattern from Next.js 13 App Router.
  </action>
  <verify>
npm test -- tests/components/admin/RoomManagePanel.test.tsx -t "should require admin access"
  </verify>
  <done>Admin rooms page with authorization check and RoomManagePanel</done>
</task>

<task type="auto">
  <name>Task 6: Create admin room creation page</name>
  <files>src/app/admin/rooms/create/page.tsx</files>
  <action>
Create src/app/admin/rooms/create/page.tsx with:

1. **Server component:**
   - Check admin authorization: const authCheck = await requireAdmin(request)
   - If not authorized, redirect to /login or show 403
   - Fetch interest tags (for tag selection in form)
   - Pass data to client component

2. **Page structure:**
   - Page title: "Create New Room"
   - Breadcrumb: "Admin > Rooms > Create"
   - CreateRoomForm component
   - "Cancel" button → Link back to /admin/rooms

3. **Layout:**
   - Centered form container
   - Max-width for readability
   - Responsive design

4. **Error handling:**
   - If not admin: show 403 page
   - If tag fetch fails: show warning but allow form submission

5. **Form submission:**
   - On success: redirect to /admin/rooms
   - On error: show error message, stay on form

Use same layout pattern as other admin pages.
  </action>
  <verify>
npm test -- tests/components/admin/CreateRoomForm.test.tsx -t "should require admin access"
  </verify>
  <done>Admin room creation page with authorization and form</done>
</task>

<task type="auto">
  <name>Task 7: Create admin component tests</name>
  <files>tests/components/admin/CreateRoomForm.test.tsx, tests/components/admin/RoomManagePanel.test.tsx, tests/components/admin/NoShowManager.test.tsx, tests/components/admin/InterestTagManager.test.tsx</files>
  <action>
Create test suites for admin components:

**tests/components/admin/CreateRoomForm.test.tsx:**
- describe('CreateRoomForm')
- Test: validates required fields
- Test: validates datetime format
- Test: validates capacity limits
- Test: submits form with correct data
- Test: shows loading state while submitting
- Test: shows success toast on success
- Test: shows error toast on failure

**tests/components/admin/RoomManagePanel.test.tsx:**
- describe('RoomManagePanel')
- Test: renders room table
- Test: sorts rooms by time
- Test: filters rooms by status
- Test: opens edit dialog
- Test: confirms room cancellation
- Test: handles room update
- Test: handles room cancellation

**tests/components/admin/NoShowManager.test.tsx:**
- describe('NoShowManager')
- Test: renders participant list
- Test: shows no-show confirmation dialog
- Test: submits no-show with remarks
- Test: shows promoted user message
- Test: refreshes room data on success

**tests/components/admin/InterestTagManager.test.tsx:**
- describe('InterestTagManager')
- Test: renders tag list
- Test: opens add tag dialog
- Test: creates new tag
- Test: edits existing tag
- Test: deletes tag with confirmation
- Test: toggles tag active status

Use React Testing Library patterns from Phase 1 tests.
  </action>
  <verify>
npm test -- tests/components/admin/ -- --run
  </verify>
  <done>All admin component tests passing (20+ tests)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Admin API | Admin-only endpoints, server-side authorization required |
| Admin actions → Room data | Admin can modify rooms, must validate all inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-32 | Elevation of Privilege | Admin page access | mitigate | Server-side requireAdmin() check. Redirect non-admins to 403. |
| T-02-33 | Tampering | Room creation input | mitigate | Zod validation on client + server. Allow only defined fields. |
| T-02-34 | Tampering | Room updates | mitigate | Server-side validation in API. Admin role check. |
| T-02-35 | Information Disclosure | Admin panel | mitigate | Server-side authorization. Can't access pages without admin role. |
| T-02-36 | Tampering | No-show marking | mitigate | Server-side admin check. Validate user is registered for room. |
| T-02-37 | Denial of Service | Tag creation | accept | Authenticated admin users only. Low risk. |
</threat_model>

<verification>
1. Admin rooms page requires admin role
2. CreateRoomForm validates all inputs
3. CreateRoomForm submits to admin API
4. RoomManagePanel displays room table
5. RoomManagePanel handles edit/cancel actions
6. NoShowManager marks no-shows with remarks
7. NoShowManager shows promoted user message
8. InterestTagManager creates/edits/deletes tags
9. All admin components have auth checks
10. All tests passing (npm test -- tests/components/admin/)
</verification>

<success_criteria>
1. Admin rooms page accessible only to admin users
2. CreateRoomForm validates inputs with Zod
3. CreateRoomForm submits to admin API successfully
4. RoomManagePanel displays room table with actions
5. RoomManagePanel handles edit and cancel operations
6. NoShowManager lists participants and marks no-shows
7. NoShowManager shows waitlist promotion
8. InterestTagManager manages tag CRUD
9. All admin components have server-side auth checks
10. Component tests cover UI, interactions, and auth
11. All tests passing (20+ tests)
12. Admin panel ready for integration testing
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-management/02-06-SUMMARY.md`
</output>

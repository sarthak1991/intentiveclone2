---
phase: 02-room-management
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - src/app/rooms/page.tsx
  - src/components/rooms/RoomList.tsx
  - src/components/rooms/RoomCalendar.tsx
  - src/components/rooms/RoomCard.tsx
  - src/components/rooms/RegisterButton.tsx
  - src/components/rooms/JoinRoomButton.tsx
  - src/lib/rooms.ts
  - tests/components/rooms/RoomList.test.tsx
  - tests/components/rooms/RoomCalendar.test.tsx
  - tests/components/rooms/RoomCard.test.tsx
  - tests/components/rooms/RegisterButton.test.tsx
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-05
user_setup: []
must_haves:
  truths:
    - User can view 8 daily scheduled rooms in list view
    - User can toggle between calendar view and list view
    - Room times display in user's local timezone
    - Register button shows correct state (closed, open, registered, full)
    - Registration opens 30 minutes before session start
    - Join room button appears for registered rooms
    - One-click join navigates to room page
  artifacts:
    - path: src/app/rooms/page.tsx
      provides: Main rooms page with view toggle
      min_lines: 50
      contains: RoomList, RoomCalendar, view state, API fetch
    - path: src/components/rooms/RoomList.tsx
      provides: List view of rooms with time, capacity, registration button
      min_lines: 80
      contains: map rooms to RoomCard, displayTime, participant count
    - path: src/components/rooms/RoomCalendar.tsx
      provides: Calendar view using shadcn/ui calendar component
      min_lines: 60
      contains: shadcn/ui Calendar, date selection, room highlighting
    - path: src/components/rooms/RoomCard.tsx
      provides: Individual room display with details and actions
      min_lines: 80
      contains: room time, capacity, status, RegisterButton, JoinRoomButton
    - path: src/components/rooms/RegisterButton.tsx
      provides: Registration button with state-based UI
      min_lines: 60
      contains: registrationStatus states, API call, loading/error states
    - path: src/components/rooms/JoinRoomButton.tsx
      provides: One-click join button for registered rooms
      min_lines: 30
      contains: Link to /room/[id], disabled state
  key_links:
    - from: Room page
      to: GET /api/rooms
      via: fetch() on mount
      pattern: fetch\('/api/rooms'\)
    - from: RegisterButton
      to: POST /api/rooms/[id]/register
      via: fetch() on click
      pattern: fetch\(`/api/rooms/\${id}/register`,.*method:\s*['\"]POST['\"]
    - from: JoinRoomButton
      to: /room/[id] page
      via: Next.js Link component
      pattern: <Link.*href=.*room
    - from: RoomCalendar
      to: shadcn/ui Calendar
      via: import from @/components/ui/calendar
      pattern: import.*Calendar.*from.*calendar
---

<objective>
Create user-facing room management UI components including room listing page, calendar/list view toggle, room cards with registration buttons, and one-click join functionality to enable users to browse and register for focus rooms.

Purpose: Provide the frontend interface for room management features. Users can view scheduled rooms, see times in their timezone, register for sessions, and join rooms they've registered for.

Output: Working room page with calendar/list views, registration flow with state-based buttons, and one-click join navigation with comprehensive test coverage.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-room-management/02-RESEARCH.md
@src/app/api/rooms/route.ts
@src/lib/rooms.ts
@src/lib/auth.ts

# API shape from Wave 2, Plan 04
GET /api/rooms returns:
```json
{
  "rooms": [{
    "_id": "...",
    "scheduledTime": "2026-04-07T09:00:00Z",
    "displayTime": "9:00 AM",
    "capacity": 12,
    "participantCount": 5,
    "status": "open",
    "registrationStatus": {
      "status": "open",
      "canRegister": true,
      "message": "Register now"
    }
  }]
}
```

# UI patterns from Phase 1
- shadcn/ui components already installed
- Tailwind CSS for styling
- Client components with useState/useEffect
- Gentle error messages per D-07
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui calendar component and date-fns</name>
  <files>package.json, src/components/ui/calendar.tsx</files>
  <action>
1. **Install date-fns and date-fns-tz** (if not already installed in Plan 03):
   ```bash
   npm install date-fns@4.1.0 date-fns-tz@3.2.0
   ```

2. **Install shadcn/ui calendar component:**
   ```bash
   npx shadcn-ui@latest add calendar
   ```

3. **Verify calendar component installed:**
   - src/components/ui/calendar.tsx should exist
   - @/components/ui/calendar can be imported

4. **Install additional shadcn/ui components if needed:**
   ```bash
   npx shadcn-ui@latest add dialog
   npx shadcn-ui@latest add select
   ```

Per RESEARCH.md UI Components section: Use shadcn/ui calendar for calendar view.
  </action>
  <verify>
[ -f src/components/ui/calendar.tsx ] && grep -q "date-fns" package.json
  </verify>
  <done>shadcn/ui calendar component installed with date-fns dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create RoomCard component with room details and action buttons</name>
  <files>src/components/rooms/RoomCard.tsx</files>
  <action>
Create src/components/rooms/RoomCard.tsx with:

1. **Props interface:**
   - interface RoomCardProps { room: IRoom, userTimezone: string }

2. **Component structure:**
   - Card container with shadcn/ui Card component
   - CardHeader: room title (e.g., "Focus Room")
   - CardContent:
     - Time: displayTime (from API response)
     - Capacity: "X / 12 participants"
     - Status badge (scheduled, open, full, in-progress, completed)
     - Interest tags (if any)
   - CardFooter:
     - RegisterButton component
     - JoinRoomButton component (if registered)

3. **Styling:**
   - Use Tailwind CSS for layout (flex, grid, spacing)
   - Status badge colors: green (open), yellow (scheduled), red (full), blue (in-progress)
   - Responsive design (mobile-friendly)

4. **Data display:**
   - Show room.scheduledTime converted to user timezone
   - Show room.participantCount / room.capacity
   - Show room.status badge
   - Show registrationStatus.message from API

5. **Error handling:**
   - Handle missing data gracefully
   - Show loading state while fetching

Use shadcn/ui Card component for consistent styling.
  </action>
  <verify>
npm test -- tests/components/rooms/RoomCard.test.tsx -t "should render room details"
  </verify>
  <done>RoomCard component displays room details with action buttons</done>
</task>

<task type="auto">
  <name>Task 3: Create RegisterButton component with state-based UI</name>
  <files>src/components/rooms/RegisterButton.tsx</files>
  <action>
Create src/components/rooms/RegisterButton.tsx with:

1. **Props interface:**
   - interface RegisterButtonProps { roomId: string, registrationStatus: RegistrationStatus, onRegister?: () => void, onCancel?: () => void }
   - interface RegistrationStatus { status: string, canRegister: boolean, message: string }

2. **Component states:**
   - status: 'closed' - show disabled button with "Registration opens in X time"
   - status: 'opening-soon' - show countdown or "Opening soon"
   - status: 'open' - show enabled "Register" button
   - status: 'registered' - show "Registered ✓" (disabled)
   - status: 'full' - show "Room Full" (disabled, red)

3. **Button actions:**
   - Click "Register" → POST to /api/rooms/[id]/register
   - Show loading spinner while registering
   - Show success toast or error message (gentle per D-07)
   - Refresh room list after successful registration
   - Click "Cancel Registration" → DELETE to /api/rooms/[id]/register
   - Confirm cancellation with dialog (optional)

4. **Implementation:**
   - useSWR or useState for room data
   - fetch() for API calls
   - Error handling with gentle messages
   - Loading state with disabled button

5. **Styling:**
   - Primary button for "Register"
   - Outline button for "Cancel Registration"
   - Disabled states for closed/full/registered
   - Green checkmark for "Registered ✓"

Follow Phase 1 gentle error message pattern.
  </action>
  <verify>
npm test -- tests/components/rooms/RegisterButton.test.tsx -t "should show register button when open"
npm test -- tests/components/rooms/RegisterButton.test.tsx -t "should handle registration click"
  </verify>
  <done>RegisterButton with state-based UI and API integration</done>
</task>

<task type="auto">
  <name>Task 4: Create JoinRoomButton component for one-click access</name>
  <files>src/components/rooms/JoinRoomButton.tsx</files>
  <action>
Create src/components/rooms/JoinRoomButton.tsx with:

1. **Props interface:**
   - interface JoinRoomButtonProps { roomId: string, isRegistered: boolean }

2. **Component logic:**
   - If not registered: show disabled button or hide completely
   - If registered: show enabled "Join Room" button
   - Click → Next.js Link to /room/[roomId]
   - Button should be prominent (primary style)

3. **Implementation:**
   - import Link from 'next/link'
   - Button as child of Link: <Link href={`/room/${roomId}`}><Button>Join Room</Button></Link>
   - Or use router.push() for programmatic navigation

4. **Styling:**
   - Primary button (green or brand color)
   - Large size for prominence
   - Icon (arrow right or play icon)
   - Disabled state when not registered

5. **Accessibility:**
   - aria-label for screen readers
   - Keyboard navigation support
   - Clear focus states

Per requirement ROOM-05: User can join registered room via one-click access.
  </action>
  <verify>
npm test -- tests/components/rooms/JoinRoomButton.test.tsx -t "should link to room page"
  </verify>
  <done>JoinRoomButton with one-click navigation to /room/[id]</done>
</task>

<task type="auto">
  <name>Task 5: Create RoomList component with room cards grid</name>
  <files>src/components/rooms/RoomList.tsx</files>
  <action>
Create src/components/rooms/RoomList.tsx with:

1. **Props interface:**
   - interface RoomListProps { rooms: IRoom[], userTimezone: string }

2. **Component structure:**
   - Grid layout (1 col mobile, 2 col tablet, 3 col desktop)
   - Map rooms to RoomCard components
   - Show empty state if no rooms
   - Show loading state while fetching

3. **Layout:**
   - Section header: "Today's Focus Rooms"
   - Date display: "Monday, April 7, 2026"
   - Grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4
   - Each room: <RoomCard key={room._id} room={room} userTimezone={userTimezone} />

4. **Empty state:**
   - "No rooms scheduled for today"
   - Suggest checking another day or contact support

5. **Loading state:**
   - Skeleton cards or spinner
   - "Loading rooms..."

6. **Error state:**
   - Gentle error message: "Couldn't load rooms. Please try again."
   - Retry button

Use Tailwind CSS grid for responsive layout.
  </action>
  <verify>
npm test -- tests/components/rooms/RoomList.test.tsx -t "should render room cards"
  </verify>
  <done>RoomList with responsive grid and empty/loading states</done>
</task>

<task type="auto">
  <name>Task 6: Create RoomCalendar component with date selection</name>
  <files>src/components/rooms/RoomCalendar.tsx</files>
  <action>
Create src/components/rooms/RoomCalendar.tsx with:

1. **Props interface:**
   - interface RoomCalendarProps { rooms: IRoom[], selectedDate: Date, onDateSelect: (date: Date) => void }

2. **Component logic:**
   - Import Calendar from @/components/ui/calendar
   - Extract dates that have rooms: rooms.map(r => new Date(r.scheduledTime))
   - Pass as modifiers to Calendar component
   - Highlight dates with rooms using modifiersStyles

3. **Styling:**
   - Highlight days with rooms: bold text, background color
   - Selected date: primary color
   - Hover effects for interactivity

4. **Implementation:**
   ```typescript
   const modifiers = {
     hasRoom: rooms.map(room => new Date(room.scheduledTime))
   }
   const modifiersStyles = {
     hasRoom: {
       fontWeight: 'bold',
       backgroundColor: 'hsl(var(--primary))',
       color: 'hsl(var(--primary-foreground))'
     }
   }
   ```

5. **Date selection:**
   - When user clicks date, call onDateSelect(date)
   - Parent component filters rooms by selected date
   - Update URL query param (optional): ?date=2026-04-07

Follow RESEARCH.md shadcn/ui calendar code example.
  </action>
  <verify>
npm test -- tests/components/rooms/RoomCalendar.test.tsx -t "should highlight dates with rooms"
  </verify>
  <done>RoomCalendar with date highlighting and selection</done>
</task>

<task type="auto">
  <name>Task 7: Create main rooms page with view toggle</name>
  <files>src/app/rooms/page.tsx</files>
  <action>
Create src/app/rooms/page.tsx with:

1. **Page structure:**
   - Page title: "Focus Rooms"
   - View toggle: Tab switcher (List / Calendar)
   - Content: RoomList or RoomCalendar + filtered RoomList
   - Metadata for SEO

2. **State management:**
   - viewState: 'list' | 'calendar' (useState)
   - selectedDate: Date (useState, default today)
   - rooms: IRoom[] (useSWR from /api/rooms)
   - userTimezone: string (from session or user data)

3. **Data fetching:**
   - useSWR('/api/rooms', fetcher) for rooms
   - Show loading state while fetching
   - Show error state if fetch fails

4. **View toggle:**
   - Tabs component (shadcn/ui) or custom buttons
   - List view: Show RoomList with all today's rooms
   - Calendar view: Show RoomCalendar + filtered RoomList for selected date

5. **Timezone handling:**
   - Get user timezone from session
   - Pass to RoomCard for displayTime

6. **Authentication:**
   - Require authentication (middleware or page-level check)
   - Redirect to /login if not authenticated

7. **Layout:**
   - Responsive container with max-width
   - Sticky header for page title and view toggle
   - Scrollable content area

Per requirement ROOM-02: Toggle between calendar view and list view.
  </action>
  <verify>
npm test -- tests/components/rooms/RoomList.test.tsx -t "should toggle between views"
  </verify>
  <done>Rooms page with view toggle, data fetching, and responsive layout</done>
</task>

<task type="auto">
  <name>Task 8: Create comprehensive component tests</name>
  <files>tests/components/rooms/RoomList.test.tsx, tests/components/rooms/RoomCalendar.test.tsx, tests/components/rooms/RoomCard.test.tsx, tests/components/rooms/RegisterButton.test.tsx</files>
  <action>
Create test suites for all components:

**tests/components/rooms/RoomCard.test.tsx:**
- describe('RoomCard')
- Test: renders room title and time
- Test: displays participant count
- Test: shows status badge
- Test: renders RegisterButton
- Test: renders JoinRoomButton when registered
- Test: handles missing data gracefully

**tests/components/rooms/RegisterButton.test.tsx:**
- describe('RegisterButton')
- Test: shows "Register" when status is open
- Test: shows "Registered ✓" when status is registered
- Test: shows "Room Full" when status is full
- Test: shows disabled button when status is closed
- Test: calls onRegister when "Register" clicked
- Test: calls onCancel when "Cancel" clicked
- Test: shows loading state while registering

**tests/components/rooms/JoinRoomButton.test.tsx:**
- describe('JoinRoomButton')
- Test: renders Link to /room/[id]
- Test: is disabled when not registered
- Test: is enabled when registered

**tests/components/rooms/RoomList.test.tsx:**
- describe('RoomList')
- Test: renders room cards for each room
- Test: shows empty state when no rooms
- Test: shows loading state while fetching
- Test: uses responsive grid layout

**tests/components/rooms/RoomCalendar.test.tsx:**
- describe('RoomCalendar')
- Test: renders calendar component
- Test: highlights dates with rooms
- Test: calls onDateSelect when date clicked
- Test: filters rooms by selected date

Use React Testing Library patterns from Phase 1 tests.
  </action>
  <verify>
npm test -- tests/components/rooms/ -- --run
  </verify>
  <done>All component tests passing (20+ tests covering UI, interactions, and states)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted client input, server must validate |
| UI state → API | Client-side state can be manipulated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27 | Tampering | Registration request | mitigate | Server validates 30-min window and capacity. API validates, not UI. |
| T-02-28 | Spoofing | Room access | mitigate | Registration checked server-side. UI button state is display-only hint. |
| T-02-29 | Information Disclosure | Room details | accept | Room times and participant counts are public within authenticated app. |
| T-02-30 | Denial of Service | Excessive API calls | accept | No rate limiting in Phase 2. Authenticated users only. Add in Phase 7. |
| T-02-31 | Tampering | View state manipulation | accept | View state is client-side preference. No security impact. |
</threat_model>

<verification>
1. RoomCard displays room details correctly
2. RegisterButton shows correct state based on registrationStatus
3. RegisterButton calls API on click
4. JoinRoomButton navigates to /room/[id]
5. RoomList renders room cards in responsive grid
6. RoomCalendar highlights dates with rooms
7. Rooms page toggles between list and calendar views
8. All components handle loading and error states
9. All components use gentle error messages
10. All tests passing (npm test -- tests/components/rooms/)
</verification>

<success_criteria>
1. RoomCard component displays all room details
2. RegisterButton shows 5 states (closed, opening-soon, open, registered, full)
3. JoinRoomButton provides one-click navigation
4. RoomList uses responsive grid layout
5. RoomCalendar highlights dates with rooms
6. Rooms page has view toggle (list/calendar)
7. All components handle loading/error states
8. All components use gentle error messages
9. Component tests cover UI, interactions, and edge cases
10. All tests passing (20+ tests)
11. UI ready for admin panel integration (Plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-management/02-05-SUMMARY.md`
</output>

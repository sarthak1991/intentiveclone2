---
phase: 02-room-management
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-03
files_modified:
  - src/app/api/rooms/route.ts
  - src/app/api/rooms/[id]/route.ts
  - src/app/api/rooms/[id]/register/route.ts
  - src/app/api/admin/rooms/route.ts
  - src/app/api/admin/rooms/[id]/noshow/route.ts
  - tests/api/rooms/index.test.ts
  - tests/api/rooms/[id]/route.test.ts
  - tests/api/rooms/[id]/register.test.ts
  - tests/api/admin/rooms/create.test.ts
  - tests/api/admin/rooms/[id]/noshow.test.ts
autonomous: true
requirements:
  - ROOM-01
  - ROOM-03
  - ROOM-05
  - ADMN-01
  - ADMN-06
user_setup: []
must_haves:
  truths:
    - GET /api/rooms returns list of scheduled rooms with user's timezone
    - POST /api/rooms/[id]/register registers user for room (30-min window, capacity check)
    - DELETE /api/rooms/[id]/register cancels user's registration
    - GET /api/rooms/[id] returns room details with participant list
    - POST /api/admin/rooms creates new room (admin only)
    - POST /api/admin/rooms/[id]/noshow reassigns no-show slot (admin only)
  artifacts:
    - path: src/app/api/rooms/route.ts
      provides: GET endpoint for room listing
      min_lines: 40
      contains: GET handler, getTodaysRooms(), timezone conversion
    - path: src/app/api/rooms/[id]/route.ts
      provides: GET room details, PATCH update (admin), DELETE (admin)
      min_lines: 60
      contains: GET/PATCH/DELETE handlers, populate participants
    - path: src/app/api/rooms/[id]/register/route.ts
      provides: POST register, DELETE cancel registration
      min_lines: 80
      contains: POST with atomic operations, DELETE cancel, 30-min window check
    - path: src/app/api/admin/rooms/route.ts
      provides: POST create room (admin only)
      min_lines: 50
      contains: requireAdmin(), Room.create()
    - path: src/app/api/admin/rooms/[id]/noshow/route.ts
      provides: POST reassign no-show (admin only)
      min_lines: 40
      contains: requireAdmin(), no-show handling, waitlist promotion
  key_links:
    - from: API routes
      to: Business logic
      via: import from src/lib/rooms.ts
      pattern: import.*from '@/lib/rooms'
    - from: API routes
      to: Admin auth
      via: requireAdmin() from src/lib/admin.ts
      pattern: await requireAdmin\(req\)
    - from: API routes
      to: Models
      via: Room and Registration imports
      pattern: import.*Room.*from '@/models'
    - from: /api/rooms/[id]/register
      to: Socket.IO
      via: emit registration event (future Phase 3)
      pattern: io\.to.*emit.*'user-registered'
---

<objective>
Create REST API endpoints for room listing, room details, user registration/cancellation, admin room creation, and no-show management to support both user-facing room browsing and admin room management features.

Purpose: Expose business logic via HTTP API for frontend integration. These endpoints power the room listing UI, registration flow, and admin panel.

Output: Working API routes with authentication, authorization, validation, and comprehensive test coverage.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-room-management/02-RESEARCH.md
@src/lib/rooms.ts
@src/lib/admin.ts
@src/lib/timezone.ts
@src/lib/auth.ts
@src/models/Room.ts
@src/models/Registration.ts

# Business logic available (from Wave 2, Plan 03)
- getTodaysRooms(): Promise<IRoom[]>
- registerForRoom(roomId, userId): Promise<IRoom>
- cancelRegistration(roomId, userId): Promise<IRoom>
- requireAdmin(request): Promise<{authorized, user, error}>

# Auth pattern from Phase 1
From src/app/api/auth/[...nextauth]/route.ts:
- export const { handlers: { GET, POST }, auth } = NextAuth(...)

# API route pattern from Phase 1
- Use async function GET/POST/PATCH/DELETE(req: NextRequest)
- Return NextResponse.json(data, { status })
- Use await auth() to get session
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create room listing API (GET /api/rooms)</name>
  <files>src/app/api/rooms/route.ts</files>
  <action>
Create src/app/api/rooms/route.ts with:

1. **GET /api/rooms:**
   - Export async function GET(req: NextRequest)
   - const session = await auth()
   - if (!session?.user?.id) return 401
   - await connectDB()
   - const rooms = await getTodaysRooms() (from src/lib/rooms.ts)
   - Get user timezone: const user = await User.findById(session.user.id)
   - Map rooms to add displayTime: formatRoomTime(room.scheduledTime, user.timezone)
   - Add registrationStatus for each room: getRegistrationStatus(room, user)
   - Return JSON with rooms array

2. **Response shape:**
   ```json
   {
     "rooms": [{
       "_id": "...",
       "title": "Focus Room",
       "scheduledTime": "2026-04-07T09:00:00Z",
       "displayTime": "9:00 AM",
       "capacity": 12,
       "participantCount": 5,
       "status": "open",
       "registrationStatus": {
         "status": "open",
         "canRegister": true,
         "message": "Register now"
       }
     }]
   }
   ```

3. **Error handling:**
   - 401 if not authenticated
   - 500 if database error
  </action>
  <verify>
npm test -- tests/api/rooms/index.test.ts -t "should return today's rooms"
  </verify>
  <done>GET /api/rooms returns today's rooms with user timezone and registration status</done>
</task>

<task type="auto">
  <name>Task 2: Create room details API (GET/PATCH/DELETE /api/rooms/[id])</name>
  <files>src/app/api/rooms/[id]/route.ts</files>
  <action>
Create src/app/api/rooms/[id]/route.ts with:

1. **GET /api/rooms/[id]:**
   - const session = await auth()
   - if (!session?.user?.id) return 401
   - await connectDB()
   - const room = await Room.findById(params.id).populate('participants')
   - if (!room) return 404
   - Add displayTime and registrationStatus (same as list endpoint)
   - Return room JSON with populated participants

2. **PATCH /api/rooms/[id] (admin only):**
   - const authCheck = await requireAdmin(req)
   - if (!authCheck.authorized) return 403
   - Parse JSON body: { title?, scheduledTime?, capacity?, interestTags?, status? }
   - Validate with zod schema
   - Update room with Room.findByIdAndUpdate(params.id, body, { new: true, runValidators: true })
   - Return updated room

3. **DELETE /api/rooms/[id] (admin only):**
   - const authCheck = await requireAdmin(req)
   - if (!authCheck.authorized) return 403
   - Update room.status = 'cancelled'
   - Cancel all related registrations (status = 'cancelled')
   - Return success message

4. **Error handling:**
   - 401 for unauthenticated
   - 403 for non-admin (PATCH/DELETE)
   - 404 for room not found
   - 400 for validation errors
  </action>
  <verify>
npm test -- tests/api/rooms/[id]/route.test.ts -t "should return room details"
npm test -- tests/api/rooms/[id]/route.test.ts -t "should update room as admin"
  </verify>
  <done>GET/PATCH/DELETE /api/rooms/[id] endpoints with admin authorization</done>
</task>

<task type="auto">
  <name>Task 3: Create registration API (POST/DELETE /api/rooms/[id]/register)</name>
  <files>src/app/api/rooms/[id]/register/route.ts</files>
  <action>
Create src/app/api/rooms/[id]/register/route.ts with:

1. **POST /api/rooms/[id]/register:**
   - const session = await auth()
   - if (!session?.user?.id) return 401
   - await connectDB()
   - const roomId = params.id, userId = session.user.id
   - Check registration window: const room = await Room.findById(roomId)
   - if (!isRegistrationOpen(room)) return 400 with message
   - Call registerForRoom(roomId, userId) from src/lib/rooms.ts (atomic operation)
   - Create Registration document: { userId, roomId, status: 'registered', registeredAt: new Date() }
   - Return 201 with updated room

2. **DELETE /api/rooms/[id]/register:**
   - const session = await auth()
   - if (!session?.user?.id) return 401
   - const roomId = params.id, userId = session.user.id
   - Call cancelRegistration(roomId, userId) from src/lib/rooms.ts
   - Update Registration.status = 'cancelled'
   - Return 200 with success message

3. **Error cases:**
   - 401 if not authenticated
   - 404 if room not found
   - 400 if registration window not open
   - 400 if room full (caught by atomic operation)
   - 400 if already registered (unique index)

4. **Success responses:**
   - POST 201: { success: true, room: {...}, message: "Registered successfully" }
   - DELETE 200: { success: true, message: "Registration cancelled" }

Follow RESEARCH.md registration API pattern with atomic operations.
  </action>
  <verify>
npm test -- tests/api/rooms/[id]/register.test.ts -t "should register for room"
npm test -- tests/api/rooms/[id]/register.test.ts -t "should enforce 30-minute window"
  </verify>
  <done>POST/DELETE /api/rooms/[id]/register with 30-min window and atomic operations</done>
</task>

<task type="auto">
  <name>Task 4: Create admin room creation API (POST /api/admin/rooms)</name>
  <files>src/app/api/admin/rooms/route.ts</files>
  <action>
Create src/app/api/admin/rooms/route.ts with:

1. **POST /api/admin/rooms (admin only):**
   - const authCheck = await requireAdmin(req)
   - if (!authCheck.authorized) return 403
   - Parse JSON body: { title?, scheduledTime, duration?, capacity?, interestTags? }
   - Validate with zod schema:
     - scheduledTime: required ISO date string
     - duration: optional number (default 45)
     - capacity: optional number 1-12 (default 12)
     - interestTags: optional string[]
   - Convert scheduledTime to Date object
   - Create room: await Room.create({ ...body, status: 'scheduled' })
   - Return 201 with created room

2. **Zod validation schema:**
   ```typescript
   const CreateRoomSchema = z.object({
     title: z.string().min(1).max(100).optional(),
     scheduledTime: z.string().datetime().refine(val => !isNaN(Date.parse(val))),
     duration: z.number().min(15).max(120).optional(),
     capacity: z.number().min(1).max(12).optional(),
     interestTags: z.array(z.string().trim()).optional()
   })
   ```

3. **Error handling:**
   - 401 if not authenticated
   - 403 if not admin
   - 400 for validation errors
   - 500 for database errors

4. **Response shape:**
   ```json
   {
     "success": true,
     "room": { "_id": "...", "title": "...", "scheduledTime": "...", ... }
   }
   ```
  </action>
  <verify>
npm test -- tests/api/admin/rooms/create.test.ts -t "should create room as admin"
npm test -- tests/api/admin/rooms/create.test.ts -t "should reject non-admin user"
  </verify>
  <done>POST /api/admin/rooms creates room with admin authorization and validation</done>
</task>

<task type="auto">
  <name>Task 5: Create admin no-show management API (POST /api/admin/rooms/[id]/noshow)</name>
  <files>src/app/api/admin/rooms/[id]/noshow/route.ts</files>
  <action>
Create src/app/api/admin/rooms/[id]/noshow/route.ts with:

1. **POST /api/admin/rooms/[id]/noshow (admin only):**
   - const authCheck = await requireAdmin(req)
   - if (!authCheck.authorized) return 403
   - Parse body: { userId: string, remarks?: string }
   - await connectDB()
   - Find registration: await Registration.findOne({ userId, roomId: params.id })
   - If not found, return 404
   - Update registration.status = 'no-show', remarks = body.remarks
   - Remove user from room participants (atomic operation)
   - Check if waitlist has users: room.waitlist.length > 0
   - If yes, promote first waitlist user to participant
   - Send notification to promoted user (future Phase 6)
   - Return 200 with updated room

2. **Waitlist promotion logic:**
   - const firstWaiting = room.waitlist[0]
   - Add to participants: room.participants.push(firstWaiting.user)
   - Remove from waitlist: room.waitlist.shift()
   - Create registration for promoted user with status = 'registered'
   - Update room

3. **Error handling:**
   - 403 if not admin
   - 404 if registration not found
   - 400 if user not registered for this room

4. **Response shape:**
   ```json
   {
     "success": true,
     "message": "No-show recorded, slot reassigned",
     "room": { ... },
     "promotedUser": { "id": "...", "name": "..." }
   }
   ```

Per requirement ADMN-06: Admin can manage no-show reassignments.
  </action>
  <verify>
npm test -- tests/api/admin/rooms/[id]/noshow.test.ts -t "should record no-show and reassign"
  </verify>
  <done>POST /api/admin/rooms/[id]/noshow records no-show and promotes waitlist</done>
</task>

<task type="auto">
  <name>Task 6: Create comprehensive API route tests</name>
  <files>tests/api/rooms/index.test.ts, tests/api/rooms/[id]/route.test.ts, tests/api/rooms/[id]/register.test.ts, tests/api/admin/rooms/create.test.ts, tests/api/admin/rooms/[id]/noshow.test.ts</files>
  <action>
Create test suites for all API routes:

**tests/api/rooms/index.test.ts:**
- describe('GET /api/rooms')
- Test: returns 401 for unauthenticated request
- Test: returns today's 8 rooms for authenticated user
- Test: rooms include displayTime in user timezone
- Test: rooms include registrationStatus for each room
- Test: handles database errors gracefully

**tests/api/rooms/[id]/route.test.ts:**
- describe('GET /api/rooms/[id]')
- Test: returns 401 for unauthenticated
- Test: returns 404 for non-existent room
- Test: returns room details with populated participants
- describe('PATCH /api/rooms/[id]')
- Test: returns 403 for non-admin user
- Test: updates room as admin
- Test: validates input with zod schema
- describe('DELETE /api/rooms/[id]')
- Test: returns 403 for non-admin user
- Test: cancels room as admin

**tests/api/rooms/[id]/register.test.ts:**
- describe('POST /api/rooms/[id]/register')
- Test: returns 401 for unauthenticated
- Test: returns 400 if registration window not open
- Test: returns 400 if room full
- Test: registers user successfully
- Test: creates Registration document
- describe('DELETE /api/rooms/[id]/register')
- Test: cancels registration successfully
- Test: removes user from room participants

**tests/api/admin/rooms/create.test.ts:**
- describe('POST /api/admin/rooms')
- Test: returns 403 for non-admin user
- Test: creates room as admin
- Test: validates input with zod schema
- Test: sets default values (duration, capacity)

**tests/api/admin/rooms/[id]/noshow.test.ts:**
- describe('POST /api/admin/rooms/[id]/noshow')
- Test: returns 403 for non-admin user
- Test: records no-show and removes participant
- Test: promotes first waitlist user to participant
- Test: returns 404 if registration not found

Use fetch() for API tests, mock auth sessions with test users.
  </action>
  <verify>
npm test -- tests/api/ -- --run
  </verify>
  <done>All API route tests passing (25+ tests covering all endpoints and error cases)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API routes | Unauthenticated or untrusted requests |
| Admin API → Admin helpers | Non-admin users attempting admin operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Spoofing | API authentication | mitigate | await auth() on all routes. Reject unauthenticated requests with 401. |
| T-02-21 | Elevation of Privilege | Admin operations | mitigate | requireAdmin() on all admin routes. Server-side role check. |
| T-02-22 | Tampering | Room creation input | mitigate | Zod schema validation. Allow only defined fields (no mass assignment). |
| T-02-23 | Tampering | Registration input | mitigate | Atomic operations in business logic. API passes data to validated functions. |
| T-02-24 | Information Disclosure | Room details | mitigate | Populate participants but User model has select: false on password. |
| T-02-25 | Denial of Service | Room listing | accept | No rate limiting in Phase 2. Add in Phase 7 if needed. |
| T-02-26 | Tampering | No-show marking | mitigate | Only admins can mark no-show. Server-side check prevents user manipulation. |
</threat_model>

<verification>
1. GET /api/rooms returns today's rooms with timezone and status
2. GET /api/rooms/[id] returns room details with participants
3. POST /api/rooms/[id]/register registers user (30-min window)
4. DELETE /api/rooms/[id]/register cancels registration
5. PATCH /api/rooms/[id] updates room (admin only)
6. DELETE /api/rooms/[id] cancels room (admin only)
7. POST /api/admin/rooms creates room (admin only)
8. POST /api/admin/rooms/[id]/noshow manages no-shows (admin only)
9. All routes use auth() or requireAdmin()
10. All inputs validated with zod schemas
11. All tests passing (npm test -- tests/api/)
</verification>

<success_criteria>
1. All 8 API routes created and functional
2. Authentication required on all routes
3. Admin authorization enforced on admin routes
4. Registration window enforced (30 min before session)
5. Atomic operations prevent race conditions
6. Input validation with zod schemas
7. Error handling covers all cases (401, 403, 404, 400, 500)
8. Test coverage includes auth, authz, validation, and error cases
9. All tests passing (25+ tests)
10. API ready for frontend integration (Wave 3)
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-management/02-04-SUMMARY.md`
</output>

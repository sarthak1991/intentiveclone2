---
phase: 02-room-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/Room.ts
  - src/models/Registration.ts
  - src/models/InterestTag.ts
  - src/models/types.ts
  - src/models/index.ts
  - tests/models/Room.test.ts
  - tests/models/Registration.test.ts
  - tests/models/InterestTag.test.ts
autonomous: true
requirements:
  - ROOM-01
  - ROOM-03
  - ROOM-05
  - ADMN-01
  - ADMN-06
  - ADMN-08
user_setup: []
must_haves:
  truths:
    - Room model can store 8 daily scheduled rooms with time, capacity, and participant tracking
    - Registration model can track user-room registrations with status and timestamps
    - InterestTag model can manage room interest categories
    - All models have proper indexes for efficient queries
    - Models enforce data integrity via Mongoose validation
  artifacts:
    - path: src/models/Room.ts
      provides: Room schema with scheduling, capacity, participants, waitlist, status
      min_lines: 80
      contains: RoomSchema, scheduledTime, capacity, status enum, participants array
    - path: src/models/Registration.ts
      provides: User-room registration tracking with status transitions
      min_lines: 40
      contains: RegistrationSchema, userId, roomId, status enum
    - path: src/models/InterestTag.ts
      provides: Interest tag categories for room matching
      min_lines: 30
      contains: InterestTagSchema, name, isActive
    - path: tests/models/Room.test.ts
      provides: Room model validation and constraints tests
      exports: test suite
  key_links:
    - from: Room model
      to: User model (from Phase 1)
      via: ref: 'User' in participants array
      pattern: participants:\s*\[\s*{\s*type:\s*Schema\.Types\.ObjectId,\s*ref:\s*'User'
    - from: Registration model
      to: Room and User models
      via: ref: 'Room', ref: 'User'
      pattern: userId:\s*{\s*type:\s*Schema\.Types\.ObjectId,\s*ref:\s*'User'
    - from: Room model
      to: Registration model
      via: virtual field or query lookup
      pattern: registrations.*populate.*userId
---

<objective>
Define MongoDB schemas for Room, Registration, and InterestTag models with proper indexing, validation, and TypeScript interfaces to support room scheduling, user registration, and admin management features.

Purpose: Create the data foundation for room management - all room operations (scheduling, registration, admin) depend on these schemas.

Output: Three Mongoose models (Room, Registration, InterestTag) with TypeScript interfaces, compound indexes for performance, and comprehensive test coverage.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-room-management/02-RESEARCH.md
@src/models/User.ts
@src/models/types.ts

# Interfaces from existing models

From src/models/types.ts (Phase 1):
```typescript
export interface IUser {
  _id: mongoose.Types.ObjectId
  email: string
  password?: string
  name: string
  photoId?: mongoose.Types.ObjectId
  photoUrl?: string
  timezone: string
  interests: string[]
  isOnboarded: boolean
  createdAt: Date
  updatedAt: Date
}
```

From src/models/User.ts (Phase 1):
```typescript
export const User = (mongoose.models.User as Model<IUser>) || mongoose.model<IUser>('User', UserSchema)
export function getPhotoBucket(): GridFSBucket
```

Key patterns from Phase 1:
- Mongoose 8.x schema definition with TypeScript interfaces
- Singleton connection pattern (src/lib/db.ts)
- GridFS bucket for file storage (already in User model)
- Test structure: tests/models/{Model}.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript interfaces for Room, Registration, and InterestTag models</name>
  <files>src/models/types.ts</files>
  <action>
Add TypeScript interfaces to src/models/types.ts:

1. **IRoom interface** with fields:
   - _id: ObjectId
   - title: string (default: 'Focus Room')
   - scheduledTime: Date (required, indexed)
   - duration: number (default: 45, in minutes)
   - capacity: number (default: 12, min: 1, max: 12)
   - status: enum ('scheduled', 'open', 'full', 'in-progress', 'completed', 'cancelled')
   - participants: ObjectId[] (ref: 'User')
   - waitlist: array of { user: ObjectId, joinedAt: Date }
   - interestTags: string[]
   - parentRoomId: ObjectId (ref: 'Room', optional - for overflow in Phase 4)
   - isOverflowRoom: boolean (default: false)
   - createdAt: Date
   - updatedAt: Date

2. **IRegistration interface** with fields:
   - _id: ObjectId
   - userId: ObjectId (ref: 'User')
   - roomId: ObjectId (ref: 'Room')
   - registeredAt: Date
   - status: enum ('registered', 'cancelled', 'no-show', 'attended')
   - attendedAt: Date (optional)
   - remarks: string (optional - for captain notes)

3. **IInterestTag interface** with fields:
   - _id: ObjectId
   - name: string (required, unique, trimmed)
   - description: string (optional)
   - isActive: boolean (default: true)
   - createdAt: Date
   - updatedAt: Date

DO NOT modify existing IUser interface.
  </action>
  <verify>
File exists with IRoom, IRegistration, IInterestTag interfaces. TypeScript compilation passes: npx tsc --noEmit
  </verify>
  <done>TypeScript interfaces exported, no type errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Room Mongoose schema with indexes and validation</name>
  <files>src/models/Room.ts</files>
  <action>
Create Room model in src/models/Room.ts:

1. Define RoomSchema using mongoose.Schema<IRoom>
2. Add all fields from IRoom interface with proper types
3. Set defaults: title='Focus Room', duration=45, capacity=12, status='scheduled'
4. Add validation: capacity min 1 max 12 (Phase 2 limit)
5. Add indexes:
   - Compound index: { scheduledTime: 1, status: 1 } for querying today's rooms
   - Single index on status for filtering
6. Add timestamps: true
7. Create and export Room model using Mongoose model caching pattern from User model
8. Export RoomSchema for potential middleware/plugins

Follow the exact pattern from src/models/User.ts (Mongoose 8.x, model caching).

Per RESEARCH.md Pattern 1: Store individual room documents (not recurring series).
  </action>
  <verify>
npm test -- tests/models/Room.test.ts -t "should create room with defaults"
  </verify>
  <done>Room model created with all fields, indexes configured, model exported</done>
</task>

<task type="auto">
  <name>Task 3: Create Registration and InterestTag Mongoose schemas</name>
  <files>src/models/Registration.ts, src/models/InterestTag.ts</files>
  <action>
Create two model files following the Room model pattern:

**src/models/Registration.ts:**
1. Define RegistrationSchema using mongoose.Schema<IRegistration>
2. Add compound unique index: { userId: 1, roomId: 1 } (prevent duplicate registrations)
3. Add index on status for filtering user's registrations
4. Add index on registeredAt for time-based queries
5. Set timestamps: true
6. Export Registration model with caching pattern

**src/models/InterestTag.ts:**
1. Define InterestTagSchema using mongoose.Schema<IInterestTag>
2. Add validation: name required, unique, trimmed
3. Add index on name for autocomplete lookups
4. Add index on isActive for filtering active tags
5. Set timestamps: true
6. Export InterestTag model with caching pattern

Both models should follow the same pattern as Room and User models.
  </action>
  <verify>
npm test -- tests/models/Registration.test.ts -t "should prevent duplicate registrations"
npm test -- tests/models/InterestTag.test.ts -t "should create unique interest tag"
  </verify>
  <done>Registration and InterestTag models created with proper indexes and validation</done>
</task>

<task type="auto">
  <name>Task 4: Update models barrel file and create comprehensive model tests</name>
  <files>src/models/index.ts, tests/models/Room.test.ts, tests/models/Registration.test.ts, tests/models/InterestTag.test.ts</files>
  <action>
1. **Update src/models/index.ts:**
   - Export IRoom, IRegistration, IInterestTag interfaces
   - Export Room, Registration, InterestTag models
   - Keep existing IUser and User exports

2. **Create tests/models/Room.test.ts:**
   - Test suite with describe('Room Model')
   - beforeEach: connect to test database, afterEach: cleanup
   - Test: should create room with default values
   - Test: should enforce capacity limits (min 1, max 12)
   - Test: should validate status enum values
   - Test: should add participant to room
   - Test: should prevent duplicate participants
   - Test: should add user to waitlist
   - Test: should query today's rooms using compound index
   - Use mongoose beforeAll/afterAll for connection management

3. **Create tests/models/Registration.test.ts:**
   - Test suite with describe('Registration Model')
   - Test: should create registration
   - Test: should enforce unique userId + roomId
   - Test: should validate status enum
   - Test: should update status to attended
   - Test: should populate userId and roomId

4. **Create tests/models/InterestTag.test.ts:**
   - Test suite with describe('InterestTag Model')
   - Test: should create interest tag
   - Test: should enforce unique names
   - Test: should trim tag names
   - Test: should filter active tags

Use the same test patterns from tests/models/User.test.ts (beforeEach cleanup, describe blocks, specific assertions).
  </action>
  <verify>
npm test -- tests/models/ -- --run
  </verify>
  <done>All model tests passing (18+ tests), barrel file updated with all exports</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Room model | Untrusted input crosses here during room creation/updates |
| API → Registration model | Untrusted registration requests, potential race conditions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Room.capacity | mitigate | Mongoose validation: min 1, max 12. Prevents overflow via API input. |
| T-02-02 | Tampering | Room.status | mitigate | Enum validation in schema. Only allows defined status values. |
| T-02-03 | Tampering | Registration.userId + roomId | mitigate | Compound unique index in schema prevents duplicate registrations. |
| T-02-04 | Information Disclosure | Room.participants | mitigate | Populate with User model, password field has select: false (from Phase 1). |
| T-02-05 | Denial of Service | Room.scheduledTime queries | mitigate | Compound index {scheduledTime: 1, status: 1} for efficient queries. |
| T-02-06 | Tampering | Registration.status | mitigate | Enum validation, status transitions controlled via API (not direct updates). |
| T-02-07 | Information Disclosure | InterestTag.name | accept | Tag names are public categories (e.g., "coding", "writing"). No PII. |
</threat_model>

<verification>
1. All three models created with proper TypeScript interfaces
2. Room model has compound index on scheduledTime + status
3. Registration model has unique constraint on userId + roomId
4. All models follow Mongoose 8.x pattern from Phase 1
5. Test coverage includes validation, constraints, and indexes
6. Models exported from barrel file (src/models/index.ts)
7. No conflicts with existing User model from Phase 1
</verification>

<success_criteria>
1. TypeScript interfaces (IRoom, IRegistration, IInterestTag) defined and exported
2. Mongoose schemas created with validation, defaults, and indexes
3. Room model enforces capacity limits (1-12) and status enum
4. Registration model prevents duplicate registrations via unique index
5. InterestTag model enforces unique, trimmed tag names
6. All models use same caching pattern as User model
7. Test suite validates all constraints, indexes, and relationships
8. All tests passing (npm test -- tests/models/)
9. No TypeScript compilation errors
10. Models ready for API route integration in Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-management/02-01-SUMMARY.md`
</output>

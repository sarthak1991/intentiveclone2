---
phase: 04-webrtc-integration
plan: 07
type: execute
wave: 4
depends_on: [04-06]
files_modified:
  - tests/unit/webrtc/use-media-stream.test.ts
  - tests/unit/webrtc/video-grid.test.tsx
  - tests/unit/webrtc/session-timer.test.tsx
  - tests/unit/webrtc/attendance-tracking.test.ts
  - tests/integration/webrtc/webrtc-connection.test.ts
  - tests/integration/webrtc/captain-controls.test.ts
  - tests/integration/webrtc/turn-connectivity.test.ts
  - tests/integration/webrtc/12-person-room.test.ts
  - tests/integration/webrtc/overflow-room.test.ts
  - tests/integration/webrtc/ice-config.test.ts
  - vitest.config.ts
  - tests/setup.ts
  - TESTING.md
  - .planning/phases/04-webrtc-integration/04-HANDOFF.md
autonomous: true
requirements:
  - VIDE-01
  - VIDE-02
  - VIDE-03
  - VIDE-04
  - VIDE-05
  - VIDE-06
  - ROOM-04
  - ROOM-06
  - ROOM-07
  - ROOM-08
  - TECH-05
  - TECH-07
must_haves:
  truths:
    - All WebRTC unit tests pass (useMediaStream, video grid, timer, attendance)
    - All WebRTC integration tests pass (connection, captain controls, TURN, 12-person room, overflow room, ICE config)
    - Test coverage >80% for WebRTC code
    - ICE configuration tested with multiple STUN/TURN servers
    - TURN connectivity tested with actual coturn server (or mocked)
    - 12-person room tested for capacity enforcement
    - Overflow room tested for auto-creation and chat sharing
    - Testing guide updated with WebRTC test instructions
    - Phase 4 handoff document created with implementation notes
  artifacts:
    - path: tests/unit/webrtc/use-media-stream.test.ts
      provides: Unit tests for useMediaStream hook
      min_lines: 80
    - path: tests/unit/webrtc/video-grid.test.tsx
      provides: Unit tests for VideoGrid component
      min_lines: 100
    - path: tests/integration/webrtc/webrtc-connection.test.ts
      provides: Integration tests for WebRTC connection flow
      min_lines: 150
    - path: tests/integration/webrtc/12-person-room.test.ts
      provides: Integration tests for 12-person room capacity
      min_lines: 120
    - path: TESTING.md
      provides: Testing guide with WebRTC test instructions
      min_lines: 200
    - path: .planning/phases/04-webrtc-integration/04-HANDOFF.md
      provides: Phase 4 implementation notes for next phase
      min_lines: 300
  key_links:
    - from: tests/integration/webrtc/webrtc-connection.test.ts
      to: server/socket-server.ts
      via: WebRTC signaling events
      pattern: "get-router-rtp-capabilities|create-transport"
    - from: tests/integration/webrtc/turn-connectivity.test.ts
      to: server/webrtc-server.ts
      via: TURN credential generation
      pattern: "generateTurnCredentials"
</key_links>
---

<objective>
Create comprehensive WebRTC integration tests, ICE configuration testing, and Phase 4 documentation.

Purpose: Ensure WebRTC implementation is reliable and production-ready with comprehensive test coverage. Test ICE configuration with multiple STUN/TURN servers. Test 12-person room capacity enforcement. Test overflow room auto-creation. Test attendance tracking. Create testing guide for WebRTC features. Document implementation notes for Phase 5 (Focus Session Features).

Output: Complete test suite for WebRTC functionality (>80% coverage), ICE configuration tests, TURN connectivity tests, 12-person room tests, overflow room tests, updated testing guide, and Phase 4 handoff document.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webrtc-integration/04-CONTEXT.md
@.planning/phases/04-webrtc-integration/04-RESEARCH.md
@.planning/phases/04-webrtc-integration/04-06-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Existing Infrastructure
@tests/unit/ — Unit test structure from Phase 1
@tests/integration/ — Integration test structure from Phase 2
@tests/setup.ts — Test setup with Vitest and Testing Library
@TESTING.md — Testing guide from Phase 3
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WebRTC test mocks and fixtures</name>
  <files>tests/setup.ts</files>
  <action>
    Extend tests/setup.ts with WebRTC-specific mocks and fixtures:

    1. **Mock mediasoup**:
       ```typescript
       vi.mock('mediasoup', () => ({
         createWorker: vi.fn(() => mockWorker)
       }))
       ```
       - Mock worker.createRouter()
       - Mock router.createWebRtcTransport()
       - Mock transport.produce()
       - Mock transport.consume()

    2. **Mock mediasoup-client**:
       ```typescript
       vi.mock('mediasoup-client', () => ({
         Device: vi.fn(() => mockDevice)
       }))
       ```
       - Mock device.load()
       - Mock device.createSendTransport()
       - Mock device.createRecvTransport()

    3. **Mock getUserMedia**:
       ```typescript
       global.navigator.mediaDevices = {
         getUserMedia: vi.fn(() => Promise.resolve(mockMediaStream))
       }
       ```

    4. **Mock Socket.IO**:
       - Mock socket.emit()
       - Mock socket.on()
       - Track emitted events for assertions

    5. **Fixtures**:
       - mockRoom: Room document with capacity 12
       - mockUser: User document with _id, name, email
       - mockMediaStream: MediaStream with audio/video tracks
       - mockTransport: WebRTC transport mock
       - mockProducer: Producer mock
       - mockConsumer: Consumer mock with track

    Use existing test setup structure from Phase 1-3 as template.

    **CLEANUP**: Clear all mocks after each test (vi.clearAllMocks()).
  </action>
  <verify>
    <automated>grep -E "(mediasoup|mediasoup-client|getUserMedia)" tests/setup.ts</automated>
  </verify>
  <done>tests/setup.ts includes WebRTC mocks and fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for useMediaStream</name>
  <files>tests/unit/webrtc/use-media-stream.test.ts</files>
  <action>
    Create tests/unit/webrtc/use-media-stream.test.ts:

    1. **Test getUserMedia success**:
       - Mock getUserMedia to return stream
       - Render hook with useMediaStream()
       - Assert stream is not null
       - Assert audioTrack and videoTrack refs are set

    2. **Test toggleAudio**:
       - Call toggleAudio()
       - Assert isMuted toggles true/false
       - Assert audioTrack.enabled toggles
       - Assert socket.emit called with 'toggle-audio'

    3. **Test toggleVideo**:
       - Call toggleVideo()
       - Assert isVideoOff toggles true/false
       - Assert videoTrack.enabled toggles
       - Assert socket.emit called with 'toggle-video'

    4. **Test getUserMedia error**:
       - Mock getUserMedia to reject (permission denied)
       - Render hook
       - Assert error toast shown
       - Assert stream is null

    5. **Test cleanup on unmount**:
       - Render hook
       - Unmount component
       - Assert track.stop() called for all tracks

    Use @testing-library/react-hooks for hook testing. Use vi.fn() for mocking.

    **COVERAGE**: Target >90% for useMediaStream hook.
  </action>
  <verify>
    <automated>npm test -- tests/unit/webrtc/use-media-stream.test.ts</automated>
  </verify>
  <done>tests/unit/webrtc/use-media-stream.test.ts covers all useMediaStream functionality</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for VideoGrid component</name>
  <files>tests/unit/webrtc/video-grid.test.tsx</files>
  <action>
    Create tests/unit/webrtc/video-grid.test.tsx:

    1. **Test grid layout with 1 participant**:
       - Mock roomStore with 1 participant
       - Render VideoGrid
       - Assert grid-cols-1 class applied

    2. **Test grid layout with 4 participants**:
       - Mock roomStore with 4 participants
       - Render VideoGrid
       - Assert grid-cols-2 class applied

    3. **Test grid layout with 9 participants**:
       - Mock roomStore with 9 participants
       - Render VideoGrid
       - Assert grid-cols-3 class applied

    4. **Test grid layout with 12 participants**:
       - Mock roomStore with 12 participants
       - Render VideoGrid
       - Assert grid-cols-3 class applied (3x4 grid)

    5. **Test empty state**:
       - Mock roomStore with 0 participants
       - Render VideoGrid
       - Assert "Waiting for others..." message shown

    6. **Test VideoCard rendering**:
       - Mock roomStore with participants
       - Render VideoGrid
       - Assert VideoCard rendered for each participant
       - Assert participant data passed correctly

    Use @testing-library/react for component testing. Mock roomStore with zustand mock store.

    **COVERAGE**: Target >85% for VideoGrid component.
  </action>
  <verify>
    <automated>npm test -- tests/unit/webrtc/video-grid.test.tsx</automated>
  </verify>
  <done>tests/unit/webrtc/video-grid.test.tsx covers grid layout variations</done>
</task>

<task type="auto">
  <name>Task 4: Create unit tests for SessionTimer</name>
  <files>tests/unit/webrtc/session-timer.test.tsx</files>
  <action>
    Create tests/unit/webrtc/session-timer.test.tsx:

    1. **Test countdown displays correctly**:
       - Render SessionTimer with startTime and durationMinutes=45
       - Fast-forward time by vi.useFakeTimers()
       - Assert timer displays "45:00 remaining" initially
       - Assert timer displays "44:59 remaining" after 1 second
       - Assert timer displays "0:00 remaining" after 45 minutes

    2. **Test formatTime function**:
       - Test edge cases (0 seconds, 59 seconds, 60 seconds)
       - Assert leading zeros for seconds <10

    3. **Test cleanup on unmount**:
       - Render SessionTimer
       - Unmount component
       - Assert interval cleared (useFakeTimers)

    4. **Test accent color per D-10**:
       - Render SessionTimer
       - Assert text-accent class applied
       - Assert no color change throughout countdown

    Use @testing-library/react for component testing. Use vi.useFakeTimers() for time manipulation.

    **COVERAGE**: Target >90% for SessionTimer component.
  </action>
  <verify>
    <automated>npm test -- tests/unit/webrtc/session-timer.test.tsx</automated>
  </verify>
  <done>tests/unit/webrtc/session-timer.test.tsx covers countdown timer logic</done>
</task>

<task type="auto">
  <name>Task 5: Create unit tests for attendance tracking</name>
  <files>tests/unit/webrtc/attendance-tracking.test.ts</files>
  <action>
    Create tests/unit/webrtc/attendance-tracking.test.ts:

    1. **Test attendance tracking starts on connect**:
       - Mock isConnected=false initially
       - Render useAttendanceTracking hook
       - Assert cumulativeTime = 0
       - Change isConnected=true
       - Fast-forward time by 30 seconds
       - Assert cumulativeTime = 30

    2. **Test attendance pauses on disconnect**:
       - Start with isConnected=true
       - Fast-forward 30 seconds
       - Change isConnected=false
       - Fast-forward 10 seconds
       - Assert cumulativeTime = 30 (not 40, time paused)

    3. **Test attendance resumes on reconnect**:
       - Disconnect after 30 seconds
       - Reconnect after 10 seconds
       - Fast-forward 60 seconds
       - Assert cumulativeTime = 90 (30 + 60)

    4. **Test hasAttended becomes true after 90 seconds**:
       - Start connected
       - Fast-forward 89 seconds
       - Assert hasAttended = false
       - Fast-forward 1 second
       - Assert hasAttended = true

    5. **Test attendance confirmation sent once**:
       - Wait for hasAttended = true
       - Assert 'attendance-confirmed' event emitted once
       - Re-render hook
       - Assert event not emitted again

    Use @testing-library/react-hooks for hook testing. Use vi.useFakeTimers() for time manipulation.

    **COVERAGE**: Target >90% for useAttendanceTracking hook.
  </action>
  <verify>
    <automated>npm test -- tests/unit/webrtc/attendance-tracking.test.ts</automated>
  </verify>
  <done>tests/unit/webrtc/attendance-tracking.test.ts covers 90-second threshold logic</done>
</task>

<task type="auto">
  <name>Task 6: Create integration tests for WebRTC connection</name>
  <files>tests/integration/webrtc/webrtc-connection.test.ts</files>
  <action>
    Create tests/integration/webrtc/webrtc-connection.test.ts:

    1. **Test complete WebRTC connection flow**:
       - Mock Socket.IO server with WebRTC handlers
       - Create mock mediasoup worker and router
       - Render useWebRTCConnection hook
       - Emit 'get-router-rtp-capabilities'
       - Assert device created with RTP capabilities
       - Emit 'create-transport'
       - Assert transport created
       - Emit 'connect-transport'
       - Assert transport connected
       - Emit 'produce'
       - Assert producers created (audio, video)

    2. **Test consumer creation**:
       - Emit 'new-producer' event from server
       - Assert createConsumer called
       - Assert consumer added to state
       - Assert consumer track attached to video element

    3. **Test transport cleanup on disconnect**:
       - Simulate socket disconnect
       - Assert all producers closed
       - Assert all consumers closed
       - Assert transport closed

    4. **Test ICE candidate gathering**:
       - Mock ICE candidates from server
       - Assert transport connects with ICE candidates
       - Assert DTLS handshake completes

    Use Vitest describe() blocks for test organization. Mock server responses.

    **COVERAGE**: Target >80% for WebRTC connection flow.
  </action>
  <verify>
    <automated>npm test -- tests/integration/webrtc/webrtc-connection.test.ts</automated>
  </verify>
  <done>tests/integration/webrtc/webrtc-connection.test.ts covers complete connection flow</done>
</task>

<task type="auto">
  <name>Task 7: Create integration tests for 12-person room</name>
  <files>tests/integration/webrtc/12-person-room.test.ts</files>
  <action>
    Create tests/integration/webrtc/12-person-room.test.ts:

    1. **Test room capacity enforced at 12 participants**:
       - Create room with capacity=12
       - Connect 12 participants via Socket.IO
       - Assert all 12 can connect
       - Attempt to connect 13th participant
       - Assert 'room-full' error emitted
       - Assert 13th participant rejected

    2. **Test participant count updates correctly**:
       - Connect 5 participants
       - Assert roomStore.participantCount = 5
       - Disconnect 2 participants
       - Assert roomStore.participantCount = 3

    3. **Test producers created for all participants**:
       - Connect 12 participants
       - Each participant creates audio/video producers
       - Assert 12 audio producers in room
       - Assert 12 video producers in room

    4. **Test consumers created for all remote participants**:
       - Connect 12 participants
       - Each participant consumes 11 remote streams
       - Assert 11 consumers per participant

    5. **Test performance with 12 participants**:
       - Connect 12 participants
       - Measure memory usage
       - Assert no memory leaks (transport cleanup)
       - Assert CPU usage reasonable (<80%)

    Use Vitest beforeEach() to set up fresh room for each test. Clean up connections in afterEach().

    **COVERAGE**: Target >80% for room capacity enforcement.
  </action>
  <verify>
    <automated>npm test -- tests/integration/webrtc/12-person-room.test.ts</automated>
  </verify>
  <done>tests/integration/webrtc/12-person-room.test.ts covers 12-person capacity enforcement</done>
</task>

<task type="auto">
  <name>Task 8: Create integration tests for overflow rooms</name>
  <files>tests/integration/webrtc/overflow-room.test.ts</files>
  <action>
    Create tests/integration/webrtc/overflow-room.test.ts:

    1. **Test overflow room auto-creation**:
       - Create main room with capacity=12
       - Connect 12 participants to main room
       - Connect 13th participant
       - Assert overflow room created automatically
       - Assert overflow room.name = "{mainRoom.name} - Overflow" (per D-12)
       - Assert 13th participant redirected to overflow room

    2. **Test overflow room capacity**:
       - Main room: 12 participants
       - Overflow room: Connect 4 more participants (13-16)
       - Assert overflow room has 4 participants
       - Attempt to connect 17th participant
       - Assert rejected (room full)

    3. **Test chat shared across rooms per D-12**:
       - Send chat message from main room
       - Assert message received in overflow room
       - Send chat message from overflow room
       - Assert message received in main room

    4. **Test presence shared across rooms**:
       - Get participants from main room
       - Assert count includes overflow room participants (total 16)
       - Display "Main: 12/12, Overflow: 4/4"

    Use Vitest describe() blocks. Mock Socket.IO room namespaces.

    **COVERAGE**: Target >80% for overflow room logic.
  </action>
  <verify>
    <automated>npm test -- tests/integration/webrtc/overflow-room.test.ts</automated>
  </verify>
  <done>tests/integration/webrtc/overflow-room.test.ts covers overflow room auto-creation</done>
</task>

<task type="auto">
  <name>Task 9: Create integration tests for TURN connectivity</name>
  <files>tests/integration/webrtc/turn-connectivity.test.ts</files>
  <action>
    Create tests/integration/webrtc/turn-connectivity.test.ts:

    1. **Test TURN credential generation**:
       - Call generateTurnCredentials(userId)
       - Assert username format: "{timestamp}:{userId}"
       - Assert password is HMAC-SHA1 signature
       - Assert TTL = 3600 seconds (1 hour)

    2. **Test ICE server configuration**:
       - Call getIceServers(turnServerUrl)
       - Assert includes Google public STUN (stun:stun.l.google.com:19302)
       - Assert includes TURN server (turn:{url}:3478)
       - Assert includes fallback STUN servers

    3. **Test TURN authentication**:
       - Mock TURN REST API endpoint
       - Request TURN credentials with valid JWT
       - Assert credentials returned
       - Request with invalid JWT
       - Assert 401 error

    4. **Test ICE connection types**:
       - Mock ICE candidate gathering
       - Test host candidate (direct connection)
       - Test srflx candidate (STUN-assisted)
       - Test relay candidate (TURN-relayed)

    5. **Test TURN connectivity (if coturn deployed)**:
       - If TURN server available in test environment
       - Test actual TURN connection
       - Assert relay connection works
       - Else: Mock TURN server responses

    Use vi.mock() for HTTP requests to coturn REST API. Use nock or msw for API mocking.

    **COVERAGE**: Target >75% for TURN/ICE functionality.
  </action>
  <verify>
    <automated>npm test -- tests/integration/webrtc/turn-connectivity.test.ts</automated>
  </verify>
  <done>tests/integration/webrtc/turn-connectivity.test.ts covers TURN/ICE configuration</done>
</task>

<task type="auto">
  <name>Task 10: Update TESTING.md with WebRTC test instructions</name>
  <files>TESTING.md</files>
  <action>
    Update TESTING.md with WebRTC testing section:

    1. **Add "WebRTC Testing" section**:
       - How to run WebRTC tests: `npm test -- webrtc`
       - How to run specific test: `npm test -- webrtc-connection`
       - Coverage target: >80% for WebRTC code

    2. **Test environment setup**:
       - TURN server deployment (skip for local dev, use STUN-only)
       - Camera/microphone permissions (grant in browser)
       - HTTPS required for getUserMedia (localhost is exception)

    3. **Manual testing checklist**:
       - Test video connection with 2 participants
       - Test mute/unmute controls
       - Test camera on/off
       - Test speaker border highlight
       - Test connection quality indicator
       - Test 12-person room (simulate with multiple browsers)
       - Test overflow room creation
       - Test attendance tracking (wait 90 seconds)

    4. **Troubleshooting**:
       - getUserMedia fails: Check camera permissions
       - TURN connection fails: Check TURN server deployment
       - ICE connection fails: Check firewall/STUN/TURN config
       - Memory leaks: Check transport cleanup on disconnect

    5. **Performance benchmarks**:
       - 12-person room should use <2GB RAM
       - CPU usage should stay <80%
       - Connection time should be <5 seconds

    Use existing TESTING.md structure from Phase 3 as template. Add WebRTC-specific sections.

    **LENGTH**: Target 200+ lines for comprehensive testing guide.
  </action>
  <verify>
    <automated>grep -E "(WebRTC|TURN|ICE|webrtc-connection)" TESTING.md</automated>
  </verify>
  <done>TESTING.md includes WebRTC testing instructions and manual checklist</done>
</task>

<task type="auto">
  <name>Task 11: Create Phase 4 handoff document</name>
  <files>.planning/phases/04-webrtc-integration/04-HANDOFF.md</files>
  <action>
    Create .planning/phases/04-webrtc-integration/04-HANDOFF.md with implementation notes:

    1. **Phase Overview**:
       - Goal: Users can connect to video rooms with reliable audio/video
       - Duration: [actual duration from execution]
       - Status: [success/blockers/lessons learned]

    2. **Implementation Summary**:
       - mediasoup 3.19.19 SFU server setup
       - coturn TURN server deployment (or STUN-only for dev)
       - WebRTC client hooks (useMediaStream, useWebRTCConnection)
       - Producer/consumer logic for audio/video streams
       - Video grid UI with auto-responsive layout
       - Control bar with mute/camera/leave/settings
       - Session timer with countdown
       - Connection quality indicator
       - Room capacity enforcement (12 participants)
       - Overflow room auto-creation (16 total)
       - Attendance tracking (90-second threshold)

    3. **Key Decisions Made**:
       - Video grid breakpoints (1-3, 4-6, 7-9, 10-12)
       - Speaker detection: Hybrid (audio + manual + captain)
       - Speaker border: 2px solid, no animation
       - Control bar: Always visible, 4 buttons
       - Mute visual: Red background + icon change
       - Timer: "... remaining" format, accent color, no color change
       - Connection status: Subtle dot, no intrusive alerts
       - Overflow naming: "{Room Name} - Overflow"
       - Attendance: 90+ seconds cumulative

    4. **Technical Stack**:
       - mediasoup 3.19.19 SFU
       - mediasoup-client 3.18.7
       - Socket.IO 4.8.3 for signaling
       - coturn TURN server (separate VPS)
       - Next.js 16.2.2, React 19
       - Tailwind CSS 3.4.17

    5. **Known Issues**:
       - TURN server deployment pending (using STUN-only for dev)
       - Captain controls not implemented (deferred to Phase 5)
       - Bandwidth estimation not implemented (Claude's discretion)
       - Mobile testing not completed

    6. **Open Questions for Phase 5**:
       - How to integrate task submission with video room?
       - Where to display participant goals in UI?
       - Captain eligibility and invitation flow?
       - Gamification (streak counter, session history)?

    7. **Files Modified**:
       - List all files created/modified in Phase 4
       - Highlight key files for Phase 5 integration

    8. **Next Steps**:
       - Run `/gsd-verify-phase 04` to verify Phase 4 completion
       - Run `/gsd-plan-phase 05-focus-session-features` to start Phase 5

    Use existing handoff format from Phase 3 as template. Target 300+ lines.

    **SECTIONS**: Overview, Summary, Decisions, Stack, Issues, Questions, Files, Next Steps.
  </action>
  <verify>
    <automated>wc -l .planning/phases/04-webrtc-integration/04-HANDOFF.md | awk '{print $1 > 300}'</automated>
  </verify>
  <done>.planning/phases/04-webrtc-integration/04-HANDOFF.md documents Phase 4 implementation</done>
</task>

<task type="auto">
  <name>Task 12: Run all tests and verify coverage</name>
  <files></files>
  <action>
    Run complete test suite and verify coverage:

    1. **Run all WebRTC tests**:
       ```bash
       npm test -- webrtc
       ```

    2. **Run all tests**:
       ```bash
       npm test
       ```

    3. **Check coverage**:
       ```bash
       npm run test:coverage
       ```
       - Verify >80% coverage for WebRTC code
       - Check for uncovered lines

    4. **Fix failing tests**:
       - Debug any test failures
       - Update mocks if needed
       - Fix race conditions (use vi.waitFor())

    5. **Run manual testing checklist**:
       - Test video connection with 2 browsers
       - Test mute/unmute
       - Test camera on/off
       - Test speaker border
       - Test connection quality
       - Test attendance tracking (wait 90 seconds)

    Use TESTING.md checklist for manual testing.

    **GOAL**: All tests passing, >80% coverage, manual testing complete.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All WebRTC tests passing with >80% coverage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real TURN server | Tests should not call real TURN server in CI/CD |
| test → getUserMedia | Tests must mock getUserMedia (no camera in CI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-28 | Spoofing | Test credential leakage | mitigate | Use mock TURN server in tests, never use real TURN_SECRET in test code |
| T-04-29 | Denial of Service | Test resource exhaustion | mitigate | Limit concurrent tests, cleanup mocks after each test, use vi.clearAllMocks() |
| T-04-30 | Information Disclosure | Test data in logs | mitigate | Sanitize test logs, don't log real tokens/credentials |
</threat_model>

<verification>
1. All unit tests passing (useMediaStream, video grid, timer, attendance)
2. All integration tests passing (WebRTC connection, 12-person room, overflow room, TURN)
3. Test coverage >80% for WebRTC code
4. ICE configuration tested with multiple STUN/TURN servers
5. TURN connectivity tested with mocked coturn server
6. 12-person room tested for capacity enforcement
7. Overflow room tested for auto-creation and chat sharing
8. TESTING.md updated with WebRTC test instructions
9. Phase 4 handoff document created
10. Manual testing checklist completed
</verification>

<success_criteria>
1. 11 test files created covering all WebRTC functionality
2. tests/setup.ts extended with WebRTC mocks (mediasoup, getUserMedia, Socket.IO)
3. Unit tests for hooks (useMediaStream, useAttendanceTracking)
4. Unit tests for components (VideoGrid, SessionTimer)
5. Integration tests for WebRTC connection flow
6. Integration tests for 12-person room capacity
7. Integration tests for overflow room auto-creation
8. Integration tests for TURN/ICE configuration
9. All tests passing with >80% coverage
10. TESTING.md updated with WebRTC testing guide
11. Phase 4 handoff document (04-HANDOFF.md) created
12. Ready for Phase 5 planning (Focus Session Features)
</success_criteria>

<output>
After completion, create `.planning/phases/04-webrtc-integration/04-07-SUMMARY.md`
</output>

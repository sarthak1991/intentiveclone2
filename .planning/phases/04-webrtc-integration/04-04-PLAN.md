---
phase: 04-webrtc-integration
plan: 04
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - src/hooks/useWebRTCConnection.ts
  - src/hooks/useSpeakerDetection.ts
  - src/store/roomStore.ts
  - src/hooks/useConnectionQuality.ts
autonomous: true
requirements:
  - VIDE-01
  - VIDE-02
  - VIDE-06
must_haves:
  truths:
    - Producer sends audio track to server
    - Producer sends video track to server
    - Consumer receives audio track from other participants
    - Consumer receives video track from other participants
    - Consumer tracks are attached to HTMLVideoElement elements
    - Speaker detection monitors audio levels and detects active speaker
    - Connection quality metrics track bitrate, packet loss, and connection type
    - roomStore is extended with video/audio state (isMuted, isVideoOff, activeSpeakerId)
  artifacts:
    - path: src/hooks/useWebRTCConnection.ts
      provides: Complete producer/consumer logic
      min_lines: 200
    - path: src/hooks/useSpeakerDetection.ts
      provides: Audio level monitoring for active speaker detection
      min_lines: 80
    - path: src/hooks/useConnectionQuality.ts
      provides: Connection metrics (bitrate, packet loss, connection type)
      min_lines: 100
    - path: src/store/roomStore.ts
      provides: Extended room state with WebRTC properties
      min_lines: 130
  key_links:
    - from: src/hooks/useWebRTCConnection.ts
      to: src/hooks/useSpeakerDetection.ts
      via: Consumer audio track monitoring
      pattern: "useSpeakerDetection.*audioTrack"
    - from: src/hooks/useWebRTCConnection.ts
      to: src/store/roomStore.ts
      via: setActiveSpeakerId state update
      pattern: "setActiveSpeakerId"
    - from: src/hooks/useConnectionQuality.ts
      to: mediasoup consumer
      via: consumer.getStats()
      pattern: "consumer.getStats"
</key_links>
---

<objective>
Implement producer/consumer logic for audio/video streams with mediasoup-client, including speaker detection and connection quality monitoring.

Purpose: Complete the client-side WebRTC implementation by handling producer (outgoing) and consumer (incoming) stream lifecycle. Producers send local audio/video to the SFU. Consumers receive remote streams from other participants. Speaker detection uses Web Audio API to monitor audio levels and identify the active speaker (for border highlight per D-01/D-02). Connection quality monitoring tracks bitrate, packet loss, and connection type (for status indicator per D-11).

Output: Working producer/consumer implementation with speaker detection and connection quality monitoring.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webrtc-integration/04-CONTEXT.md
@.planning/phases/04-webrtc-integration/04-RESEARCH.md
@.planning/phases/04-webrtc-integration/04-03-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Existing Infrastructure
@server/socket-server.ts — Socket.IO server with WebRTC signaling handlers (from plan 04-02)
@src/hooks/useMediaStream.ts — Media stream hook with mute/unmute (from plan 04-03)
@src/hooks/useWebRTCConnection.ts — WebRTC connection hook with Device/transport (from plan 04-03)
@src/store/roomStore.ts — Zustand store for room state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend roomStore with WebRTC state</name>
  <files>src/store/roomStore.ts</files>
  <action>
    Extend src/store/roomStore.ts with WebRTC state:

    1. **Add to RoomState interface**:
       - isMuted: boolean (local user audio state)
       - isVideoOff: boolean (local user video state)
       - activeSpeakerId: string | null (current speaker's userId)
       - producers: Map<string, any> (local producers: audio, video)
       - consumers: Map<string, any[]> (remote consumers by userId)

    2. **Add actions**:
       - setMuted: (isMuted: boolean) => void
       - setVideoOff: (isVideoOff: boolean) => void
       - setActiveSpeakerId: (userId: string | null) => void
       - addProducer: (kind: string, producer: any) => void
       - addConsumer: (userId: string, consumer: any) => void
       - removeConsumer: (userId: string, consumerId: string) => void

    3. **Update reset()**: Clear WebRTC state along with existing room state

    This extends the existing roomStore with WebRTC-specific state. Don't break existing participants/messages state.

    **IMPORTANT**: Don't store MediaStream or MediaStreamTrack in Zustand (not serializable). Store only metadata (isMuted, isVideoOff, activeSpeakerId).
  </action>
  <verify>
    <automated>grep -E "(isMuted|isVideoOff|activeSpeakerId|producers|consumers)" src/store/roomStore.ts</automated>
  </verify>
  <done>src/store/roomStore.ts includes WebRTC state properties and actions</done>
</task>

<task type="auto">
  <name>Task 2: Complete producer logic in useWebRTCConnection</name>
  <files>src/hooks/useWebRTCConnection.ts</files>
  <action>
    Extend src/hooks/useWebRTCConnection.ts with complete producer logic:

    1. **Create audio producer**:
       - Get audio track from localStream (from useMediaStream hook)
       - Call createProducer(transport, audioTrack, 'audio')
       - Store in producers Map
       - Handle producer 'pause'/'resume' events (mute/unmute)
       - Update roomStore.isMuted state
       - Emit 'new-producer' to server

    2. **Create video producer**:
       - Get video track from localStream
       - Call createProducer(transport, videoTrack, 'video')
       - Store in producers Map
       - Handle producer 'pause'/'resume' events (camera on/off)
       - Update roomStore.isVideoOff state
       - Emit 'new-producer' to server

    3. **Handle producer close**:
       - On unmount or error, close producers
       - Remove from producers Map
       - Emit to server that producer closed

    4. **Implement replaceTrack()**:
       - Allow replacing track without recreating producer
       - Useful for camera switching (front/back on mobile)

    Use the code example from RESEARCH.md §WebRTC Connection Hook (React) — produceAudio/produceVideo functions.
  </action>
  <verify>
    <automated>grep -E "(produceAudio|produceVideo|replaceTrack)" src/hooks/useWebRTCConnection.ts</automated>
  </verify>
  <done>src/hooks/useWebRTCConnection.ts creates audio/video producers and handles mute/unmute</done>
</task>

<task type="auto">
  <name>Task 3: Implement consumer logic for incoming streams</name>
  <files>src/hooks/useWebRTCConnection.ts</files>
  <action>
    Extend src/hooks/useWebRTCConnection.ts with consumer logic:

    1. **Listen for new-producer events**:
       - Socket.IO event: 'new-producer' { producerId, userId, kind }
       - Call createConsumer() for each new producer
       - Add to consumers array

    2. **createConsumer() function**:
       - Emit 'consume' request to server
       - Receive consumer data (id, producerId, kind, rtpParameters)
       - Call device.createConsumer()
       - Handle consumer 'transportclose' event
       - Return consumer object

    3. **Attach consumer track to video element**:
       - Create new MediaStream from consumer track
       - Attach stream to HTMLVideoElement via srcObject
       - Handle track mute/unmute events
       - Store consumer in roomStore.consumers Map

    4. **Resume consumer**:
       - Call consumer.resume() to start receiving media
       - Emit 'resume-consumer' to server

    5. **Handle consumer close**:
       - On user leave or disconnect, close consumer
       - Remove from roomStore.consumers
       - Clean up video element

    Use the code example from RESEARCH.md §WebRTC Connection Hook (React) — new-producer event handler.

    **OPTIMIZATION**: Don't consume own producers (filter by userId).
  </action>
  <verify>
    <automated>grep -E "(createConsumer|new-producer|resume)" src/hooks/useWebRTCConnection.ts</automated>
  </verify>
  <done>src/hooks/useWebRTCConnection.ts consumes incoming streams and attaches to video elements</done>
</task>

<task type="auto">
  <name>Task 4: Create speaker detection hook</name>
  <files>src/hooks/useSpeakerDetection.ts</files>
  <action>
    Create src/hooks/useSpeakerDetection.ts with audio level monitoring:

    1. **Function signature**:
       ```typescript
       useSpeakerDetection(
         audioTrack: MediaStreamTrack | null,
         onSpeakerDetected: () => void,
         threshold: number = -60 // dB
       )
       ```

    2. **Create AudioContext and AnalyserNode**:
       - Create AudioContext on mount
       - Create AnalyserNode with fftSize=256, smoothingTimeConstant=0.8
       - Connect audio track to analyser via MediaStreamSource

    3. **Monitor audio levels**:
       - Use requestAnimationFrame loop
       - Get frequency data from analyser
       - Calculate average volume in dB
       - Detect if above threshold (speaking)

    4. **Debounce speaker detection**:
       - Only trigger onSpeakerDetected() once per second max
       - Prevents rapid switching between speakers (avoid distraction per ADHD-friendly design)

    5. **Cleanup on unmount**:
       - Close AudioContext
       - Disconnect nodes

    Use the code example from RESEARCH.md §Pattern 5: Audio Level Detection for Speaker Indication.

    **IMPORTANT**: Set threshold to -60 dB per RESEARCH.md recommendations. Adjust based on testing (avoid false positives from background noise).
  </action>
  <verify>
    <automated>test -f src/hooks/useSpeakerDetection.ts && grep -E "(useSpeakerDetection|AudioContext|AnalyserNode)" src/hooks/useSpeakerDetection.ts</automated>
  </verify>
  <done>src/hooks/useSpeakerDetection.ts monitors audio levels and detects active speaker</done>
</task>

<task type="auto">
  <name>Task 5: Create connection quality hook</name>
  <files>src/hooks/useConnectionQuality.ts</files>
  <action>
    Create src/hooks/useConnectionQuality.ts with connection metrics:

    1. **Function signature**:
       ```typescript
       useConnectionQuality(consumer: Consumer | null) => {
         bitrate: number
         packetLoss: number
         connectionType: 'host' | 'srflx' | 'relay'
         quality: 'good' | 'degraded' | 'poor'
       }
       ```

    2. **Get consumer stats**:
       - Use setInterval to poll consumer.getStats() every 5 seconds
       - Extract bitrate (bytes received per second)
       - Extract packet loss (packets lost / packets received)
       - Extract ICE connection type (host, srflx, relay)

    3. **Determine quality level**:
       - Good: bitrate >500kbps AND packet loss <2%
       - Degraded: bitrate 100-500kbps OR packet loss 2-10%
       - Poor: bitrate <100kbps OR packet loss >10%

    4. **Return metrics**:
       - bitrate: number (kbps)
       - packetLoss: number (percentage)
       - connectionType: 'host' | 'srflx' | 'relay'
       - quality: 'good' | 'degraded' | 'poor'

    5. **Cleanup on unmount**:
       - Clear stats polling interval

    Use the code structure from RESEARCH.md §Code Examples — Connection Quality Hook.

    **IMPLEMENTATION NOTE**: This is Claude's discretion per D-11. Keep it minimal: just status indicator, no intrusive alerts.
  </action>
  <verify>
    <automated>test -f src/hooks/useConnectionQuality.ts && grep -E "(useConnectionQuality|getStats|bitrate|packetLoss)" src/hooks/useConnectionQuality.ts</automated>
  </verify>
  <done>src/hooks/useConnectionQuality.ts tracks connection quality metrics</done>
</task>

<task type="auto">
  <name>Task 6: Integrate speaker detection with useWebRTCConnection</name>
  <files>src/hooks/useWebRTCConnection.ts</files>
  <action>
    Integrate speaker detection into src/hooks/useWebRTCConnection.ts:

    1. **For each consumer audio track**:
       - Extract audio track from consumer
       - Call useSpeakerDetection(audioTrack, onSpeakerDetected)
       - Update roomStore.activeSpeakerId when detected

    2. **Handle active speaker changes**:
       - When audio level exceeds threshold, set activeSpeakerId
       - Clear previous active speaker after 2 seconds of silence
       - Debounce to prevent rapid switching

    3. **Implement hybrid speaker detection per D-02**:
       - Automatic: Audio level detection (implemented)
       - Manual: Add "raise hand" button (future phase)
       - Captain override: Captain can manually highlight speaker (future phase)

    This enables the speaker border highlight feature (D-01/D-03) for video grid.
  </action>
  <verify>
    <automated>grep -E "(useSpeakerDetection|activeSpeakerId)" src/hooks/useWebRTCConnection.ts</automated>
  </verify>
  <done>src/hooks/useWebRTCConnection.ts integrates speaker detection and updates activeSpeakerId</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer audio track | Untrusted remote audio, potential for loud/abusive audio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Tampering | Consumer track injection | mitigate | Server validates producer parameters, only forwards authorized streams |
| T-04-17 | Denial of Service | Audio processing exhaustion | mitigate | Limit speaker detection polling to 60fps max, debounce to 1 update/second |
| T-04-18 | Information Disclosure | Local audio leakage | mitigate | Don't consume own producers, filter by userId |
| T-04-19 | Elevation of Privilege | Unauthorized speaker detection | accept | Client-side only, no security impact (visual indicator only) |
</threat_model>

<verification>
1. Producers send audio/video tracks to server successfully
2. Consumers receive remote tracks and attach to video elements
3. Speaker detection monitors audio levels and detects active speaker
4. Connection quality hook tracks bitrate, packet loss, connection type
5. roomStore extended with isMuted, isVideoOff, activeSpeakerId
6. Producer mute/unmute updates roomStore state
7. Consumer close cleans up video elements
8. No memory leaks (all resources cleaned up on unmount)
</verification>

<success_criteria>
1. src/store/roomStore.ts extended with WebRTC state (isMuted, isVideoOff, activeSpeakerId, producers, consumers)
2. src/hooks/useWebRTCConnection.ts creates audio/video producers and consumes incoming streams
3. src/hooks/useSpeakerDetection.ts monitors audio levels with debouncing
4. src/hooks/useConnectionQuality.ts tracks bitrate, packet loss, connection type
5. Producer mute/unmute works and updates roomStore
6. Consumer tracks attached to video elements
7. Speaker detection updates activeSpeakerId in roomStore
8. All hooks include proper cleanup
9. Ready for video grid UI implementation (plan 04-05)
</success_criteria>

<output>
After completion, create `.planning/phases/04-webrtc-integration/04-04-SUMMARY.md`
</output>

---
phase: 04-webrtc-integration
plan: 06
type: execute
wave: 3
depends_on: [04-05]
files_modified:
  - server/socket-server.ts
  - src/models/Room.ts
  - src/hooks/useAttendanceTracking.ts
  - src/api/room/capacity.ts
  - src/app/api/room/[roomId]/join/route.ts
autonomous: true
requirements:
  - ROOM-04
  - ROOM-07
  - ROOM-08
must_haves:
  truths:
    - System enforces 12 participant capacity limit per room
    - System auto-creates overflow room when 13th participant joins
    - Overflow room named "{Original Room Name} - Overflow" per D-12
    - First 12 participants stay in main room, participants 13-16 go to overflow
    - Chat is shared across main and overflow rooms per D-12
    - System tracks cumulative time in session (90+ seconds = attended per D-13)
    - Attendance tracking persists across disconnect/reconnect
    - System marks user as "attended" when 90-second threshold reached
  artifacts:
    - path: server/socket-server.ts
      provides: Room capacity enforcement and overflow room creation
      min_lines: 50
    - path: src/models/Room.ts
      provides: Room schema with capacity and overflowRoomId fields
      min_lines: 60
    - path: src/hooks/useAttendanceTracking.ts
      provides: Attendance tracking hook with 90-second threshold
      min_lines: 80
    - path: src/app/api/room/[roomId]/join/route.ts
      provides: API endpoint for room join with capacity check
      min_lines: 80
  key_links:
    - from: src/app/api/room/[roomId]/join/route.ts
      to: server/socket-server.ts
      via: Room capacity check
      pattern: "participants.length >= capacity"
    - from: src/hooks/useAttendanceTracking.ts
      to: src/models/Room.ts
      via: Attendance record update
      pattern: "updateOne.*attended"
</key_links>
---

<objective>
Implement room capacity enforcement (12 participants), auto-scaling to overflow rooms (16 total), and attendance validation (90+ seconds in session).

Purpose: Enforce room capacity limits to prevent overcrowding (per ROOM-04). Auto-create overflow rooms when main room is full (per ROOM-07, D-12). Track attendance to validate session participation (per ROOM-08, D-13). Overflow room naming follows "{Original Room Name} - Overflow" convention. Chat is shared across main and overflow rooms. Attendance tracking uses cumulative time (disconnect/reconnect doesn't reset timer).

Output: Working room capacity enforcement, overflow room auto-creation, shared chat across rooms, and attendance tracking with 90-second threshold.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webrtc-integration/04-CONTEXT.md
@.planning/phases/04-webrtc-integration/04-RESEARCH.md
@.planning/phases/04-webrtc-integration/04-05-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Existing Infrastructure
@server/socket-server.ts — Socket.IO server with WebRTC signaling (from plan 04-02)
@server/presence.ts — Presence tracking with Map<roomId, Set<userId>>
@src/models/Room.ts — Room schema with capacity field (from Phase 2)
@src/store/roomStore.ts — Zustand store with room state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Room model with overflow room field</name>
  <files>src/models/Room.ts</files>
  <action>
    Extend src/models/Room.ts with overflow room support:

    1. **Add overflowRoomId field**:
       ```typescript
       overflowRoomId: {
         type: Schema.Types.ObjectId,
         ref: 'Room',
         default: null
       }
       ```

    2. **Add isOverflowRoom field**:
       ```typescript
       isOverflowRoom: {
         type: Boolean,
         default: false
       }
       ```

    3. **Update indexes**:
       - Add compound index on { startTime, isOverflowRoom } for efficient queries
       - Keep existing capacity index

    This allows linking main room to overflow room and distinguishing overflow rooms.

    **MIGRATION**: Update existing Room documents to add these fields (default null/false).
  </action>
  <verify>
    <automated>grep -E "(overflowRoomId|isOverflowRoom)" src/models/Room.ts</automated>
  </verify>
  <done>src/models/Room.ts includes overflowRoomId and isOverflowRoom fields</done>
</task>

<task type="auto">
  <name>Task 2: Implement room capacity enforcement in Socket.IO server</name>
  <files>server/socket-server.ts</files>
  <action>
    Extend server/socket-server.ts with room capacity enforcement:

    1. **Import Room model**: Import Room from src/models/Room.ts

    2. **Add capacity check in connection handler**:
       - Before allowing user to join room, check current participant count
       - Use getParticipants(roomId) from presence.ts
       - If count >= room.capacity (12), reject connection
       - Emit 'room-full' error to client

    3. **Handle overflow room creation per ROOM-07**:
       - If main room is full (12 participants), check for existing overflow room
       - If no overflow room exists, create new Room document:
         - name: "{originalRoom.name} - Overflow" (per D-12)
         - startTime: same as main room
         - capacity: 4 (participants 13-16)
         - isOverflowRoom: true
         - overflowRoomId: null
       - Link main room to overflow: mainRoom.overflowRoomId = overflowRoom._id
       - Redirect 13th participant to overflow room

    4. **Chat sharing per D-12**:
       - Forward chat messages from main room to overflow room
       - Forward chat messages from overflow room to main room
       - Use roomNamespace.to(mainRoomId).emit() and roomNamespace.to(overflowRoomId).emit()

    5. **Presence sharing**:
       - Show participant count across both rooms (main + overflow)
       - Display "Main room: 12/12, Overflow: 1/4" to users

    Use the architecture pattern from RESEARCH.md §Code Examples — Overflow Room Logic.

    **ERROR HANDLING**: If overflow room creation fails, emit 'room-full' error and deny join.
  </action>
  <verify>
    <automated>grep -E "(capacity|overflowRoomId|room-full)" server/socket-server.ts</automated>
  </verify>
  <done>server/socket-server.ts enforces capacity and creates overflow rooms</done>
</task>

<task type="auto">
  <name>Task 3: Create attendance tracking hook</name>
  <files>src/hooks/useAttendanceTracking.ts</files>
  <action>
    Create src/hooks/useAttendanceTracking.ts with 90-second attendance tracking:

    1. **Function signature**:
       ```typescript
       useAttendanceTracking(sessionId: string, isConnected: boolean) => {
         hasAttended: boolean
         cumulativeTime: number
       }
       ```

    2. **Track cumulative time per D-13**:
       - Start tracking when isConnected=true (WebRTC connected)
       - Pause tracking when isConnected=false (disconnected)
       - Don't reset timer on reconnect (accumulate time)
       - Store cumulativeTime in ref

    3. **Calculate attendance threshold per ROOM-08**:
       - ATTENDANCE_THRESHOLD = 90 seconds
       - Check every second: cumulativeTime >= threshold
       - When threshold reached, set hasAttended=true
       - Emit 'attendance-confirmed' event to server

    4. **Persist attendance state**:
       - Store hasAttended in localStorage or roomStore
       - Prevent duplicate attendance confirmations
       - Send to server only once per session

    5. **Cleanup**:
       - Clear interval on unmount
       - Don't lose cumulativeTime on disconnect

    Use the code example from RESEARCH.md §Code Examples — Attendance Tracking Hook.

    **EDGE CASE**: If user reconnects multiple times, accumulate time across all connections.
  </action>
  <verify>
    <automated>test -f src/hooks/useAttendanceTracking.ts && grep -E "(useAttendanceTracking|cumulativeTime|90|hasAttended)" src/hooks/useAttendanceTracking.ts</automated>
  </verify>
  <done>src/hooks/useAttendanceTracking.ts tracks cumulative time and validates attendance</done>
</task>

<task type="auto">
  <name>Task 4: Create attendance API endpoint</name>
  <files>src/app/api/room/[roomId]/attendance/route.ts</files>
  <action>
    Create src/app/api/room/[roomId]/attendance/route.ts with attendance confirmation:

    1. **POST endpoint**: Confirm attendance for user in session

    2. **Request body**:
       ```typescript
       {
         userId: string
         sessionId: string
       }
       ```

    3. **Authentication**: Require NextAuth session token (verify in middleware)

    4. **Update attendance record**:
       - Find Registration document for userId + sessionId
       - Set attended = true
       - Set attendedAt = new Date()
       - Save to database

    5. **Return success**:
       ```json
       {
         success: true,
         attended: true
       }
       ```

    6. **Error handling**:
       - 404 if registration not found
       - 400 if already attended
       - 401 if not authenticated

    Use existing Registration model from Phase 2. Update schema to include attended and attendedAt fields if not present.

    **RATE LIMITING**: Max 1 attendance confirmation per user per session (prevent duplicate records).
  </action>
  <verify>
    <automated>test -f src/app/api/room/[roomId]/attendance/route.ts && grep -E "(attendance|attended)" src/app/api/room/[roomId]/attendance/route.ts</automated>
  </verify>
  <done>src/app/api/room/[roomId]/attendance/route.ts handles attendance confirmation</done>
</task>

<task type="auto">
  <name>Task 5: Update room join API with capacity check</name>
  <files>src/app/api/room/[roomId]/join/route.ts</files>
  <action>
    Update or create src/app/api/room/[roomId]/join/route.ts with capacity check:

    1. **GET/POST endpoint**: Check if user can join room

    2. **Authentication**: Require NextAuth session token

    3. **Check room capacity**:
       - Get room by roomId
       - Get current participant count from presence.ts
       - If count >= capacity, check for overflow room
       - If overflow room exists, return overflow room info
       - If no overflow room, create it and return info

    4. **Response**:
       ```json
       {
         canJoin: true,
         roomId: "original-room-id" | "overflow-room-id",
         roomName: "9:00 AM Focus Room" | "9:00 AM Focus Room - Overflow",
         isOverflow: false | true,
         capacity: 12 | 4,
         currentParticipants: 5 | 1
       }
       ```

    5. **Error response**:
       ```json
       {
         canJoin: false,
         error: "Room is full"
       }
       ```

    Use this endpoint on room page load to check capacity before attempting WebRTC connection.

    **REDIRECTION**: If user is redirected to overflow room, show toast notification: "This room is full. You've been moved to the overflow room."
  </action>
  <verify>
    <automated>test -f src/app/api/room/[roomId]/join/route.ts && grep -E "(canJoin|capacity|overflow)" src/app/api/room/[roomId]/join/route.ts</automated>
  </verify>
  <done>src/app/api/room/[roomId]/join/route.ts checks capacity and returns room info</done>
</task>

<task type="auto">
  <name>Task 6: Integrate attendance tracking in video room page</name>
  <files>src/app/room/[roomId]/page.tsx</files>
  <action>
    Integrate attendance tracking into src/app/room/[roomId]/page.tsx:

    1. **Import useAttendanceTracking hook**

    2. **Call hook with WebRTC connection state**:
       ```typescript
       const { hasAttended } = useAttendanceTracking(roomId, isWebRTCConnected)
       ```

    3. **Pass isConnected state**:
       - Use transport state from useWebRTCConnection
       - isConnected = transport !== null && transport.connected

    4. **Handle attendance confirmation**:
       - When hasAttended becomes true, call API endpoint
       - Show toast notification: "Attendance confirmed! ✓"
       - Prevent duplicate confirmations

    5. **Display attendance status**:
       - Show small badge or text in UI when attended
       - Use green checkmark icon
       - Position near session timer or user info

    6. **Store attendance locally**:
       - Use roomStore to track hasAttended state
       - Persist across page refreshes (localStorage)

    Use shadcn/ui Badge component for attendance status. Use lucide-react CheckCircle icon.

    **IMPLEMENTATION NOTE**: This implements D-13 (90+ seconds in session = attended).
  </action>
  <verify>
    <automated>grep -E "(useAttendanceTracking|hasAttended|attendance-confirmed)" src/app/room/[roomId]/page.tsx</automated>
  </verify>
  <done>src/app/room/[roomId]/page.tsx integrates attendance tracking with 90-second threshold</done>
</task>

<task type="auto">
  <name>Task 7: Update presence tracking for overflow rooms</name>
  <files>server/presence.ts</files>
  <action>
    Update server/presence.ts to handle overflow rooms:

    1. **Add function to get total participant count**:
       ```typescript
       getParticipantsWithOverflow(roomId: string): Participant[] {
         // Get main room participants
         // Get overflow room participants
         // Return combined list
       }
       ```

    2. **Update broadcastPresence function**:
       - Broadcast to main room AND overflow room
       - Show total count across both rooms

    3. **Handle overflow room link**:
       - If room.isOverflowRoom is true, get parent room ID
       - Fetch participants from both rooms
       - Combine and return

    This ensures accurate participant counts across main and overflow rooms.

    **PERFORMANCE**: Cache overflow room lookup to avoid excessive database queries.
  </action>
  <verify>
    <automated>grep -E "(getParticipantsWithOverflow|overflowRoomId|isOverflowRoom)" server/presence.ts</automated>
  </verify>
  <done>server/presence.ts handles overflow room participant tracking</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → capacity check | Untrusted client may try to bypass capacity limits |
| attendance confirmation | Untrusted client may claim attendance without actually attending |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-24 | Tampering | Capacity bypass | mitigate | Server-side capacity enforcement in Socket.IO handler, don't trust client counts |
| T-04-25 | Spoofing | Attendance fraud | mitigate | Server tracks actual time in session via WebRTC connection state, don't trust client reports |
| T-04-26 | Denial of Service | Overflow room exhaustion | mitigate | Limit to 1 overflow room per main room (16 total max), reject 17th participant |
| T-04-27 | Tampering | Overflow room injection | mitigate | Only server can create overflow rooms, validate isOverflowRoom field on all operations |
</threat_model>

<verification>
1. Room capacity enforced at 12 participants
2. Overflow room auto-created when 13th participant joins
3. Overflow room named "{Original Room Name} - Overflow"
4. First 12 in main room, participants 13-16 in overflow
5. Chat messages shared across main and overflow rooms
6. Attendance tracking starts when WebRTC connected
7. Attendance tracking pauses on disconnect, resumes on reconnect
8. Cumulative time tracked across reconnects
9. hasAttended becomes true after 90 seconds cumulative
10. Attendance confirmation sent to server API
</verification>

<success_criteria>
1. src/models/Room.ts extended with overflowRoomId and isOverflowRoom fields
2. server/socket-server.ts enforces 12-participant capacity
3. server/socket-server.ts auto-creates overflow rooms when main room full
4. server/socket-server.ts forwards chat messages between main and overflow rooms
5. server/presence.ts tracks participants across both rooms
6. src/hooks/useAttendanceTracking.ts tracks cumulative time with 90-second threshold
7. src/app/api/room/[roomId]/attendance/route.ts confirms attendance server-side
8. src/app/api/room/[roomId]/join/route.ts checks capacity before join
9. src/app/room/[roomId]/page.tsx integrates attendance tracking
10. All overflow room naming follows D-12 convention
11. Chat is shared across rooms per D-12
12. Ready for comprehensive testing and documentation (plan 04-07)
</success_criteria>

<output>
After completion, create `.planning/phases/04-webrtc-integration/04-06-SUMMARY.md`
</output>

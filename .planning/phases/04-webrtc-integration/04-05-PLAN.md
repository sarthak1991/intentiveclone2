---
phase: 04-webrtc-integration
plan: 05
type: execute
wave: 3
depends_on: [04-04]
files_modified:
  - src/components/room/VideoGrid.tsx
  - src/components/room/VideoCard.tsx
  - src/components/room/ControlBar.tsx
  - src/components/room/SessionTimer.tsx
  - src/components/room/ConnectionStatus.tsx
autonomous: true
requirements:
  - VIDE-04
  - ROOM-06
must_haves:
  truths:
    - Video grid displays 1-12 participants in auto-responsive layout
    - Grid adapts based on participant count (1-3: rows, 4-6: 2x3, 7-9: 3x3, 10-12: 3x4)
    - Video card displays participant name and photo
    - Video card displays video stream from consumer
    - Active speaker has 2px solid border (no animation per D-03)
    - Control bar displays 4 buttons (Mute, Camera, Leave, Settings)
    - Mute button shows red background when muted per D-06
    - Camera button shows red background when off per D-06
    - Session timer displays "MM:SS remaining" format per D-09
    - Timer uses accent color with no color change per D-10
    - Connection status displays subtle dot indicator (green/yellow/red) per D-11
    - All components use shadcn/ui primitives
  artifacts:
    - path: src/components/room/VideoGrid.tsx
      provides: Auto-responsive grid layout (1-12 participants)
      min_lines: 80
    - path: src/components/room/VideoCard.tsx
      provides: Individual video participant card with border highlight
      min_lines: 120
    - path: src/components/room/ControlBar.tsx
      provides: Bottom control bar with mute/camera/leave/settings
      min_lines: 100
    - path: src/components/room/SessionTimer.tsx
      provides: 45-minute countdown timer
      min_lines: 60
    - path: src/components/room/ConnectionStatus.tsx
      provides: Subtle connection quality indicator
      min_lines: 80
  key_links:
    - from: src/components/room/VideoGrid.tsx
      to: src/store/roomStore.ts
      via: participants state
      pattern: "useRoomStore.*participants"
    - from: src/components/room/VideoCard.tsx
      to: src/hooks/useConnectionQuality.ts
      via: Connection metrics
      pattern: "useConnectionQuality"
    - from: src/components/room/ControlBar.tsx
      to: src/hooks/useMediaStream.ts
      via: toggleAudio/toggleVideo
      pattern: "toggleAudio|toggleVideo"
</key_links>
---

<objective>
Create video grid UI component with adaptive layout (1-12 participants) and participant cards with speaker border highlight, control bar, session timer, and connection status.

Purpose: Build the complete video room UI using React components and Tailwind CSS. VideoGrid adapts layout based on participant count (per D-01). VideoCard displays participant video, name, photo, and speaker border highlight (per D-03). ControlBar provides mute/camera/leave/settings buttons (per D-04/D-05/D-06). SessionTimer shows 45-minute countdown (per D-08/D-09/D-10). ConnectionStatus shows subtle quality indicator (per D-11). All components use shadcn/ui primitives and prioritize ADHD-friendly design (calm, predictable, minimal stress).

Output: Complete video room UI with auto-responsive grid, participant cards, control bar, timer, and connection status.
</objective>

<execution_context>
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sarthakbatra/Documents/new_dev/intentiveclone2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webrtc-integration/04-CONTEXT.md
@.planning/phases/04-webrtc-integration/04-RESEARCH.md
@.planning/phases/04-webrtc-integration/04-04-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Existing Infrastructure
@src/store/roomStore.ts — Zustand store with participants, WebRTC state (from plan 04-04)
@src/hooks/useMediaStream.ts — Media stream hook with toggleAudio/toggleVideo (from plan 04-03)
@src/hooks/useConnectionQuality.ts — Connection quality hook (from plan 04-04)
@src/components/ui/ — shadcn/ui components (Button, Badge, etc.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VideoGrid component with auto-responsive layout</name>
  <files>src/components/room/VideoGrid.tsx</files>
  <action>
    Create src/components/room/VideoGrid.tsx with auto-responsive grid:

    1. **Import dependencies**:
       - useRoomStore for participants and activeSpeakerId
       - VideoCard component (to be created)
       - Tailwind CSS grid utilities

    2. **Calculate grid layout**:
       ```typescript
       const getGridClass = (count: number): string => {
         if (count <= 3) return 'grid-cols-1' // Full-width rows
         if (count <= 6) return 'grid-cols-2' // 2x3 grid
         if (count <= 9) return 'grid-cols-3' // 3x3 grid
         return 'grid-cols-3' // 3x4 grid (10-12 participants)
       }
       ```
       Implements per D-01 (auto-responsive grid)

    3. **Render video cards**:
       - Map over participants from roomStore
       - Pass participant data, activeSpeakerId, consumer stream to VideoCard
       - Use grid-cols-* classes for layout
       - Add gap-4 for spacing

    4. **Handle empty state**:
       - If no participants, show "Waiting for others..." message
       - Use centered layout with icon

    Use the code example from RESEARCH.md §Pattern 4: Video Grid Component with Auto-Responsive Layout.

    **STYLING**: Use aspect-ratio: 16/9 for consistent video card dimensions. Use rounded-lg for corners. Use bg-gray-900 for video placeholder.
  </action>
  <verify>
    <automated>test -f src/components/room/VideoGrid.tsx && grep -E "(getGridClass|VideoCard|useRoomStore)" src/components/room/VideoGrid.tsx</automated>
  </verify>
  <done>src/components/room/VideoGrid.tsx renders auto-responsive grid for 1-12 participants</done>
</task>

<task type="auto">
  <name>Task 2: Create VideoCard component with speaker border</name>
  <files>src/components/room/VideoCard.tsx</files>
  <action>
    Create src/components/room/VideoCard.tsx with participant video card:

    1. **Props interface**:
       - participant: Participant (userId, userName, userPhoto)
       - consumer: Consumer | null
       - isActiveSpeaker: boolean
       - onMuteToggle?: (userId: string) => void (for captain controls per D-07)

    2. **Render structure**:
       - Container div with aspect-ratio: 16/9
       - Video element (srcObject from consumer track)
       - Overlay with participant name and photo
       - Speaker border (2px solid accent color) when isActiveSpeaker=true

    3. **Speaker border highlight per D-03**:
       - 2px solid border using accent color (use ring-2 ring-accent)
       - NO animation (avoids distraction for ADHD users)
       - Apply when isActiveSpeaker=true

    4. **Video placeholder**:
       - If no video or video off, show participant photo
       - Use gray background with centered photo
       - Show "Camera off" icon

    5. **Participant info overlay**:
       - Bottom-left corner: participant name
       - Small circular photo (32x32)
       - Semi-transparent background for readability

    6. **Captain controls per D-07** (optional, for future phase):
       - Click card to mute/unmute participant
       - Show confirmation modal
       - Only visible if current user is captain

    7. **Connection status**:
       - Use useConnectionQuality hook
       - Show green/yellow/red dot in top-right corner
       - Tooltip on hover with details (bitrate, packet loss)

    Use shadcn/ui Avatar component for participant photo. Use lucide-react icons (CameraOff, Signal, etc.).

    **STYLING**: Use relative positioning for overlay. Use object-cover for video. Use rounded-lg corners.
  </action>
  <verify>
    <automated>test -f src/components/room/VideoCard.tsx && grep -E "(isActiveSpeaker|ring-2|useConnectionQuality)" src/components/room/VideoCard.tsx</automated>
  </verify>
  <done>src/components/room/VideoCard.tsx displays participant video with speaker border highlight</done>
</task>

<task type="auto">
  <name>Task 3: Create ControlBar component with 4 buttons</name>
  <files>src/components/room/ControlBar.tsx</files>
  <action>
    Create src/components/room/ControlBar.tsx with bottom control bar:

    1. **Import dependencies**:
       - useMediaStream hook for toggleAudio/toggleVideo
       - useRoomStore for isMuted, isVideoOff state
       - shadcn/ui Button component
       - lucide-react icons (Mic, MicOff, Video, VideoOff, LogOut, Settings)

    2. **Fixed bottom bar layout**:
       - Fixed position at bottom of screen (bottom-4)
       - Centered horizontally (left-1/2 -translate-x-1/2)
       - Flex row layout with gap-4
       - Dark background (bg-gray-900/90 backdrop-blur)
       - Rounded-full pill shape
       - Padding: px-6 py-3
       - Always visible (no auto-hide per D-04)

    3. **Four primary buttons per D-05**:
       - **Mute button**: Mic/MicOff icon, red background when muted (bg-red-500 per D-06)
       - **Camera button**: Video/VideoOff icon, red background when off (bg-red-500 per D-06)
       - **Leave button**: LogOut icon, prominent styling (red or destructive variant)
       - **Settings button**: Settings icon, opens device settings modal

    4. **Button interactions**:
       - Mute: Call toggleAudio() from useMediaStream
       - Camera: Call toggleVideo() from useMediaStream
       - Leave: Navigate away from room page
       - Settings: Open dialog to select mic/camera/speaker (future phase)

    5. **Visual feedback per D-06**:
       - Muted state: Red background (bg-red-500), icon changes to slashed mic
       - Camera off: Red background (bg-red-500), icon changes to slashed camera
       - Normal state: Gray background (bg-gray-700)

    Use shadcn/ui Button component with size="icon" for circular buttons. Use variant="destructive" for leave button.

    **ACCESSIBILITY**: Add aria-label to each button. Add keyboard shortcuts (M for mute, V for video, L for leave).
  </action>
  <verify>
    <automated>test -f src/components/room/ControlBar.tsx && grep -E "(toggleAudio|toggleVideo|Mute|Camera|Leave|Settings)" src/components/room/ControlBar.tsx</automated>
  </verify>
  <done>src/components/room/ControlBar.tsx displays bottom control bar with 4 buttons</done>
</task>

<task type="auto">
  <name>Task 4: Create SessionTimer component with countdown</name>
  <files>src/components/room/SessionTimer.tsx</files>
  <action>
    Create src/components/room/SessionTimer.tsx with 45-minute countdown:

    1. **Props interface**:
       - startTime: Date (session start time)
       - durationMinutes: number (default: 45)

    2. **Countdown logic**:
       - Calculate endTime = startTime + durationMinutes
       - Use useEffect with setInterval to update every second
       - Calculate remainingSeconds = endTime - now
       - Clamp to 0 (don't show negative time)

    3. **Format time as "MM:SS remaining" per D-09**:
       ```typescript
       const formatTime = (seconds: number): string => {
         const mins = Math.floor(seconds / 60)
         const secs = seconds % 60
         return `${mins}:${secs.toString().padStart(2, '0')} remaining`
       }
       ```

    4. **Timer placement per D-08**:
       - Small sticker on header with accent color
       - Use text-accent color class (prominent but not obtrusive)
       - Font size: text-sm or text-base
       - Font weight: font-semibold

    5. **No color change per D-10**:
       - Keep accent color throughout entire 45 minutes
       - No red/orange urgency signals
       - Consistent, less stressful for ADHD users

    Use date-fns for date arithmetic (addMinutes, differenceInSeconds). If not available, use native Date math.

    **PLACEMENT**: Put timer in page header (top of screen), not in video grid. Use absolute positioning or header slot.

    **CLEANUP**: Clear interval in useEffect cleanup.
  </action>
  <verify>
    <automated>test -f src/components/room/SessionTimer.tsx && grep -E "(formatTime|remaining|setInterval)" src/components/room/SessionTimer.tsx</automated>
  </verify>
  <done>src/components/room/SessionTimer.tsx displays countdown timer in "MM:SS remaining" format</done>
</task>

<task type="auto">
  <name>Task 5: Create ConnectionStatus component with quality indicator</name>
  <files>src/components/room/ConnectionStatus.tsx</files>
  <action>
    Create src/components/room/ConnectionStatus.tsx with subtle quality indicator:

    1. **Props interface**:
       - quality: 'good' | 'degraded' | 'poor' (from useConnectionQuality hook)
       - bitrate?: number
       - packetLoss?: number
       - connectionType?: 'host' | 'srflx' | 'relay'

    2. **Subtle status indicator per D-11**:
       - Small dot (w-2 h-2 rounded-full) next to user's name
       - Color: green (bg-green-500), yellow (bg-yellow-500), red (bg-red-500)
       - Positioned in header (top-right or near session timer)

    3. **Tooltip on hover**:
       - Show details on hover (bitrate, packet loss, connection type)
       - Use shadcn/ui Tooltip component
       - Format: "Bitrate: {bitrate} kbps, Loss: {packetLoss}%, Type: {connectionType}"
       - Small text (text-xs)

    4. **No intrusive alerts per D-11**:
       - Silent reconnection in background (use existing Socket.IO reconnection from Phase 3)
       - No full-screen notifications or popups
       - No sound alerts

    5. **Connection type explanation**:
       - host: Direct connection (best)
       - srflx: STUN-assisted (good)
       - relay: TURN-relayed (acceptable, but higher latency)

    Use lucide-react icons (Signal, Wifi) for visual indicator. Use shadcn/ui Tooltip component.

    **IMPLEMENTATION NOTE**: This is Claude's discretion per D-11. Keep it minimal and non-distracting.
  </action>
  <verify>
    <automated>test -f src/components/room/ConnectionStatus.tsx && grep -E "(quality|bitrate|packetLoss|Tooltip)" src/components/room/ConnectionStatus.tsx</automated>
  </verify>
  <done>src/components/room/ConnectionStatus.tsx displays subtle connection quality indicator</done>
</task>

<task type="auto">
  <name>Task 6: Create video room page layout</name>
  <files>src/app/room/[roomId]/page.tsx</files>
  <action>
    Create or update src/app/room/[roomId]/page.tsx with complete video room layout:

    1. **Layout structure**:
       - Header: Session timer + connection status + user info
       - Main: VideoGrid component
       - Overlay: ControlBar (fixed at bottom)
       - Sidebar: Chat (from Phase 3, if visible)

    2. **Integrate components**:
       - Wrap with WebRTCContextProvider (from plan 04-03)
       - Use useMediaStream for local stream
       - Use useWebRTCConnection for producers/consumers
       - Pass props to child components

    3. **Room join logic**:
       - Connect to room via Socket.IO (useSocket hook from Phase 3)
       - Get router RTP capabilities
       - Create transport and producers
       - Handle errors (show toast notifications)

    4. **Room leave logic**:
       - Close all producers/consumers/transport
       - Disconnect from Socket.IO room
       - Navigate to dashboard or home

    5. **Error handling**:
       - Show toast for getUserMedia permission denied
       - Show toast for WebRTC connection failures
       - Show toast for transport creation failures

    Use existing room page from Phase 2 as base. Add video components on top.

    **RESPONSIVE**: Ensure layout works on mobile (stack components vertically, adjust grid cols).
  </action>
  <verify>
    <automated>test -f src/app/room/[roomId]/page.tsx && grep -E "(VideoGrid|ControlBar|SessionTimer|ConnectionStatus)" src/app/room/[roomId]/page.tsx</automated>
  </verify>
  <done>src/app/room/[roomId]/page.tsx integrates all video room components</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → WebRTC state | Untrusted user input (mute/camera toggle) |
| UI → captain controls | Only captains can mute other participants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Spoofing | Captain impersonation | mitigate | Verify captain permission server-side before allowing remote mute (check in Socket.IO handler) |
| T-04-21 | Tampering | UI state manipulation | accept | Client-side only, no security impact (mute/camera buttons are local) |
| T-04-22 | Denial of Service | Excessive re-renders | mitigate | Use React.memo for VideoCard, prevent unnecessary re-renders on participant state changes |
| T-04-23 | Information Disclosure | Participant info leakage | accept | Participant info already in roomStore (names, photos), no sensitive data exposed |
</threat_model>

<verification>
1. VideoGrid displays 1-12 participants in auto-responsive layout
2. Grid adapts based on participant count (1-3, 4-6, 7-9, 10-12)
3. VideoCard displays participant name, photo, video stream
4. Active speaker has 2px solid border (no animation)
5. ControlBar displays 4 buttons (Mute, Camera, Leave, Settings)
6. Mute/Camera buttons show red background when off
7. SessionTimer displays "MM:SS remaining" format with accent color
8. ConnectionStatus displays subtle dot indicator with tooltip
9. All components use shadcn/ui primitives
10. Layout works on mobile devices
</verification>

<success_criteria>
1. src/components/room/VideoGrid.tsx renders auto-responsive grid for 1-12 participants
2. src/components/room/VideoCard.tsx displays participant video with speaker border highlight
3. src/components/room/ControlBar.tsx displays bottom control bar with 4 buttons
4. src/components/room/SessionTimer.tsx displays countdown timer in "MM:SS remaining" format
5. src/components/room/ConnectionStatus.tsx displays subtle connection quality indicator
6. src/app/room/[roomId]/page.tsx integrates all components in complete layout
7. All UX decisions implemented per D-01 through D-11
8. Components use shadcn/ui primitives and Tailwind CSS
9. Layout is responsive on mobile devices
10. Ready for room capacity enforcement and overflow rooms (plan 04-06)
</success_criteria>

<output>
After completion, create `.planning/phases/04-webrtc-integration/04-05-SUMMARY.md`
</output>
